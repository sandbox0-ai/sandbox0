---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: sandbox0infras.infra.sandbox0.ai
spec:
  group: infra.sandbox0.ai
  names:
    kind: Sandbox0Infra
    listKind: Sandbox0InfraList
    plural: sandbox0infras
    shortNames:
    - s0i
    singular: sandbox0infra
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.progress
      name: Progress
      type: string
    - jsonPath: .status.lastMessage
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Sandbox0Infra is the Schema for the sandbox0infras API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Sandbox0InfraSpec defines the desired state of Sandbox0Infra
            properties:
              builtinTemplates:
                default: []
                description: BuiltinTemplates defines system builtin templates to
                  seed the template store
                items:
                  description: BuiltinTemplateConfig defines a system builtin template.
                  properties:
                    description:
                      type: string
                    displayName:
                      type: string
                    image:
                      type: string
                    pool:
                      description: BuiltinTemplatePoolConfig holds pool defaults for
                        builtin templates.
                      properties:
                        maxIdle:
                          default: 5
                          format: int32
                          type: integer
                        minIdle:
                          default: 1
                          format: int32
                          type: integer
                      type: object
                    templateId:
                      type: string
                  required:
                  - templateId
                  type: object
                type: array
              cluster:
                description: Cluster configures cluster identification and capacity
                properties:
                  capacity:
                    description: Capacity specifies cluster resource capacity
                    properties:
                      cpu:
                        description: CPU specifies CPU capacity
                        properties:
                          available:
                            description: Available is the available capacity
                            type: string
                          total:
                            description: Total is the total capacity
                            type: string
                        type: object
                      maxSandboxes:
                        description: MaxSandboxes is the maximum number of sandboxes
                        format: int32
                        type: integer
                      memory:
                        description: Memory specifies memory capacity
                        properties:
                          available:
                            description: Available is the available capacity
                            type: string
                          total:
                            description: Total is the total capacity
                            type: string
                        type: object
                    type: object
                  id:
                    description: ID is the unique cluster identifier
                    type: string
                  name:
                    description: Name is the human-readable cluster name
                    type: string
                required:
                - id
                type: object
              controlPlane:
                description: ControlPlane configures external control plane connection.
                properties:
                  internalAuthPublicKeySecret:
                    description: InternalAuthPublicKeySecret references the secret
                      containing control plane's public key
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the name of the secret
                        type: string
                    type: object
                  url:
                    description: URL is the control plane edge-gateway URL
                    type: string
                required:
                - internalAuthPublicKeySecret
                - url
                type: object
              database:
                description: Database configures the main database for sandbox0
                properties:
                  builtin:
                    default: {}
                    description: Builtin configures the built-in single-node PostgreSQL
                    properties:
                      database:
                        default: sandbox0
                        description: Database specifies the database name
                        type: string
                      enabled:
                        default: true
                        description: Enabled enables the built-in database
                        type: boolean
                      image:
                        default: postgres:16-alpine
                        description: Image specifies the postgres image for the builtin
                          database
                        type: string
                      persistence:
                        description: Persistence configures database storage
                        properties:
                          enabled:
                            default: true
                            description: Enabled enables persistence
                            type: boolean
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 20Gi
                            description: Size specifies the storage size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: StorageClass specifies the storage class
                              (empty for default)
                            type: string
                        type: object
                      port:
                        default: 5432
                        description: Port specifies the database port
                        format: int32
                        type: integer
                      sslMode:
                        default: disable
                        description: SSLMode specifies the SSL mode for builtin DSN
                        type: string
                      username:
                        default: sandbox0
                        description: Username specifies the database username
                        type: string
                    type: object
                  external:
                    description: External configures connection to external database
                    properties:
                      database:
                        description: Database specifies the database name
                        type: string
                      host:
                        description: Host specifies the database host
                        type: string
                      passwordSecret:
                        description: PasswordSecret references the secret containing
                          the password
                        properties:
                          key:
                            default: password
                            description: Key is the key in the secret
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                        type: object
                      port:
                        default: 5432
                        description: Port specifies the database port
                        format: int32
                        type: integer
                      sslMode:
                        default: require
                        description: SSLMode specifies the SSL mode for connection
                        type: string
                      username:
                        description: Username specifies the database username
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - username
                    type: object
                  type:
                    default: builtin
                    description: 'Type specifies the postgres database type: builtin,
                      or external'
                    enum:
                    - builtin
                    - external
                    type: string
                type: object
              initUser:
                description: InitUser configures the initial admin user
                properties:
                  email:
                    description: Email is the admin user's email
                    type: string
                  name:
                    description: Name is the admin user's display name
                    type: string
                  passwordSecret:
                    description: PasswordSecret references the secret containing the
                      password
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the name of the secret
                        type: string
                    type: object
                type: object
              internalAuth:
                description: InternalAuth configures internal authentication keys
                properties:
                  controlPlane:
                    description: ControlPlane configures control plane key pair
                    properties:
                      generate:
                        default: true
                        description: Generate enables automatic key generation
                        type: boolean
                      secretRef:
                        description: SecretRef references an existing secret containing
                          the key pair
                        properties:
                          name:
                            description: Name is the name of the secret
                            type: string
                          privateKeyKey:
                            default: private.key
                            description: PrivateKeyKey is the key for private key
                            type: string
                          publicKeyKey:
                            default: public.key
                            description: PublicKeyKey is the key for public key
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  dataPlane:
                    description: DataPlane configures data plane key pair
                    properties:
                      generate:
                        default: true
                        description: Generate enables automatic key generation
                        type: boolean
                      secretRef:
                        description: SecretRef references an existing secret containing
                          the key pair
                        properties:
                          name:
                            description: Name is the name of the secret
                            type: string
                          privateKeyKey:
                            default: private.key
                            description: PrivateKeyKey is the key for private key
                            type: string
                          publicKeyKey:
                            default: public.key
                            description: PublicKeyKey is the key for public key
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                type: object
              juicefsDatabase:
                description: JuicefsDatabase configures the JuiceFS metadata database
                properties:
                  external:
                    description: External configures an independent database for JuiceFS
                    properties:
                      database:
                        description: Database specifies the database name
                        type: string
                      host:
                        description: Host specifies the database host
                        type: string
                      passwordSecret:
                        description: PasswordSecret references the secret containing
                          the password
                        properties:
                          key:
                            default: password
                            description: Key is the key in the secret
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                        type: object
                      port:
                        default: 5432
                        description: Port specifies the database port
                        format: int32
                        type: integer
                      sslMode:
                        default: require
                        description: SSLMode specifies the SSL mode for connection
                        type: string
                      username:
                        description: Username specifies the database username
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - username
                    type: object
                  shareWithMain:
                    default: true
                    description: ShareWithMain uses the main database for JuiceFS
                      metadata
                    type: boolean
                type: object
              publicExposure:
                description: PublicExposure configures public URL exposure for sandboxes
                properties:
                  enabled:
                    default: true
                    description: Enabled enables public exposure routing
                    type: boolean
                  regionId:
                    default: aws-us-east-1
                    description: |-
                      RegionID is the DNS-safe region label used in public URLs.
                      It is not the canonical multi-region tenancy identifier.
                    type: string
                  rootDomain:
                    default: sandbox0.app
                    description: RootDomain is the root domain for public exposure
                      URLs
                    type: string
                type: object
              region:
                description: Region identifies the region for multi-cluster deployments
                type: string
              registry:
                description: Registry configures the container registry
                properties:
                  aliyun:
                    description: Aliyun configures Aliyun registry integration.
                    properties:
                      credentialsSecret:
                        description: CredentialsSecret references Aliyun credentials
                          for short-lived tokens.
                        properties:
                          accessKeyKey:
                            default: accessKeyId
                            description: AccessKeyKey is the key for access key ID.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                          secretKeyKey:
                            default: accessKeySecret
                            description: SecretKeyKey is the key for secret access
                              key.
                            type: string
                        required:
                        - name
                        type: object
                      instanceId:
                        description: InstanceID specifies the ACR instance ID.
                        type: string
                      pullSecret:
                        description: PullSecret references the dockerconfigjson secret
                          to use for image pulls.
                        properties:
                          key:
                            default: .dockerconfigjson
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      region:
                        description: Region specifies the Aliyun region.
                        type: string
                      registry:
                        description: Registry specifies the registry hostname.
                        type: string
                    required:
                    - credentialsSecret
                    - instanceId
                    - pullSecret
                    - region
                    - registry
                    type: object
                  aws:
                    description: AWS configures AWS registry integration.
                    properties:
                      credentialsSecret:
                        description: CredentialsSecret references AWS credentials
                          for short-lived tokens.
                        properties:
                          accessKeyKey:
                            default: accessKeyId
                            description: AccessKeyKey is the key for access key ID.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                          secretKeyKey:
                            default: secretAccessKey
                            description: SecretKeyKey is the key for secret access
                              key.
                            type: string
                          sessionTokenKey:
                            description: SessionTokenKey is the key for session token
                              (optional).
                            type: string
                        required:
                        - name
                        type: object
                      pullSecret:
                        description: PullSecret references the dockerconfigjson secret
                          to use for image pulls.
                        properties:
                          key:
                            default: .dockerconfigjson
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      region:
                        description: Region specifies the AWS region.
                        type: string
                      registry:
                        description: Registry specifies the registry hostname.
                        type: string
                      registryId:
                        description: RegistryID specifies the AWS account ID (optional).
                        type: string
                    required:
                    - credentialsSecret
                    - pullSecret
                    - region
                    type: object
                  azure:
                    description: Azure configures Azure registry integration.
                    properties:
                      credentialsSecret:
                        description: CredentialsSecret references the client credentials
                          for ACR.
                        properties:
                          clientIdKey:
                            default: clientId
                            description: ClientIDKey is the key for client ID.
                            type: string
                          clientSecretKey:
                            default: clientSecret
                            description: ClientSecretKey is the key for client secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                          tenantIdKey:
                            default: tenantId
                            description: TenantIDKey is the key for tenant ID.
                            type: string
                        required:
                        - name
                        type: object
                      pullSecret:
                        description: PullSecret references the dockerconfigjson secret
                          to use for image pulls.
                        properties:
                          key:
                            default: .dockerconfigjson
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      registry:
                        description: Registry specifies the registry hostname.
                        type: string
                    required:
                    - credentialsSecret
                    - pullSecret
                    - registry
                    type: object
                  builtin:
                    description: Builtin configures the built-in registry.
                    properties:
                      credentialsSecret:
                        description: |-
                          CredentialsSecret references the secret containing registry credentials.
                          If omitted, the operator will generate a secret named "<infra-name>-registry-credentials".
                        properties:
                          name:
                            description: Name is the name of the secret.
                            type: string
                          passwordKey:
                            default: password
                            description: PasswordKey is the key for password.
                            type: string
                          usernameKey:
                            default: username
                            description: UsernameKey is the key for username.
                            type: string
                        required:
                        - name
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables the built-in registry.
                        type: boolean
                      image:
                        default: registry:2.8.3
                        description: Image specifies the registry image.
                        type: string
                      ingress:
                        description: Ingress configures ingress settings for external
                          registry access.
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      persistence:
                        description: Persistence configures registry persistence.
                        properties:
                          enabled:
                            default: true
                            description: Enabled enables persistence
                            type: boolean
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 20Gi
                            description: Size specifies the storage size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: StorageClass specifies the storage class
                              (empty for default)
                            type: string
                        type: object
                      port:
                        default: 5000
                        description: Port specifies the registry port.
                        format: int32
                        type: integer
                      pushEndpoint:
                        description: |-
                          PushEndpoint overrides the external registry endpoint used for image push credentials.
                          Use host[:port] format, without scheme.
                        type: string
                      service:
                        description: Service configures the registry service exposure.
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  gcp:
                    description: GCP configures GCP registry integration.
                    properties:
                      pullSecret:
                        description: PullSecret references the dockerconfigjson secret
                          to use for image pulls.
                        properties:
                          key:
                            default: .dockerconfigjson
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      registry:
                        description: Registry specifies the registry hostname.
                        type: string
                      serviceAccountSecret:
                        description: ServiceAccountSecret references the service account
                          JSON key.
                        properties:
                          key:
                            default: serviceAccount.json
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - pullSecret
                    - registry
                    - serviceAccountSecret
                    type: object
                  harbor:
                    description: Harbor configures Harbor registry integration.
                    properties:
                      credentialsSecret:
                        description: CredentialsSecret references Harbor credentials
                          for push authentication.
                        properties:
                          name:
                            description: Name is the name of the secret.
                            type: string
                          passwordKey:
                            default: password
                            description: PasswordKey is the key for password.
                            type: string
                          usernameKey:
                            default: username
                            description: UsernameKey is the key for username.
                            type: string
                        required:
                        - name
                        type: object
                      pullSecret:
                        description: PullSecret references the dockerconfigjson secret
                          to use for image pulls.
                        properties:
                          key:
                            default: .dockerconfigjson
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      registry:
                        description: Registry specifies the registry hostname.
                        type: string
                    required:
                    - credentialsSecret
                    - pullSecret
                    - registry
                    type: object
                  imagePullSecretName:
                    default: sandbox0-registry-pull
                    description: ImagePullSecretName is the secret name to create
                      in template namespaces.
                    type: string
                  provider:
                    allOf:
                    - enum:
                      - builtin
                      - aws
                      - gcp
                      - azure
                      - aliyun
                      - harbor
                    - enum:
                      - builtin
                      - aws
                      - gcp
                      - azure
                      - aliyun
                      - harbor
                    default: builtin
                    description: 'Provider specifies the registry provider: builtin,
                      aws, gcp, azure, aliyun, or harbor.'
                    type: string
                type: object
              services:
                default: {}
                description: Services configures individual services
                properties:
                  edgeGateway:
                    default: {}
                    description: EdgeGateway configures the edge-gateway service (control
                      plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains edge-gateway specific configuration
                        properties:
                          authMode:
                            default: self_hosted
                            description: |-
                              AuthMode controls how human-facing authentication is handled.
                              Allowed values: "self_hosted", "federated_global".
                            enum:
                            - self_hosted
                            - federated_global
                            type: string
                          baseUrl:
                            default: http://localhost:8080
                            description: Base URL for OIDC callbacks
                            type: string
                          builtInAuth:
                            default: {}
                            description: Built-in Authentication
                            properties:
                              adminOnly:
                                description: AdminOnly restricts built-in auth to
                                  admin accounts only
                                type: boolean
                              allowRegistration:
                                description: AllowRegistration allows new users to
                                  register
                                type: boolean
                              emailVerificationRequired:
                                description: EmailVerificationRequired requires email
                                  verification
                                type: boolean
                              enabled:
                                default: true
                                description: Enabled enables built-in email/password
                                  authentication
                                type: boolean
                            type: object
                          clusterCacheTTL:
                            default: 30s
                            description: Cache configuration
                            type: string
                          databaseMaxConns:
                            default: 30
                            type: integer
                          databaseMinConns:
                            default: 8
                            type: integer
                          defaultTeamName:
                            default: Personal Team
                            description: Identity and Teams
                            type: string
                          edition:
                            default: self-hosted
                            description: 'Edition: "saas" or "self-hosted"'
                            type: string
                          httpPort:
                            default: 8080
                            description: Server configuration
                            type: integer
                          internalAuthCaller:
                            default: edge-gateway
                            type: string
                          internalAuthTTL:
                            default: 30s
                            description: Internal Authentication
                            type: string
                          jwtAccessTokenTTL:
                            default: 15m
                            type: string
                          jwtIssuer:
                            type: string
                          jwtRefreshTokenTTL:
                            default: 168h
                            type: string
                          logLevel:
                            default: info
                            type: string
                          oidcProviders:
                            description: OIDC Providers
                            items:
                              description: OIDCProviderConfig configures an OIDC provider.
                              properties:
                                autoProvision:
                                  description: AutoProvision automatically creates
                                    users on first login
                                  type: boolean
                                clientId:
                                  description: ClientID is the OAuth client ID
                                  type: string
                                clientSecret:
                                  description: ClientSecret is the OAuth client secret
                                  type: string
                                discoveryUrl:
                                  description: DiscoveryURL is the OIDC discovery
                                    URL (.well-known/openid-configuration)
                                  type: string
                                enabled:
                                  description: Enabled toggles the provider
                                  type: boolean
                                id:
                                  description: ID is the unique identifier for the
                                    provider (e.g., "github", "google")
                                  type: string
                                name:
                                  description: Name is the display name
                                  type: string
                                scopes:
                                  default:
                                  - openid
                                  - email
                                  - profile
                                  description: Scopes are the OAuth scopes to request
                                  items:
                                    type: string
                                  type: array
                                teamMapping:
                                  description: TeamMapping configures automatic team
                                    assignment
                                  properties:
                                    defaultRole:
                                      description: DefaultRole is the role assigned
                                        to new users
                                      type: string
                                    defaultTeamId:
                                      description: DefaultTeamID is the team to add
                                        users to
                                      type: string
                                    domain:
                                      description: Domain filters users by email domain
                                      type: string
                                  type: object
                              type: object
                            type: array
                          oidcStateCleanupInterval:
                            default: 5m
                            type: string
                          oidcStateTTL:
                            default: 10m
                            type: string
                          proxyTimeout:
                            default: 30s
                            description: Timeouts
                            type: string
                          rateLimitBurst:
                            default: 200
                            type: integer
                          rateLimitCleanupInterval:
                            default: 10m
                            type: string
                          rateLimitRPS:
                            default: 100
                            description: Rate limiting
                            type: integer
                          regionId:
                            description: |-
                              RegionID is the canonical region identifier used by tenancy and routing
                              contracts, for example "aws/us-east-1".
                            type: string
                          schedulerEnabled:
                            description: Scheduler configuration (optional, for multi-cluster
                              mode)
                            type: boolean
                          schedulerUrl:
                            type: string
                          serverIdleTimeout:
                            default: 120s
                            type: string
                          serverReadTimeout:
                            default: 30s
                            type: string
                          serverWriteTimeout:
                            default: 60s
                            type: string
                          shutdownTimeout:
                            default: 30s
                            type: string
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  globalDirectory:
                    default: {}
                    description: GlobalDirectory configures the global-directory service
                      (global service layer)
                    properties:
                      config:
                        default: {}
                        description: Config contains global-directory specific configuration
                        properties:
                          baseUrl:
                            default: http://localhost:8080
                            description: Base URL for OIDC callbacks
                            type: string
                          builtInAuth:
                            default: {}
                            description: Built-in Authentication
                            properties:
                              adminOnly:
                                description: AdminOnly restricts built-in auth to
                                  admin accounts only
                                type: boolean
                              allowRegistration:
                                description: AllowRegistration allows new users to
                                  register
                                type: boolean
                              emailVerificationRequired:
                                description: EmailVerificationRequired requires email
                                  verification
                                type: boolean
                              enabled:
                                default: true
                                description: Enabled enables built-in email/password
                                  authentication
                                type: boolean
                            type: object
                          databaseMaxConns:
                            default: 30
                            type: integer
                          databaseMinConns:
                            default: 8
                            type: integer
                          databaseSchema:
                            default: gd
                            type: string
                          defaultTeamName:
                            default: Personal Team
                            description: Identity and Teams
                            type: string
                          httpPort:
                            default: 8080
                            type: integer
                          jwtAccessTokenTTL:
                            default: 15m
                            type: string
                          jwtIssuer:
                            type: string
                          jwtRefreshTokenTTL:
                            default: 168h
                            type: string
                          logLevel:
                            default: info
                            type: string
                          oidcProviders:
                            description: OIDC Providers
                            items:
                              description: OIDCProviderConfig configures an OIDC provider.
                              properties:
                                autoProvision:
                                  description: AutoProvision automatically creates
                                    users on first login
                                  type: boolean
                                clientId:
                                  description: ClientID is the OAuth client ID
                                  type: string
                                clientSecret:
                                  description: ClientSecret is the OAuth client secret
                                  type: string
                                discoveryUrl:
                                  description: DiscoveryURL is the OIDC discovery
                                    URL (.well-known/openid-configuration)
                                  type: string
                                enabled:
                                  description: Enabled toggles the provider
                                  type: boolean
                                id:
                                  description: ID is the unique identifier for the
                                    provider (e.g., "github", "google")
                                  type: string
                                name:
                                  description: Name is the display name
                                  type: string
                                scopes:
                                  default:
                                  - openid
                                  - email
                                  - profile
                                  description: Scopes are the OAuth scopes to request
                                  items:
                                    type: string
                                  type: array
                                teamMapping:
                                  description: TeamMapping configures automatic team
                                    assignment
                                  properties:
                                    defaultRole:
                                      description: DefaultRole is the role assigned
                                        to new users
                                      type: string
                                    defaultTeamId:
                                      description: DefaultTeamID is the team to add
                                        users to
                                      type: string
                                    domain:
                                      description: Domain filters users by email domain
                                      type: string
                                  type: object
                              type: object
                            type: array
                          oidcStateCleanupInterval:
                            default: 5m
                            type: string
                          oidcStateTTL:
                            default: 10m
                            type: string
                          rateLimitBurst:
                            default: 200
                            type: integer
                          rateLimitCleanupInterval:
                            default: 10m
                            type: string
                          rateLimitRPS:
                            default: 100
                            description: Rate limiting
                            type: integer
                          regionId:
                            description: |-
                              RegionID is the canonical region identifier used by tenancy and routing
                              contracts, for example "aws/us-east-1".
                            type: string
                          regionTokenTTL:
                            default: 5m
                            type: string
                          serverIdleTimeout:
                            default: 120s
                            type: string
                          serverReadTimeout:
                            default: 30s
                            type: string
                          serverWriteTimeout:
                            default: 60s
                            type: string
                          shutdownTimeout:
                            default: 30s
                            type: string
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  internalGateway:
                    default: {}
                    description: InternalGateway configures the internal-gateway service
                      (data plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains internal-gateway specific configuration
                        properties:
                          allowedCallers:
                            default:
                            - edge-gateway
                            - scheduler
                            description: |-
                              AllowedCallers is the list of services allowed to call internal-gateway
                              Default: ["edge-gateway"], can include "scheduler" for multi-cluster mode
                            items:
                              type: string
                            type: array
                          authMode:
                            default: internal
                            description: |-
                              Internal authentication (for validating requests from edge-gateway and
                              generating tokens for downstream services)
                              AuthMode controls which authentication modes are accepted on /api/v1.
                              Allowed values: "internal", "public", "both".
                            enum:
                            - internal
                            - public
                            - both
                            type: string
                          baseUrl:
                            default: http://localhost:8080
                            description: Base URL for OIDC callbacks
                            type: string
                          builtInAuth:
                            default: {}
                            description: Built-in Authentication
                            properties:
                              adminOnly:
                                description: AdminOnly restricts built-in auth to
                                  admin accounts only
                                type: boolean
                              allowRegistration:
                                description: AllowRegistration allows new users to
                                  register
                                type: boolean
                              emailVerificationRequired:
                                description: EmailVerificationRequired requires email
                                  verification
                                type: boolean
                              enabled:
                                default: true
                                description: Enabled enables built-in email/password
                                  authentication
                                type: boolean
                            type: object
                          databaseMaxConns:
                            default: 30
                            type: integer
                          databaseMinConns:
                            default: 8
                            type: integer
                          defaultTeamName:
                            default: Personal Team
                            description: Identity and Teams
                            type: string
                          healthCheckPeriod:
                            default: 10s
                            type: string
                          httpPort:
                            default: 8443
                            description: Server configuration
                            type: integer
                          jwtAccessTokenTTL:
                            default: 15m
                            type: string
                          jwtIssuer:
                            type: string
                          jwtRefreshTokenTTL:
                            default: 168h
                            type: string
                          logLevel:
                            default: info
                            type: string
                          oidcProviders:
                            description: OIDC Providers
                            items:
                              description: OIDCProviderConfig configures an OIDC provider.
                              properties:
                                autoProvision:
                                  description: AutoProvision automatically creates
                                    users on first login
                                  type: boolean
                                clientId:
                                  description: ClientID is the OAuth client ID
                                  type: string
                                clientSecret:
                                  description: ClientSecret is the OAuth client secret
                                  type: string
                                discoveryUrl:
                                  description: DiscoveryURL is the OIDC discovery
                                    URL (.well-known/openid-configuration)
                                  type: string
                                enabled:
                                  description: Enabled toggles the provider
                                  type: boolean
                                id:
                                  description: ID is the unique identifier for the
                                    provider (e.g., "github", "google")
                                  type: string
                                name:
                                  description: Name is the display name
                                  type: string
                                scopes:
                                  default:
                                  - openid
                                  - email
                                  - profile
                                  description: Scopes are the OAuth scopes to request
                                  items:
                                    type: string
                                  type: array
                                teamMapping:
                                  description: TeamMapping configures automatic team
                                    assignment
                                  properties:
                                    defaultRole:
                                      description: DefaultRole is the role assigned
                                        to new users
                                      type: string
                                    defaultTeamId:
                                      description: DefaultTeamID is the team to add
                                        users to
                                      type: string
                                    domain:
                                      description: Domain filters users by email domain
                                      type: string
                                  type: object
                              type: object
                            type: array
                          oidcStateCleanupInterval:
                            default: 5m
                            type: string
                          oidcStateTTL:
                            default: 10m
                            type: string
                          procdStoragePermissions:
                            default:
                            - sandboxvolume:read
                            - sandboxvolume:write
                            items:
                              type: string
                            type: array
                          proxyTimeout:
                            default: 10s
                            description: Proxy configuration
                            type: string
                          rateLimitBurst:
                            default: 200
                            type: integer
                          rateLimitCleanupInterval:
                            default: 10m
                            type: string
                          rateLimitRPS:
                            default: 100
                            description: Rate limiting
                            type: integer
                          regionId:
                            description: |-
                              RegionID is the canonical region identifier used by tenancy and routing
                              contracts, for example "aws/us-east-1".
                            type: string
                          schedulerPermissions:
                            default:
                            - '*:*'
                            description: Permissions
                            items:
                              type: string
                            type: array
                          shutdownTimeout:
                            default: 30s
                            description: Timeouts
                            type: string
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  manager:
                    default: {}
                    description: Manager configures the manager service (data plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains manager specific configuration
                        properties:
                          autoscaler:
                            default: {}
                            description: Autoscaler config for pool scaling behavior
                            properties:
                              maxScaleStep:
                                default: 10
                                description: MaxScaleStep caps the maximum pods to
                                  add in a single scale operation.
                                format: int32
                                type: integer
                              minIdleBuffer:
                                default: 2
                                description: |-
                                  MinIdleBuffer is the minimum number of idle pods to maintain above minIdle.
                                  When idle count drops to minIdle + MinIdleBuffer, proactive scaling kicks in.
                                format: int32
                                type: integer
                              minScaleInterval:
                                default: 100ms
                                description: |-
                                  MinScaleInterval is the minimum time between scale operations for a template.
                                  This prevents thundering herd when multiple cold claims arrive simultaneously.
                                type: string
                              noTrafficScaleDownAfter:
                                default: 10m
                                description: |-
                                  NoTrafficScaleDownAfter is the duration without any claims before scaling down.
                                  Scale down is still async and happens in the background reconcile loop.
                                type: string
                              scaleDownPercent:
                                default: "0.1"
                                description: ScaleDownPercent is the percentage to
                                  reduce replicas during scale down.
                                type: string
                              scaleUpFactor:
                                default: "1.5"
                                description: |-
                                  ScaleUpFactor determines how aggressively to scale up.
                                  When cold claim occurs, newReplicas = current * ScaleUpFactor.
                                type: string
                              targetIdleRatio:
                                default: "0.2"
                                description: |-
                                  TargetIdleRatio is the target ratio of idle pods to active pods.
                                  Formula: desiredIdle = active * TargetIdleRatio
                                type: string
                            type: object
                          cleanupInterval:
                            default: 60s
                            description: Cleanup Controller
                            type: string
                          databaseMaxConns:
                            default: 10
                            format: int32
                            type: integer
                          databaseMinConns:
                            default: 2
                            format: int32
                            type: integer
                          defaultSandboxTTL:
                            default: 0s
                            description: Sandbox
                            type: string
                          httpPort:
                            default: 8080
                            description: HTTP Server
                            type: integer
                          kubeConfig:
                            description: Kubernetes
                            type: string
                          leaderElection:
                            default: true
                            type: boolean
                          logLevel:
                            default: info
                            description: Logging
                            type: string
                          metricsPort:
                            default: 9090
                            description: Metrics
                            type: integer
                          netdPolicyApplyPollInterval:
                            default: 500ms
                            type: string
                          netdPolicyApplyTimeout:
                            default: 30s
                            description: Netd apply wait
                            type: string
                          pauseMemoryBufferRatio:
                            default: "1.1"
                            type: string
                          pauseMinCPU:
                            default: 10m
                            type: string
                          pauseMinMemoryLimit:
                            default: 32Mi
                            type: string
                          pauseMinMemoryRequest:
                            default: 10Mi
                            description: Pause/Resume
                            type: string
                          procdClientTimeout:
                            default: 30s
                            description: Timeouts
                            type: string
                          procdConfig:
                            default: {}
                            description: Procd config injected into sandbox pods
                            properties:
                              cacheMaxBytes:
                                default: 104857600
                                description: Cache configuration
                                format: int64
                                type: integer
                              cacheTTL:
                                default: 30s
                                type: string
                              contextCleanupInterval:
                                default: 30s
                                description: Context cleanup configuration
                                type: string
                              contextFinishedTTL:
                                default: 0s
                                type: string
                              contextIdleTimeout:
                                default: 0s
                                type: string
                              contextMaxLifetime:
                                default: 0s
                                type: string
                              httpPort:
                                default: 49983
                                description: Server configuration
                                type: integer
                              juicefsBufferSize:
                                default: "300"
                                type: string
                              juicefsCacheSize:
                                default: "100"
                                description: JuiceFS Mount Defaults
                                type: string
                              juicefsPrefetch:
                                default: 3
                                type: integer
                              juicefsWriteback:
                                default: true
                                type: boolean
                              logLevel:
                                default: info
                                type: string
                              rootPath:
                                default: /workspace
                                description: File manager configuration
                                type: string
                              webhookBaseBackoff:
                                default: 500ms
                                type: string
                              webhookMaxRetries:
                                default: 3
                                type: integer
                              webhookQueueSize:
                                default: 256
                                type: integer
                              webhookRequestTimeout:
                                default: 5s
                                type: string
                            type: object
                          procdInitTimeout:
                            default: 6s
                            type: string
                          resyncPeriod:
                            default: 30s
                            type: string
                          shutdownTimeout:
                            default: 30s
                            type: string
                          webhookCertPath:
                            default: /tmp/k8s-webhook-server/serving-certs/tls.crt
                            type: string
                          webhookKeyPath:
                            default: /tmp/k8s-webhook-server/serving-certs/tls.key
                            type: string
                          webhookPort:
                            default: 9443
                            description: Webhook
                            type: integer
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  netd:
                    default: {}
                    description: Netd configures the netd service (data plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains netd specific configuration
                        properties:
                          auditLogMaxBackups:
                            default: 5
                            type: integer
                          auditLogMaxBytes:
                            default: 104857600
                            format: int64
                            type: integer
                          auditLogPath:
                            type: string
                          bpfFsPath:
                            type: string
                          bpfPinPath:
                            type: string
                          burstRatio:
                            default: "0.125"
                            type: string
                          dnsPort:
                            default: 53
                            type: integer
                          edtHorizon:
                            default: 200ms
                            type: string
                          failClosed:
                            default: true
                            type: boolean
                          healthPort:
                            default: 8081
                            type: integer
                          logLevel:
                            default: info
                            type: string
                          metricsPort:
                            default: 9091
                            type: integer
                          metricsReportInterval:
                            default: 10s
                            type: string
                          nodeName:
                            type: string
                          platformAllowedCidrs:
                            description: Platform allow/deny lists (override user
                              policy)
                            items:
                              type: string
                            type: array
                          platformAllowedDomains:
                            items:
                              type: string
                            type: array
                          platformDeniedCidrs:
                            items:
                              type: string
                            type: array
                          platformDeniedDomains:
                            items:
                              type: string
                            type: array
                          preferNft:
                            default: true
                            type: boolean
                          proxyHeaderLimit:
                            format: int64
                            type: integer
                          proxyHttpPort:
                            default: 18080
                            type: integer
                          proxyHttpsPort:
                            default: 18443
                            type: integer
                          proxyListenAddr:
                            default: 0.0.0.0
                            description: Proxy settings
                            type: string
                          proxyUpstreamTimeout:
                            default: 30s
                            type: string
                          resyncPeriod:
                            default: 30s
                            type: string
                          shutdownDelay:
                            default: 2s
                            type: string
                          useEbpf:
                            description: eBPF and tc
                            type: boolean
                          useEdt:
                            type: boolean
                          vethPrefix:
                            type: string
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: |-
                          NodeSelector constrains netd onto a specific node set.
                          On GKE with gVisor sandboxes, use this to place netd onto the sandbox nodes
                          while leaving RuntimeClassName unset so netd still uses the default host runtime.
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      runtimeClassName:
                        description: |-
                          RuntimeClassName specifies the Kubernetes runtime class for the netd daemonset.
                          Use a host-compatible runtime such as runc. Do not run netd on gVisor or Kata.
                        type: string
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                      tolerations:
                        description: Tolerations allow netd to run on tainted sandbox
                          nodes.
                        items:
                          description: |-
                            The pod this Toleration is attached to tolerates any taint that matches
                            the triple <key,value,effect> using the matching operator <operator>.
                          properties:
                            effect:
                              description: |-
                                Effect indicates the taint effect to match. Empty means match all taint effects.
                                When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                              type: string
                            key:
                              description: |-
                                Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                              type: string
                            operator:
                              description: |-
                                Operator represents a key's relationship to the value.
                                Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                Exists is equivalent to wildcard for value, so that a pod can
                                tolerate all taints of a particular category.
                                Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                              type: string
                            tolerationSeconds:
                              description: |-
                                TolerationSeconds represents the period of time the toleration (which must be
                                of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                it is not set, which means tolerate the taint forever (do not evict). Zero and
                                negative values will be treated as 0 (evict immediately) by the system.
                              format: int64
                              type: integer
                            value:
                              description: |-
                                Value is the taint value the toleration matches to.
                                If the operator is Exists, the value should be empty, otherwise just a regular string.
                              type: string
                          type: object
                        type: array
                    type: object
                  scheduler:
                    description: Scheduler configures the scheduler service (control
                      plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains scheduler specific configuration
                        properties:
                          databasePool:
                            description: Database Pool configuration
                            properties:
                              maxConnIdleTime:
                                default: 5m
                                type: string
                              maxConnLifetime:
                                default: 30m
                                type: string
                              maxConns:
                                default: 10
                                format: int32
                                type: integer
                              minConns:
                                default: 2
                                format: int32
                                type: integer
                            type: object
                          httpPort:
                            default: 8080
                            description: Server configuration
                            type: integer
                          idleTimeout:
                            default: 120s
                            type: string
                          logLevel:
                            default: info
                            type: string
                          podsPerNode:
                            default: 50
                            type: integer
                          proxyTimeout:
                            default: 10s
                            type: string
                          readTimeout:
                            default: 30s
                            type: string
                          reconcileInterval:
                            default: 30s
                            description: Reconciler configuration
                            type: string
                          shutdownTimeout:
                            default: 30s
                            description: Timeouts
                            type: string
                          writeTimeout:
                            default: 60s
                            type: string
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  storageProxy:
                    default: {}
                    description: StorageProxy configures the storage-proxy service
                      (data plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains storage-proxy specific configuration
                        properties:
                          auditFile:
                            default: /var/log/storage-proxy/audit.log
                            type: string
                          auditLog:
                            default: true
                            type: boolean
                          cacheDir:
                            default: /var/lib/storage-proxy/cache
                            type: string
                          cleanupInterval:
                            default: 60s
                            type: string
                          databaseMaxConns:
                            default: 30
                            type: integer
                          databaseMinConns:
                            default: 5
                            type: integer
                          databaseSchema:
                            default: sp
                            type: string
                          defaultCacheSize:
                            default: 1G
                            type: string
                          flushTimeout:
                            default: 30s
                            type: string
                          grpcAddr:
                            default: 0.0.0.0
                            description: gRPC Server
                            type: string
                          grpcPort:
                            default: 8080
                            type: integer
                          heartbeatInterval:
                            default: 5s
                            description: Coordination
                            type: string
                          heartbeatTimeout:
                            default: 15
                            type: integer
                          httpAddr:
                            default: 0.0.0.0
                            description: HTTP Management API
                            type: string
                          httpIdleTimeout:
                            default: 60s
                            type: string
                          httpPort:
                            default: 8081
                            type: integer
                          httpReadTimeout:
                            default: 15s
                            description: Timeouts
                            type: string
                          httpWriteTimeout:
                            default: 15s
                            type: string
                          juicefsAttrTimeout:
                            default: 1s
                            type: string
                          juicefsBlockSize:
                            default: 4096
                            type: integer
                          juicefsCompression:
                            default: lz4
                            type: string
                          juicefsDirEntryTimeout:
                            default: 1s
                            type: string
                          juicefsEncryptionAlgo:
                            default: aes256gcm-rsa
                            type: string
                          juicefsEncryptionEnabled:
                            default: false
                            type: boolean
                          juicefsEncryptionKeyPath:
                            type: string
                          juicefsEncryptionPassphrase:
                            type: string
                          juicefsEntryTimeout:
                            default: 1s
                            type: string
                          juicefsMaxUpload:
                            default: 20
                            type: integer
                          juicefsMetaRetries:
                            default: 10
                            type: integer
                          juicefsName:
                            default: sandbox0
                            type: string
                          juicefsTrashDays:
                            default: 1
                            type: integer
                          kubeconfigPath:
                            description: Kubernetes
                            type: string
                          logLevel:
                            default: info
                            description: Logging
                            type: string
                          maxBytesPerSecond:
                            default: 1073741824
                            format: int64
                            type: integer
                          maxOpsPerSecond:
                            default: 10000
                            description: Rate limiting
                            type: integer
                          metricsEnabled:
                            default: true
                            description: Monitoring
                            type: boolean
                          metricsPort:
                            default: 9090
                            type: integer
                          restoreRemountTimeout:
                            default: 30s
                            description: Snapshot restore
                            type: string
                          watchEventQueueSize:
                            default: 256
                            type: integer
                          watchEventsEnabled:
                            default: true
                            description: Watch events
                            type: boolean
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                type: object
              storage:
                description: Storage configures the storage backend (JuiceFS S3 backend)
                properties:
                  builtin:
                    default: {}
                    description: Builtin configures the built-in RustFS storage
                    properties:
                      bucket:
                        default: sandbox0
                        description: Bucket specifies the default bucket name for
                          builtin storage
                        type: string
                      consoleEnabled:
                        default: true
                        description: ConsoleEnabled enables the RustFS console
                        type: boolean
                      consolePort:
                        default: 9001
                        description: ConsolePort specifies the RustFS console port
                        format: int32
                        type: integer
                      credentials:
                        description: Credentials configures access credentials (auto-generated
                          if not specified)
                        properties:
                          accessKey:
                            description: AccessKey is the access key
                            type: string
                          secretKey:
                            description: SecretKey is the secret key
                            type: string
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables the built-in storage
                        type: boolean
                      image:
                        default: rustfs/rustfs:1.0.0-alpha.79
                        description: Image specifies the RustFS image for builtin
                          storage
                        type: string
                      obsEnvironment:
                        default: develop
                        description: ObsEnvironment specifies the RustFS environment
                          label
                        type: string
                      obsLogDirectory:
                        default: /data/logs
                        description: ObsLogDirectory specifies the RustFS log directory
                        type: string
                      obsLoggerLevel:
                        default: debug
                        description: ObsLoggerLevel specifies the RustFS log level
                        type: string
                      persistence:
                        description: Persistence configures storage persistence
                        properties:
                          enabled:
                            default: true
                            description: Enabled enables persistence
                            type: boolean
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 20Gi
                            description: Size specifies the storage size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: StorageClass specifies the storage class
                              (empty for default)
                            type: string
                        type: object
                      port:
                        default: 9000
                        description: Port specifies the RustFS API port
                        format: int32
                        type: integer
                      region:
                        default: us-east-1
                        description: Region specifies the default region for builtin
                          storage
                        type: string
                      volumes:
                        default: /data
                        description: Volumes specifies the RustFS data path
                        type: string
                    type: object
                  oss:
                    description: OSS configures Aliyun OSS storage
                    properties:
                      bucket:
                        description: Bucket specifies the OSS bucket name
                        type: string
                      credentialsSecret:
                        description: CredentialsSecret references the secret containing
                          Aliyun credentials
                        properties:
                          accessKeyKey:
                            default: accessKeyId
                            description: AccessKeyKey is the key for access key ID
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                          secretKeyKey:
                            default: accessKeySecret
                            description: SecretKeyKey is the key for access key secret
                            type: string
                        required:
                        - name
                        type: object
                      endpoint:
                        description: Endpoint specifies the OSS endpoint
                        type: string
                      region:
                        description: Region specifies the Aliyun region
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    - endpoint
                    - region
                    type: object
                  s3:
                    description: S3 configures S3 or S3-compatible storage
                    properties:
                      bucket:
                        description: Bucket specifies the S3 bucket name
                        type: string
                      credentialsSecret:
                        description: CredentialsSecret references the secret containing
                          AWS credentials
                        properties:
                          accessKeyKey:
                            default: accessKeyId
                            description: AccessKeyKey is the key for access key ID
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                          secretKeyKey:
                            default: secretAccessKey
                            description: SecretKeyKey is the key for secret access
                              key
                            type: string
                        required:
                        - name
                        type: object
                      endpoint:
                        description: Endpoint specifies the S3 endpoint (optional
                          for AWS)
                        type: string
                      region:
                        description: Region specifies the AWS region
                        type: string
                      sessionTokenKey:
                        description: SessionTokenKey is the key for session token
                          in the secret (optional)
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    - region
                    type: object
                  type:
                    default: builtin
                    description: 'Type specifies the storage type: builtin, s3, or
                      oss'
                    enum:
                    - builtin
                    - s3
                    - oss
                    type: string
                type: object
            type: object
          status:
            description: Sandbox0InfraStatus defines the observed state of Sandbox0Infra
            properties:
              cluster:
                description: Cluster contains cluster registration status
                properties:
                  id:
                    description: ID is the cluster identifier
                    type: string
                  registered:
                    description: Registered indicates if the cluster is registered
                    type: boolean
                  registeredAt:
                    description: RegisteredAt is the registration timestamp
                    format: date-time
                    type: string
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the infrastructure's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endpoints:
                description: Endpoints contains the service endpoints
                properties:
                  edgeGateway:
                    description: EdgeGateway is the external edge-gateway URL
                    type: string
                  edgeGatewayInternal:
                    description: EdgeGatewayInternal is the internal edge-gateway
                      URL
                    type: string
                  globalDirectory:
                    description: GlobalDirectory is the global-directory URL
                    type: string
                  internalGateway:
                    description: InternalGateway is the internal-gateway URL
                    type: string
                type: object
              internalAuth:
                description: InternalAuth contains internal authentication status
                properties:
                  controlPlanePublicKey:
                    description: ControlPlanePublicKey references the control plane
                      public key
                    properties:
                      secretKey:
                        description: SecretKey is the key in the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - secretKey
                    - secretName
                    type: object
                  dataPlanePublicKey:
                    description: DataPlanePublicKey references the data plane public
                      key
                    properties:
                      secretKey:
                        description: SecretKey is the key in the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - secretKey
                    - secretName
                    type: object
                type: object
              lastMessage:
                description: LastMessage is the most recent status message (success
                  or error)
                type: string
              lastOperation:
                description: LastOperation contains the last operation information
                properties:
                  completedAt:
                    description: CompletedAt is when the operation completed
                    format: date-time
                    type: string
                  error:
                    description: Error contains error message if failed
                    type: string
                  startedAt:
                    description: StartedAt is when the operation started
                    format: date-time
                    type: string
                  status:
                    description: Status is the operation status
                    type: string
                  type:
                    description: Type is the operation type
                    type: string
                type: object
              observedVersion:
                description: ObservedVersion is the last observed version
                type: string
              phase:
                description: Phase represents the current phase of the infrastructure
                enum:
                - Installing
                - Ready
                - Degraded
                - Failed
                - Upgrading
                type: string
              progress:
                description: Progress shows readiness progress in "ready/total" format
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
