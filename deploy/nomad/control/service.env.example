# Copy to /etc/sandbox0/<service>.env and keep mode 0600.
CONFIG_PATH=/etc/sandbox0/<service>.yaml
# Set a stable, replica-unique value for producer and audit identity.
SANDBOX0_INSTANCE_ID=<service>-1
