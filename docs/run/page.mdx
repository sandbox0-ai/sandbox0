# Run

Sandbox0 Runs expose a production HTTP domain backed by the same Sandbox0 primitives used during development: a template, immutable snapshots, mounted volumes, and a sandbox service.

Use Sandbox Services for development and debugging domains under `sandbox0.app`. Use Runs when the service should have a stable production domain under `sandbox0.run`, immutable revisions, and scale-to-zero behavior.

## Model

| Resource | Role |
|----------|------|
| `Run` | Stable production identity, slug, public URL, enabled flag, and scale-to-zero policy |
| `RunRevision` | Immutable deployed version compiled from a source into `template + mounts + service` |
| `Snapshot` | Immutable source filesystem. At runtime it is materialized as a read-only volume mount |
| `SandboxService` | HTTP service contract: port, runtime, routes, auth, CORS, rate limits, and health check |

Runs do not introduce an artifact object. A deploy source is either an existing sandbox service or an explicit snapshot-backed spec.

## Deploy Modes

### From Sandbox Service

This is the promotion path from development to production.

Sandbox0 reads the sandbox's service definition, snapshots the sandbox mounts, and creates a run revision from that immutable state. The sandbox service keeps its development URL; the run gets a separate production URL.

### From Snapshots

This is the direct SDK and CI path.

Provide a template, one service, and snapshot mounts. Sandbox0 materializes each snapshot as a read-only volume when the run cold-starts.

The template must declare every snapshot mount path as a read-only `volumeMounts` entry. This keeps run revisions immutable after deploy. For sandbox-service promotion, mounted volumes are snapshotted and served read-only in the run runtime, so the promoted service should not require writes to those mount paths while handling production requests.

## Scale To Zero

Runs have no `minIdle` setting. Every run starts at zero runtime instances and cold-starts on the first request.

| Field | Description |
|-------|-------------|
| `max_instances` | Maximum runtime instances for the run |
| `target_concurrency` | Target request concurrency per runtime instance |
| `idle_timeout_seconds` | Idle time before runtime can return to zero |
| `startup_timeout_seconds` | Maximum time to wait for cold-start health |

## Deploy From Snapshots

<Endpoint method="POST">
/api/v1/runs/deploy
</Endpoint>

<Tabs
  tabs={[
    {
      label: "Go",
      language: "go",
      code: `result, err := client.DeployRun(ctx, sandbox0.RunDeploySpec{
    Name:     "api",
    Slug:     "api",
    Template: "python",
    Service: sandbox0.RunServiceSpec{
        ID:         "app",
        Port:       8080,
        Command:    []string{"python", "-m", "http.server", "8080"},
        CWD:        "/workspace",
        HealthPath: "/healthz",
    },
    Mounts: []sandbox0.RunSnapshotMount{
        {SnapshotID: "snap_abc123", MountPath: "/workspace"},
    },
})
if err != nil {
    log.Fatal(err)
}
fmt.Println(result.Run.URL.Or(""))`
    },
    {
      label: "Python",
      language: "python",
      code: `from sandbox0 import RunDeploySpec, RunServiceSpec, RunSnapshotMount

result = client.runs.deploy(RunDeploySpec(
    name="api",
    slug="api",
    template="python",
    service=RunServiceSpec(
        id="app",
        port=8080,
        command=["python", "-m", "http.server", "8080"],
        cwd="/workspace",
        health_path="/healthz",
    ),
    mounts=[
        RunSnapshotMount(snapshot_id="snap_abc123", mount_path="/workspace"),
    ],
))
print(result.run.url)`
    },
    {
      label: "TypeScript",
      language: "typescript",
      code: `const result = await client.runs.deploy({
    name: 'api',
    slug: 'api',
    template: 'python',
    service: {
        id: 'app',
        port: 8080,
        command: ['python', '-m', 'http.server', '8080'],
        cwd: '/workspace',
        healthPath: '/healthz',
    },
    mounts: [
        { snapshotId: 'snap_abc123', mountPath: '/workspace' },
    ],
});
console.log(result.run.url);`
    },
    {
      label: "CLI",
      language: "bash",
      code: `s0 run deploy \\
    --name api \\
    --slug api \\
    --template python \\
    --port 8080 \\
    --cmd "python -m http.server 8080" \\
    --cwd /workspace \\
    --health-path /healthz \\
    --mount snap_abc123:/workspace`
    }
  ]}
/>

## Deploy From Sandbox Service

<Endpoint method="POST">
/api/v1/runs/deploy
</Endpoint>

The request source is `sandbox_service`; the SDK and CLI expose it as a dedicated helper.

<Tabs
  tabs={[
    {
      label: "Go",
      language: "go",
      code: `result, err := client.DeployRunFromSandboxService(
    ctx,
    "sb_abc123",
    "api",
    sandbox0.WithRunName("api"),
    sandbox0.WithRunSlug("api"),
)
if err != nil {
    log.Fatal(err)
}
fmt.Println(result.Run.URL.Or(""))`
    },
    {
      label: "Python",
      language: "python",
      code: `from sandbox0 import RunDeployOptions

result = client.runs.deploy_from_sandbox_service(
    "sb_abc123",
    "api",
    RunDeployOptions(name="api", slug="api"),
)
print(result.run.url)`
    },
    {
      label: "TypeScript",
      language: "typescript",
      code: `const result = await client.runs.deployFromSandboxService(
    'sb_abc123',
    'api',
    { name: 'api', slug: 'api' },
);
console.log(result.run.url);`
    },
    {
      label: "CLI",
      language: "bash",
      code: `s0 run deploy-service sb_abc123 api --name api --slug api`
    }
  ]}
/>

## Deploy A New Revision

<Endpoint method="POST">
/api/v1/runs/{'{id}'}/deploy
</Endpoint>

Deploying to an existing run keeps its production URL stable and creates a new immutable revision.

```bash
s0 run deploy api \
    --template python \
    --port 8080 \
    --cmd "python -m http.server 8080" \
    --mount snap_def456:/workspace
```

Use `--no-activate` when you want to stage the revision before switching traffic.

## Revision Management

| Operation | Endpoint | CLI |
|-----------|----------|-----|
| List runs | `GET /api/v1/runs` | `s0 run list` |
| Get run | `GET /api/v1/runs/{'{id}'}` | `s0 run get api` |
| Update run | `PUT /api/v1/runs/{'{id}'}` | `s0 run update api --disable` |
| Delete run | `DELETE /api/v1/runs/{'{id}'}` | `s0 run delete api` |
| List revisions | `GET /api/v1/runs/{'{id}'}/revisions` | `s0 run revision list api` |
| Activate revision | `PUT /api/v1/runs/{'{id}'}/active-revision` | `s0 run revision activate api rev_abc123` |

## Run File

For CLI automation, keep the deploy spec in YAML:

```yaml
name: api
slug: api
template: python
service:
    id: app
    port: 8080
    command: ["python", "-m", "http.server", "8080"]
    cwd: /workspace
    healthPath: /healthz
mounts:
    - snapshotID: snap_abc123
      mountPath: /workspace
scale:
    maxInstances: 4
    targetConcurrency: 20
    idleTimeoutSeconds: 300
    startupTimeoutSeconds: 90
```

```bash
s0 run deploy --file run.yaml
```
