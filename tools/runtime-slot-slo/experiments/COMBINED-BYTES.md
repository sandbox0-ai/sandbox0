# D-COMBINED-BYTES: actual complete objects and corrected byte costs

2026-09-12. Diagnostic format/byte verification, not achieved startup performance.
All [fundamental requirements](STRUCTURED-OPTIMIZATION.md) remain unchanged.

## Decision

The missing-original-byte prerequisite is complete. Actual full-group objects,
combined selectors and encrypted reads validate, but they add costs omitted by
the previous coverage screen. No candidate runtime trial is admitted yet.

Continue the SAME unified policy with the existing shared 128MiB cache, including
its bounded decoded-mapping protection, checksum/length sharing, exact buffer
charges and mixed-root eviction. Do not add standalone savings or enlarge the
cache. This is the next model gate, not another original-frame export or a new
import/claim/trace campaign. Reject the variant if net costs cannot justify the
remaining gap without worsening Node or density behavior.

## Exact missing bytes, without new tenant preparation

A bounded read-only exporter fetched the 31 previously identified Coding frames,
234,751 original encoded bytes. It used the retained test environment's encryption
key in place, the existing read-only storage adapter and the unchanged product
bounded decoder/checksum verification. No credentials, private keys or private
configuration were exported. All transferred files and the result match SHA256
and size; no original frames were synthesized, split or recompressed.

The successful export recorded 44 provider-range calls: 764,453 requested and
749,345 returned ciphertext bytes. Those counts exclude an earlier failed native
launch that emitted no counters. They are not an all-attempt HTTP/retry count.
There were no object writes, new imports, claims or guest commands. These local
diagnostic fixtures do NOT authorize warming tenant data on production workers.

## Actual representation

The data-only manifest is 64 bytes plus 80 bytes per complete original member.
Each 192-byte combined selector record binds one original trigger/leaf and both
joint/data locators. Full-object hashes, independent original checksums and exact
current-parent bindings are preserved. Existing joint packets are reused and
verified. Diagnostic magic/version only; this is not a product wire contract.

| Complete topology representation | Node | Coding |
| --- | ---: | ---: |
| Actual data-only groups / unique objects | 61 | 1,789 |
| Existing verified joint records | 59 | 1,751 |
| All data-group stored bytes | 2,741,794 | 88,019,690 |
| Largest stored / decoded group, bytes | 103,437 / 853,072 | 190,385 / 984,304 |
| Combined selector raw / compressed, bytes | 11,799 / 7,192 | 343,575 / 201,304 |
| Wrapped parent stored / decoded, bytes | 7,805 / 12,909 | 206,609 / 358,903 |
| Original ordinary parent stored, bytes | 485 | 5,177 |
| Previous joint-only parent stored, bytes | 5,297 | 135,946 |

All 1,850 groups match the manifest-inclusive plan exactly, including the five
previously rejected WHOLE Coding groups remaining ordinary fallbacks. The two
current parents fit the predeclared 240KiB stored / 1MiB decoded cap. That is not
proof that an arbitrarily large or denser RootFS needs only one selector level;
root-size-independent lookup and additional hierarchy costs remain open.

Current AES-GCM-RSA and ChaCha20-RSA implementations each encrypted and decrypted
every actual group and both parents, using ephemeral in-memory RSA2048 keys and
16KiB chunks. Header cache settings stay 1,024 entries / 8MiB, with 256KiB prefix
and parallel-read bounds. All 7,408 cold/header-cached local fixture reads passed
byte equality, with one provider call per object per pass. These are in-memory
provider counts, NOT remote HTTP latency or real cached-node sandbox samples.

AES/ChaCha parent ciphertext bytes are 8,299/8,298 for Node and 207,343/207,342 for
Coding. For all data objects the totals are 2,774,668/2,774,607 and
88,992,976/88,991,187 respectively. Ciphertext bodies and keys were ephemeral;
the construction receipt records actual equality checks and cipher hashes, not
replayable ciphertext files. Actual plaintext objects remain indexed locally.

## Complete-stream cost correction

Replayed all 14,373 retained immutable request shapes from 16 identities through
ONE ideal-private-cache combined policy, substituting actual parent and data-group
lengths. Every request's source kind, member membership and mapping range schedule
matches the previous combined screen. Existing joint and ordinary dispatch remain
unchanged. Historical cohort labels are not simulated node-cache warmth.

| Per complete image stream | Node | Coding |
| --- | ---: | ---: |
| Ordinary / combined source windows | 239 / 228 | 284 / 269 |
| Ordinary / combined stored bytes | 44,056,500 / 44,238,302 | 47,683,268 / 48,237,918 |
| Additional stored bytes versus ordinary | 181,802 | 554,650 |
| Previously omitted stored bytes | 6,796 | 76,839 |
| Additional diagnostic decoded bytes | 1,982,295 | 4,266,807 |

Decoded charges include the actual diagnostic verify-all-member policy and
manifests. This is not a future cache-aware decoder CPU prediction, measured RSS,
provider cipher-range replay, bounded shared-cache result or startup timing.
Lower source-window counts alone cannot justify the candidate: the earlier
exceptionally favorable free-delivery screen left only 160–178ms below Coding's
combined 2s boundary before these new costs. Do not advertise that margin as an
achieved speedup, causal lower bound or latency forecast.

## Failed prerequisites and harness corrections

1. Before exporter staging, the original warm pool had ZERO live/ready carriers,
   not the expected two. Staging refused before upload. All 349 listed allocations
   were historical terminal records: 275 failed and 74 complete, not 349 new
   failures this turn. Job modify index 70668 and its two desired groups remained
   unchanged. Runtime readiness FAILED and was not repaired or relabeled as ready.
   Only an explicitly recorded object-export prerequisite was amended to require
   this verified empty state and unchanged durable metadata/configuration.
2. The first exporter was mistakenly built for local linux/arm64 with a dynamic
   loader. The x86_64 remote loader failed before the Go program ran, exit255.
   Preserve the binary/launch. A separate linux/amd64 CGO-disabled build, verified
   as ELF machine62, replaced only the new diagnostic launch target.
3. That native launch omitted the existing AWS profile environment and exited1
   with no completed frame files. Source configuration was present; provider
   initialization completed. The next distinct unit used the same native binary
   with the exact existing profile environment and succeeded. No key/profile was
   changed. Preserve the empty failed output directory and unknown failed-run
   request count; do not erase either failure with the successful receipt.

Exploratory path/case guesses and an overly broad allocation-state query are
retained in the attempt log. Final reports use bounded summaries. Early helper
filter/preflight script receipts precede their recorded amendments; they are not
misrepresented as hashes of the final scripts.

## Verification and remote handoff

Two exporter tests, two codec integrity/bounds tests and complete actual-object
construction pass under Go race detection. Tests include corruption, rehashed
corrupt originals, truncation, cancellation, stale bindings, reserved/overlapping
selectors, exclusive output and manifest-inclusive caps. This is not local e2e,
a complete product suite, candidate Reader integration or storage lifecycle proof.

Reverified 1,353 frozen product files, 164 trace files, 8,202 indexed predecessors
and the immediately preceding 22 artifacts. Main refs remain sandbox0 `0f092204`
and infrastructure `ceb895c6`; preserve all 193 existing worktree entries. No
product implementation changed; only new temporary diagnostic artifacts and the
user-requested experiment record changed.

The remote skill was used only for the workspace Makefile lifecycle, preserving
`/data`; its historical Kind/bootstrap/deploy procedure was not used. Before/after
boot, original service binaries/configs, DB row counts, job identity, empty
privileged runtime and 64 detached NBD devices match. Both failed launches and
successful original frames are preserved under the new diagnostic directory.

Final cloud observation **2026-09-12 01:52:01 UTC**: Singapore test instance
`i-t4n2wfukm75q4wie9j5f` is **Stopped / StopCharging**, `ecs.g9i.large`, 2CPU/8GiB.
SSH master closed, compute not resized. Warm readiness before shutdown remained
**0**, so a future startup trial must first establish a healthy current fixture.
This observation neither explains nor invalidates earlier measured cold misses.
No production operation, rollout, merge or tag. The full goal remains unachieved.

Evidence root: `/tmp/sandbox0-combined-bytes.CwzhqX`. Key files:
`download/result.json`, `transfer.json`, `combined/analysis.json`, both complete
`combined/*/catalog.json` files, `actual-shapes.json`, `artifact-checks.json`,
`after-verification.json`, `final-cloud.json`, test receipts and `attempts.md`.

All 1,961 artifacts sealed; all owned handles terminal. Evidence SHA256:
`702c2c91e866af6b2420683e2d5e1212c9cf503d3e9ffecc4eccbb1a560b0369`.
Artifact-index SHA256:
`647a77b4616167e497e725993c1c304644351ddce245b06562551aa925775d79`.
