---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: sandbox0infras.infra.sandbox0.ai
spec:
  group: infra.sandbox0.ai
  names:
    kind: Sandbox0Infra
    listKind: Sandbox0InfraList
    plural: sandbox0infras
    shortNames:
    - s0i
    singular: sandbox0infra
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.progress
      name: Progress
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Sandbox0Infra is the Schema for the sandbox0infras API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Sandbox0InfraSpec defines the desired state of Sandbox0Infra
            properties:
              cluster:
                description: Cluster configures cluster identification and capacity
                properties:
                  capacity:
                    description: Capacity specifies cluster resource capacity
                    properties:
                      cpu:
                        description: CPU specifies CPU capacity
                        properties:
                          available:
                            description: Available is the available capacity
                            type: string
                          total:
                            description: Total is the total capacity
                            type: string
                        type: object
                      maxSandboxes:
                        description: MaxSandboxes is the maximum number of sandboxes
                        format: int32
                        type: integer
                      memory:
                        description: Memory specifies memory capacity
                        properties:
                          available:
                            description: Available is the available capacity
                            type: string
                          total:
                            description: Total is the total capacity
                            type: string
                        type: object
                    type: object
                  id:
                    description: ID is the unique cluster identifier
                    type: string
                  name:
                    description: Name is the human-readable cluster name
                    type: string
                required:
                - id
                type: object
              controlPlane:
                description: ControlPlane configures external control plane connection
                  (only for data-plane mode)
                properties:
                  internalAuthPublicKeySecret:
                    description: InternalAuthPublicKeySecret references the secret
                      containing control plane's public key
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the name of the secret
                        type: string
                    required:
                    - name
                    type: object
                  url:
                    description: URL is the control plane edge-gateway URL
                    type: string
                required:
                - internalAuthPublicKeySecret
                - url
                type: object
              database:
                description: Database configures the main database for sandbox0
                properties:
                  builtin:
                    default: {}
                    description: Builtin configures the built-in single-node PostgreSQL
                    properties:
                      database:
                        default: sandbox0
                        description: Database specifies the database name
                        type: string
                      enabled:
                        default: true
                        description: Enabled enables the built-in database
                        type: boolean
                      image:
                        default: postgres:16-alpine
                        description: Image specifies the postgres image for the builtin
                          database
                        type: string
                      persistence:
                        description: Persistence configures database storage
                        properties:
                          enabled:
                            default: true
                            description: Enabled enables persistence
                            type: boolean
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 20Gi
                            description: Size specifies the storage size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: StorageClass specifies the storage class
                              (empty for default)
                            type: string
                        type: object
                      port:
                        default: 5432
                        description: Port specifies the database port
                        format: int32
                        type: integer
                      sslMode:
                        default: disable
                        description: SSLMode specifies the SSL mode for builtin DSN
                        type: string
                      username:
                        default: sandbox0
                        description: Username specifies the database username
                        type: string
                    type: object
                  external:
                    description: External configures connection to external database
                    properties:
                      database:
                        description: Database specifies the database name
                        type: string
                      host:
                        description: Host specifies the database host
                        type: string
                      passwordSecret:
                        description: PasswordSecret references the secret containing
                          the password
                        properties:
                          key:
                            default: password
                            description: Key is the key in the secret
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                        required:
                        - name
                        type: object
                      port:
                        default: 5432
                        description: Port specifies the database port
                        format: int32
                        type: integer
                      sslMode:
                        default: require
                        description: SSLMode specifies the SSL mode for connection
                        type: string
                      username:
                        description: Username specifies the database username
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - username
                    type: object
                  type:
                    default: builtin
                    description: 'Type specifies the database type: builtin, postgres,
                      or mysql'
                    enum:
                    - builtin
                    - postgres
                    - mysql
                    type: string
                type: object
              initUser:
                description: InitUser configures the initial admin user
                properties:
                  email:
                    description: Email is the admin user's email
                    type: string
                  enabled:
                    default: true
                    description: Enabled enables initial user creation
                    type: boolean
                  name:
                    description: Name is the admin user's display name
                    type: string
                  passwordSecret:
                    description: PasswordSecret references the secret containing the
                      password
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the name of the secret
                        type: string
                    required:
                    - name
                    type: object
                required:
                - email
                - passwordSecret
                type: object
              internalAuth:
                description: InternalAuth configures internal authentication keys
                properties:
                  controlPlane:
                    description: ControlPlane configures control plane key pair
                    properties:
                      generate:
                        default: true
                        description: Generate enables automatic key generation
                        type: boolean
                      secretRef:
                        description: SecretRef references an existing secret containing
                          the key pair
                        properties:
                          name:
                            description: Name is the name of the secret
                            type: string
                          privateKeyKey:
                            default: private.key
                            description: PrivateKeyKey is the key for private key
                            type: string
                          publicKeyKey:
                            default: public.key
                            description: PublicKeyKey is the key for public key
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  dataPlane:
                    description: DataPlane configures data plane key pair
                    properties:
                      generate:
                        default: true
                        description: Generate enables automatic key generation
                        type: boolean
                      secretRef:
                        description: SecretRef references an existing secret containing
                          the key pair
                        properties:
                          name:
                            description: Name is the name of the secret
                            type: string
                          privateKeyKey:
                            default: private.key
                            description: PrivateKeyKey is the key for private key
                            type: string
                          publicKeyKey:
                            default: public.key
                            description: PublicKeyKey is the key for public key
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                type: object
              juicefsDatabase:
                description: JuicefsDatabase configures the JuiceFS metadata database
                properties:
                  external:
                    description: External configures an independent database for JuiceFS
                    properties:
                      database:
                        description: Database specifies the database name
                        type: string
                      host:
                        description: Host specifies the database host
                        type: string
                      passwordSecret:
                        description: PasswordSecret references the secret containing
                          the password
                        properties:
                          key:
                            default: password
                            description: Key is the key in the secret
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                        required:
                        - name
                        type: object
                      port:
                        default: 5432
                        description: Port specifies the database port
                        format: int32
                        type: integer
                      sslMode:
                        default: require
                        description: SSLMode specifies the SSL mode for connection
                        type: string
                      username:
                        description: Username specifies the database username
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - username
                    type: object
                  shareWithMain:
                    default: true
                    description: ShareWithMain uses the main database for JuiceFS
                      metadata
                    type: boolean
                type: object
              mode:
                default: all
                description: 'Mode specifies the deployment mode: all, control-plane,
                  or data-plane'
                enum:
                - all
                - control-plane
                - data-plane
                type: string
              region:
                description: Region identifies the region for multi-cluster deployments
                type: string
              services:
                default: {}
                description: Services configures individual services
                properties:
                  edgeGateway:
                    default: {}
                    description: EdgeGateway configures the edge-gateway service (control
                      plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains edge-gateway specific configuration
                        properties:
                          baseUrl:
                            default: http://localhost:8080
                            description: Base URL for OIDC callbacks
                            type: string
                          builtInAuth:
                            default: {}
                            description: Built-in Authentication
                            properties:
                              adminOnly:
                                description: AdminOnly restricts built-in auth to
                                  admin accounts only
                                type: boolean
                              allowRegistration:
                                description: AllowRegistration allows new users to
                                  register
                                type: boolean
                              emailVerificationRequired:
                                description: EmailVerificationRequired requires email
                                  verification
                                type: boolean
                              enabled:
                                default: true
                                description: Enabled enables built-in email/password
                                  authentication
                                type: boolean
                            type: object
                          clusterCacheTTL:
                            default: 30s
                            description: Cache configuration
                            type: string
                          databaseMaxConns:
                            default: 30
                            type: integer
                          databaseMinConns:
                            default: 8
                            type: integer
                          defaultTeamName:
                            default: Personal Team
                            description: Identity and Teams
                            type: string
                          edition:
                            default: self-hosted
                            description: 'Edition: "saas" or "self-hosted"'
                            type: string
                          httpPort:
                            default: 8080
                            description: Server configuration
                            type: integer
                          internalAuthCaller:
                            default: edge-gateway
                            type: string
                          internalAuthTTL:
                            default: 30s
                            description: Internal Authentication
                            type: string
                          jwtAccessTokenTTL:
                            default: 15m
                            type: string
                          jwtIssuer:
                            default: edge-gateway
                            type: string
                          jwtRefreshTokenTTL:
                            default: 168h
                            type: string
                          jwtSecret:
                            description: JWT Configuration
                            type: string
                          logLevel:
                            default: info
                            type: string
                          oidcProviders:
                            description: OIDC Providers
                            items:
                              description: OIDCProviderConfig configures an OIDC provider.
                              properties:
                                autoProvision:
                                  description: AutoProvision automatically creates
                                    users on first login
                                  type: boolean
                                clientId:
                                  description: ClientID is the OAuth client ID
                                  type: string
                                clientSecret:
                                  description: ClientSecret is the OAuth client secret
                                  type: string
                                discoveryUrl:
                                  description: DiscoveryURL is the OIDC discovery
                                    URL (.well-known/openid-configuration)
                                  type: string
                                enabled:
                                  description: Enabled toggles the provider
                                  type: boolean
                                id:
                                  description: ID is the unique identifier for the
                                    provider (e.g., "github", "google")
                                  type: string
                                name:
                                  description: Name is the display name
                                  type: string
                                scopes:
                                  default:
                                  - openid
                                  - email
                                  - profile
                                  description: Scopes are the OAuth scopes to request
                                  items:
                                    type: string
                                  type: array
                                teamMapping:
                                  description: TeamMapping configures automatic team
                                    assignment
                                  properties:
                                    defaultRole:
                                      description: DefaultRole is the role assigned
                                        to new users
                                      type: string
                                    defaultTeamId:
                                      description: DefaultTeamID is the team to add
                                        users to
                                      type: string
                                    domain:
                                      description: Domain filters users by email domain
                                      type: string
                                  type: object
                              type: object
                            type: array
                          oidcStateCleanupInterval:
                            default: 5m
                            type: string
                          oidcStateTTL:
                            default: 10m
                            type: string
                          proxyTimeout:
                            default: 30s
                            description: Timeouts
                            type: string
                          rateLimitBurst:
                            default: 200
                            type: integer
                          rateLimitCleanupInterval:
                            default: 10m
                            type: string
                          rateLimitRPS:
                            default: 100
                            description: Rate limiting
                            type: integer
                          schedulerEnabled:
                            description: Scheduler configuration (optional, for multi-cluster
                              mode)
                            type: boolean
                          schedulerUrl:
                            type: string
                          serverIdleTimeout:
                            default: 120s
                            type: string
                          serverReadTimeout:
                            default: 30s
                            type: string
                          serverWriteTimeout:
                            default: 60s
                            type: string
                          shutdownTimeout:
                            default: 30s
                            type: string
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  internalGateway:
                    default: {}
                    description: InternalGateway configures the internal-gateway service
                      (data plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains internal-gateway specific configuration
                        properties:
                          allowedCallers:
                            default:
                            - edge-gateway
                            - scheduler
                            description: |-
                              Internal authentication (for validating requests from edge-gateway and
                              generating tokens for downstream services)
                              AllowedCallers is the list of services allowed to call internal-gateway
                              Default: ["edge-gateway"], can include "scheduler" for multi-cluster mode
                            items:
                              type: string
                            type: array
                          healthCheckPeriod:
                            default: 10s
                            type: string
                          httpPort:
                            default: 8443
                            description: Server configuration
                            type: integer
                          logLevel:
                            default: info
                            type: string
                          procdStoragePermissions:
                            default:
                            - sandboxvolume:read
                            - sandboxvolume:write
                            items:
                              type: string
                            type: array
                          proxyTimeout:
                            default: 10s
                            description: Proxy configuration
                            type: string
                          schedulerPermissions:
                            default:
                            - '*:*'
                            description: Permissions
                            items:
                              type: string
                            type: array
                          shutdownTimeout:
                            default: 30s
                            description: Timeouts
                            type: string
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  manager:
                    default: {}
                    description: Manager configures the manager service (data plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains manager specific configuration
                        properties:
                          bandwidthAccountingInterval:
                            default: 10
                            type: integer
                          cleanupInterval:
                            default: 60s
                            description: Cleanup Controller
                            type: string
                          databaseMaxConns:
                            default: 10
                            description: Database
                            format: int32
                            type: integer
                          databaseMinConns:
                            default: 2
                            format: int32
                            type: integer
                          defaultBandwidthBurstBytes:
                            default: 12500000
                            format: int64
                            type: integer
                          defaultBandwidthRateBps:
                            default: 100000000
                            description: Network
                            format: int64
                            type: integer
                          defaultSandboxTTL:
                            default: 5m
                            description: Sandbox
                            type: string
                          defaultTemplate:
                            default: {}
                            description: template
                            properties:
                              image:
                                default: sandbox0ai/otemplates:default-v0.1.0
                                type: string
                              name:
                                default: default
                                type: string
                              pool:
                                default: {}
                                properties:
                                  autoScale:
                                    default: true
                                    type: boolean
                                  maxIdle:
                                    default: 5
                                    format: int32
                                    type: integer
                                  minIdle:
                                    default: 1
                                    format: int32
                                    type: integer
                                type: object
                            type: object
                          templateNamespace:
                            default: sandbox0
                            type: string
                          httpPort:
                            default: 8080
                            description: HTTP Server
                            type: integer
                          kubeConfig:
                            description: Kubernetes
                            type: string
                          leaderElection:
                            default: true
                            type: boolean
                          logLevel:
                            default: info
                            description: Logging
                            type: string
                          metricsPort:
                            default: 9090
                            description: Metrics
                            type: integer
                          pauseMemoryBufferRatio:
                            default: "1.1"
                            type: string
                          pauseMinCPU:
                            default: 10m
                            type: string
                          pauseMinMemoryLimit:
                            default: 32Mi
                            type: string
                          pauseMinMemoryRequest:
                            default: 10Mi
                            description: Pause/Resume
                            type: string
                          procdClientTimeout:
                            default: 30s
                            description: Timeouts
                            type: string
                          procdConfig:
                            default: {}
                            description: Procd config injected into sandbox pods
                            properties:
                              cacheMaxBytes:
                                default: 104857600
                                description: Cache configuration
                                format: int64
                                type: integer
                              cacheTTL:
                                default: 30s
                                type: string
                              httpPort:
                                default: 49983
                                description: Server configuration
                                type: integer
                              juicefsBufferSize:
                                default: "300"
                                type: string
                              juicefsCacheSize:
                                default: "100"
                                description: JuiceFS Mount Defaults
                                type: string
                              juicefsPrefetch:
                                default: 3
                                type: integer
                              juicefsWriteback:
                                default: true
                                type: boolean
                              logLevel:
                                default: info
                                type: string
                              rootPath:
                                default: /workspace
                                description: File manager configuration
                                type: string
                              storageProxyReplicas:
                                default: 3
                                type: integer
                            type: object
                          resyncPeriod:
                            default: 30s
                            type: string
                          shutdownTimeout:
                            default: 30s
                            type: string
                          webhookCertPath:
                            default: /tmp/k8s-webhook-server/serving-certs/tls.crt
                            type: string
                          webhookKeyPath:
                            default: /tmp/k8s-webhook-server/serving-certs/tls.key
                            type: string
                          webhookPort:
                            default: 9443
                            description: Webhook
                            type: integer
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  netd:
                    default: {}
                    description: Netd configures the netd service (data plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains netd specific configuration
                        properties:
                          bpfFSPath:
                            default: /sys/fs/bpf
                            description: BPFFSPath is the path to bpf filesystem (usually
                              /sys/fs/bpf)
                            type: string
                          bpfPinPath:
                            default: netd
                            description: BPFPinPath is the sub-path within BPFFS to
                              pin BPF maps/programs
                            type: string
                          burstRatio:
                            default: "0.125"
                            description: BurstRatio is the default ratio of rate to
                              burst (e.g. 0.125 for 1/8)
                            type: string
                          clusterDNSCIDR:
                            description: ClusterDNSCIDR is the CIDR for cluster DNS
                              (always allowed for DNS)
                            type: string
                          dnsPort:
                            default: 53
                            description: DNSPort is the port used for DNS lookups
                            type: integer
                          dnsResolvers:
                            default:
                            - 8.8.8.8:53
                            description: DNSResolvers are the upstream DNS resolvers
                              for the proxy
                            items:
                              type: string
                            type: array
                          edtHorizon:
                            default: 10ms
                            description: EDTHorizon is the horizon for EDT-based pacing
                            type: string
                          failClosed:
                            default: true
                            description: FailClosed if true, blocks all traffic when
                              netd is not ready
                            type: boolean
                          healthPort:
                            default: 8080
                            description: HealthPort is the port for health checks
                            type: integer
                          internalGatewayCIDR:
                            description: InternalGatewayCIDR is the CIDR for internal-gateway
                              (allowed for ingress to procd)
                            type: string
                          kubeConfig:
                            description: KubeConfig is the path to kubeconfig file
                              (optional, uses in-cluster config if empty)
                            type: string
                          logLevel:
                            default: info
                            description: LogLevel is the logging level (debug, info,
                              warn, error)
                            type: string
                          metricsPort:
                            default: 9090
                            description: MetricsPort is the port for Prometheus metrics
                            type: integer
                          metricsReportInterval:
                            default: 10s
                            description: MetricsReportInterval is the interval for
                              reporting metrics
                            type: string
                          nodeName:
                            default: ${NODE_NAME}
                            description: NodeName is the name of the node this netd
                              is running on
                            type: string
                          procdPort:
                            default: 49983
                            description: ProcdPort is the port procd listens on
                            type: integer
                          proxyDNSTimeout:
                            default: 10s
                            description: ProxyDNSTimeout is the timeout for DNS resolution
                              in the proxy
                            type: string
                          proxyHTTPPort:
                            default: 18080
                            description: ProxyHTTPPort is the port for HTTP proxy
                              (redirect from port 80)
                            type: integer
                          proxyHTTPSPort:
                            default: 18443
                            description: ProxyHTTPSPort is the port for HTTPS/TLS
                              proxy (redirect from port 443)
                            type: integer
                          proxyHeaderLimit:
                            default: 65536
                            description: ProxyHeaderLimit is the maximum size for
                              HTTP headers
                            format: int64
                            type: integer
                          proxyListenAddr:
                            default: 0.0.0.0
                            description: ProxyListenAddr is the address for the L7
                              proxy to listen on
                            type: string
                          proxyUpstreamTimeout:
                            default: 10s
                            description: ProxyUpstreamTimeout is the timeout for connecting
                              to upstream servers
                            type: string
                          resyncPeriod:
                            default: 30s
                            description: ResyncPeriod is the period for informer resync
                            type: string
                          shutdownDelay:
                            default: 2s
                            description: ShutdownDelay is the delay before final shutdown
                            type: string
                          storageProxyCIDR:
                            description: StorageProxyCIDR is the CIDR for storage-proxy
                              (always allowed)
                            type: string
                          useEBPF:
                            default: true
                            description: UseEBPF enables eBPF-based bandwidth control
                              (more efficient than tc htb)
                            type: boolean
                          useEDT:
                            default: true
                            description: UseEDT enables Earliest Departure Time pacing
                              for eBPF
                            type: boolean
                          vethPrefix:
                            default: veth
                            description: VethPrefix is the prefix for veth interfaces
                            type: string
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  scheduler:
                    description: Scheduler configures the scheduler service (control
                      plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains scheduler specific configuration
                        properties:
                          databasePool:
                            description: Database Pool configuration
                            properties:
                              maxConnIdleTime:
                                default: 5m
                                type: string
                              maxConnLifetime:
                                default: 30m
                                type: string
                              maxConns:
                                default: 10
                                format: int32
                                type: integer
                              minConns:
                                default: 2
                                format: int32
                                type: integer
                            type: object
                          httpPort:
                            default: 8080
                            description: Server configuration
                            type: integer
                          idleTimeout:
                            default: 120s
                            type: string
                          logLevel:
                            default: info
                            type: string
                          podsPerNode:
                            default: 50
                            type: integer
                          proxyTimeout:
                            default: 10s
                            type: string
                          readTimeout:
                            default: 30s
                            type: string
                          reconcileInterval:
                            default: 30s
                            description: Reconciler configuration
                            type: string
                          shutdownTimeout:
                            default: 30s
                            description: Timeouts
                            type: string
                          writeTimeout:
                            default: 60s
                            type: string
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                  storageProxy:
                    default: {}
                    description: StorageProxy configures the storage-proxy service
                      (data plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains storage-proxy specific configuration
                        properties:
                          auditFile:
                            default: /var/log/storage-proxy/audit.log
                            type: string
                          auditLog:
                            default: true
                            type: boolean
                          cacheDir:
                            default: /var/lib/storage-proxy/cache
                            type: string
                          cleanupInterval:
                            default: 60s
                            type: string
                          databaseMaxConns:
                            default: 30
                            type: integer
                          databaseMinConns:
                            default: 5
                            type: integer
                          databaseSchema:
                            default: sp
                            type: string
                          defaultCacheSize:
                            default: 1G
                            type: string
                          flushTimeout:
                            default: 30s
                            type: string
                          grpcAddr:
                            default: 0.0.0.0
                            description: gRPC Server
                            type: string
                          grpcPort:
                            default: 8080
                            type: integer
                          heartbeatInterval:
                            default: 5s
                            description: Coordination
                            type: string
                          heartbeatTimeout:
                            default: 15
                            type: integer
                          httpAddr:
                            default: 0.0.0.0
                            description: HTTP Management API
                            type: string
                          httpIdleTimeout:
                            default: 60s
                            type: string
                          httpPort:
                            default: 8081
                            type: integer
                          httpReadTimeout:
                            default: 15s
                            description: Timeouts
                            type: string
                          httpWriteTimeout:
                            default: 15s
                            type: string
                          juicefsAttrTimeout:
                            default: 1s
                            type: string
                          juicefsBlockSize:
                            default: 4096
                            type: integer
                          juicefsCompression:
                            default: lz4
                            type: string
                          juicefsDirEntryTimeout:
                            default: 1s
                            type: string
                          juicefsEntryTimeout:
                            default: 1s
                            type: string
                          juicefsMaxUpload:
                            default: 20
                            type: integer
                          juicefsMetaRetries:
                            default: 10
                            type: integer
                          juicefsName:
                            default: sandbox0
                            type: string
                          juicefsTrashDays:
                            default: 1
                            type: integer
                          kubeconfigPath:
                            description: Kubernetes
                            type: string
                          logLevel:
                            default: info
                            description: Logging
                            type: string
                          maxBytesPerSecond:
                            default: 1073741824
                            format: int64
                            type: integer
                          maxOpsPerSecond:
                            default: 10000
                            description: Rate limiting
                            type: integer
                          metricsEnabled:
                            default: true
                            description: Monitoring
                            type: boolean
                          metricsPort:
                            default: 9090
                            type: integer
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                    type: object
                type: object
              storage:
                description: Storage configures the storage backend (JuiceFS S3 backend)
                properties:
                  builtin:
                    default: {}
                    description: Builtin configures the built-in RustFS storage
                    properties:
                      bucket:
                        default: sandbox0
                        description: Bucket specifies the default bucket name for
                          builtin storage
                        type: string
                      consoleEnabled:
                        default: true
                        description: ConsoleEnabled enables the RustFS console
                        type: boolean
                      consolePort:
                        default: 9001
                        description: ConsolePort specifies the RustFS console port
                        format: int32
                        type: integer
                      credentials:
                        description: Credentials configures access credentials (auto-generated
                          if not specified)
                        properties:
                          accessKey:
                            description: AccessKey is the access key
                            type: string
                          secretKey:
                            description: SecretKey is the secret key
                            type: string
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables the built-in storage
                        type: boolean
                      image:
                        default: rustfs/rustfs:1.0.0-alpha.79
                        description: Image specifies the RustFS image for builtin
                          storage
                        type: string
                      obsEnvironment:
                        default: develop
                        description: ObsEnvironment specifies the RustFS environment
                          label
                        type: string
                      obsLogDirectory:
                        default: /data/logs
                        description: ObsLogDirectory specifies the RustFS log directory
                        type: string
                      obsLoggerLevel:
                        default: debug
                        description: ObsLoggerLevel specifies the RustFS log level
                        type: string
                      persistence:
                        description: Persistence configures storage persistence
                        properties:
                          enabled:
                            default: true
                            description: Enabled enables persistence
                            type: boolean
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 20Gi
                            description: Size specifies the storage size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: StorageClass specifies the storage class
                              (empty for default)
                            type: string
                        type: object
                      port:
                        default: 9000
                        description: Port specifies the RustFS API port
                        format: int32
                        type: integer
                      region:
                        default: us-east-1
                        description: Region specifies the default region for builtin
                          storage
                        type: string
                      volumes:
                        default: /data
                        description: Volumes specifies the RustFS data path
                        type: string
                    type: object
                  oss:
                    description: OSS configures Aliyun OSS storage
                    properties:
                      bucket:
                        description: Bucket specifies the OSS bucket name
                        type: string
                      credentialsSecret:
                        description: CredentialsSecret references the secret containing
                          Aliyun credentials
                        properties:
                          accessKeyKey:
                            default: accessKeyId
                            description: AccessKeyKey is the key for access key ID
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                          secretKeyKey:
                            default: accessKeySecret
                            description: SecretKeyKey is the key for access key secret
                            type: string
                        required:
                        - name
                        type: object
                      endpoint:
                        description: Endpoint specifies the OSS endpoint
                        type: string
                      region:
                        description: Region specifies the Aliyun region
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    - endpoint
                    - region
                    type: object
                  s3:
                    description: S3 configures S3 or S3-compatible storage
                    properties:
                      bucket:
                        description: Bucket specifies the S3 bucket name
                        type: string
                      credentialsSecret:
                        description: CredentialsSecret references the secret containing
                          AWS credentials
                        properties:
                          accessKeyKey:
                            default: accessKeyId
                            description: AccessKeyKey is the key for access key ID
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                          secretKeyKey:
                            default: secretAccessKey
                            description: SecretKeyKey is the key for secret access
                              key
                            type: string
                        required:
                        - name
                        type: object
                      endpoint:
                        description: Endpoint specifies the S3 endpoint (optional
                          for AWS)
                        type: string
                      region:
                        description: Region specifies the AWS region
                        type: string
                      sessionTokenKey:
                        description: SessionTokenKey is the key for session token
                          in the secret (optional)
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    - region
                    type: object
                  type:
                    default: builtin
                    description: 'Type specifies the storage type: builtin, s3, or
                      oss'
                    enum:
                    - builtin
                    - s3
                    - oss
                    type: string
                type: object
              version:
                default: v0.1.0
                description: Version specifies the version of sandbox0 components
                  to deploy
                type: string
            type: object
          status:
            description: Sandbox0InfraStatus defines the observed state of Sandbox0Infra
            properties:
              cluster:
                description: Cluster contains cluster registration status
                properties:
                  id:
                    description: ID is the cluster identifier
                    type: string
                  registered:
                    description: Registered indicates if the cluster is registered
                    type: boolean
                  registeredAt:
                    description: RegisteredAt is the registration timestamp
                    format: date-time
                    type: string
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the infrastructure's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endpoints:
                description: Endpoints contains the service endpoints
                properties:
                  edgeGateway:
                    description: EdgeGateway is the external edge-gateway URL
                    type: string
                  edgeGatewayInternal:
                    description: EdgeGatewayInternal is the internal edge-gateway
                      URL
                    type: string
                  internalGateway:
                    description: InternalGateway is the internal-gateway URL
                    type: string
                type: object
              internalAuth:
                description: InternalAuth contains internal authentication status
                properties:
                  controlPlanePublicKey:
                    description: ControlPlanePublicKey references the control plane
                      public key
                    properties:
                      secretKey:
                        description: SecretKey is the key in the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - secretKey
                    - secretName
                    type: object
                  dataPlanePublicKey:
                    description: DataPlanePublicKey references the data plane public
                      key
                    properties:
                      secretKey:
                        description: SecretKey is the key in the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - secretKey
                    - secretName
                    type: object
                type: object
              lastOperation:
                description: LastOperation contains the last operation information
                properties:
                  completedAt:
                    description: CompletedAt is when the operation completed
                    format: date-time
                    type: string
                  error:
                    description: Error contains error message if failed
                    type: string
                  startedAt:
                    description: StartedAt is when the operation started
                    format: date-time
                    type: string
                  status:
                    description: Status is the operation status
                    type: string
                  type:
                    description: Type is the operation type
                    type: string
                type: object
              observedVersion:
                description: ObservedVersion is the last observed version
                type: string
              phase:
                description: Phase represents the current phase of the infrastructure
                enum:
                - Installing
                - Ready
                - Degraded
                - Failed
                - Upgrading
                type: string
              progress:
                description: Progress shows readiness progress in "ready/total" format
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
