---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: sandbox0infras.infra.sandbox0.ai
spec:
  group: infra.sandbox0.ai
  names:
    kind: Sandbox0Infra
    listKind: Sandbox0InfraList
    plural: sandbox0infras
    shortNames:
    - s0i
    singular: sandbox0infra
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.progress
      name: Progress
      type: string
    - jsonPath: .status.lastMessage
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Sandbox0Infra is the Schema for the sandbox0infras API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Sandbox0InfraSpec defines the desired state of Sandbox0Infra
            properties:
              builtinTemplates:
                description: BuiltinTemplates defines system builtin templates to
                  seed the template store
                items:
                  description: BuiltinTemplateConfig defines a system builtin template.
                  properties:
                    description:
                      type: string
                    displayName:
                      type: string
                    image:
                      type: string
                    pool:
                      description: BuiltinTemplatePoolConfig holds pool defaults for
                        builtin templates.
                      properties:
                        maxIdle:
                          default: 5
                          format: int32
                          type: integer
                        minIdle:
                          default: 1
                          format: int32
                          type: integer
                      type: object
                    spec:
                      description: |-
                        Spec optionally overrides the full template spec seeded by infra-operator.
                        When omitted, infra-operator builds the spec from the legacy fields above
                        and the built-in preset for the template ID.
                      properties:
                        clusterId:
                          description: Environment configuration
                          type: string
                        description:
                          description: Description of the template
                          type: string
                        displayName:
                          type: string
                        envVars:
                          additionalProperties:
                            type: string
                          description: Environment variables injected into the procd-managed
                            sandbox environment.
                          type: object
                        mainContainer:
                          description: MainContainer configuration (required)
                          properties:
                            env:
                              items:
                                description: EnvVar represents an environment variable
                                properties:
                                  name:
                                    type: string
                                  value:
                                    type: string
                                required:
                                - name
                                - value
                                type: object
                              type: array
                            image:
                              type: string
                            imagePullPolicy:
                              type: string
                            resources:
                              description: ResourceQuota defines resource quota (per
                                template)
                              properties:
                                cpu:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                ephemeralStorage:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                memory:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                              type: object
                            securityContext:
                              description: SecurityContext defines security context
                                for containers
                              properties:
                                allowPrivilegeEscalation:
                                  type: boolean
                                appArmorProfile:
                                  properties:
                                    localhostProfile:
                                      type: string
                                    type:
                                      enum:
                                      - Unconfined
                                      - RuntimeDefault
                                      - Localhost
                                      type: string
                                  required:
                                  - type
                                  type: object
                                capabilities:
                                  description: Capabilities defines Linux capabilities
                                  properties:
                                    add:
                                      items:
                                        type: string
                                      type: array
                                    drop:
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                privileged:
                                  type: boolean
                                readOnlyRootFilesystem:
                                  type: boolean
                                runAsGroup:
                                  format: int64
                                  type: integer
                                runAsNonRoot:
                                  type: boolean
                                runAsUser:
                                  format: int64
                                  type: integer
                                seccompProfile:
                                  properties:
                                    localhostProfile:
                                      type: string
                                    type:
                                      enum:
                                      - Unconfined
                                      - RuntimeDefault
                                      - Localhost
                                      type: string
                                  required:
                                  - type
                                  type: object
                              type: object
                          required:
                          - image
                          - resources
                          type: object
                        network:
                          description: Template sandbox network policy and optional
                            credential bindings.
                          properties:
                            credentialBindings:
                              description: |-
                                CredentialBindings defines sandbox-scoped credential bindings that
                                EgressCredentialRule entries can resolve by CredentialRef.
                              items:
                                description: |-
                                  CredentialBinding defines one named credential projection that outbound auth
                                  rules can reference. The binding itself does not match traffic.
                                properties:
                                  cachePolicy:
                                    description: CachePolicy controls broker-side
                                      caching for resolved auth material.
                                    properties:
                                      ttl:
                                        description: TTL overrides the default broker
                                          cache TTL for resolved auth material.
                                        type: string
                                    type: object
                                  projection:
                                    description: Projection defines how resolved source
                                      material is projected into runtime auth directives.
                                    properties:
                                      httpHeaders:
                                        description: HTTPHeaders projects resolved
                                          source data into outbound HTTP headers.
                                        properties:
                                          headers:
                                            description: Headers lists the outbound
                                              headers to synthesize.
                                            items:
                                              description: ProjectedHeader defines
                                                one projected header template.
                                              properties:
                                                name:
                                                  description: Name is the outbound
                                                    header name.
                                                  type: string
                                                valueTemplate:
                                                  description: ValueTemplate is rendered
                                                    against the resolved source payload.
                                                  type: string
                                              required:
                                              - name
                                              - valueTemplate
                                              type: object
                                            type: array
                                        type: object
                                      placeholderSubstitution:
                                        description: PlaceholderSubstitution replaces
                                          sandbox-visible placeholders at the egress
                                          boundary.
                                        properties:
                                          replacements:
                                            description: Replacements lists placeholder
                                              replacement templates.
                                            items:
                                              description: PlaceholderReplacement
                                                defines one placeholder replacement
                                                template.
                                              properties:
                                                locations:
                                                  description: Locations limits replacement
                                                    to selected HTTP request locations.
                                                  items:
                                                    description: PlaceholderSubstitutionLocation
                                                      identifies an HTTP request location.
                                                    type: string
                                                  type: array
                                                placeholder:
                                                  description: Placeholder is the
                                                    opaque sandbox-visible value to
                                                    replace.
                                                  type: string
                                                valueTemplate:
                                                  description: ValueTemplate is rendered
                                                    against the resolved source payload.
                                                  type: string
                                              required:
                                              - placeholder
                                              - valueTemplate
                                              type: object
                                            type: array
                                        type: object
                                      sshProxy:
                                        description: SSHProxy projects sandbox-side
                                          fake keys and upstream identity for transparent
                                          SSH proxying.
                                        properties:
                                          knownHosts:
                                            description: KnownHosts contains OpenSSH
                                              known_hosts entries used to verify upstream
                                              host keys.
                                            items:
                                              type: string
                                            type: array
                                          sandboxPublicKeys:
                                            description: SandboxPublicKeys are fake
                                              public keys accepted from sandbox-side
                                              SSH clients.
                                            items:
                                              type: string
                                            type: array
                                          upstreamUsername:
                                            description: |-
                                              UpstreamUsername is the username the ctld network runtime uses when
                                              authenticating to the upstream SSH server.
                                            type: string
                                        type: object
                                      tlsClientCertificate:
                                        description: TLSClientCertificate projects
                                          one client certificate for TLS re-origination.
                                        type: object
                                      type:
                                        description: Type selects the runtime projection
                                          shape.
                                        type: string
                                      usernamePassword:
                                        description: UsernamePassword projects one
                                          username/password pair into an early auth
                                          exchange.
                                        type: object
                                    required:
                                    - type
                                    type: object
                                  ref:
                                    description: Ref is the stable identifier matched
                                      by EgressCredentialRule.CredentialRef.
                                    type: string
                                  sourceRef:
                                    description: SourceRef identifies the region-scoped
                                      credential source resolved by manager.
                                    type: string
                                required:
                                - projection
                                - ref
                                - sourceRef
                                type: object
                              type: array
                            egress:
                              description: |-
                                NetworkEgressPolicy defines egress policy.
                                In allow-all mode, denied* fields are enforced and allowed* fields are ignored.
                                In block-all mode, allowed* fields are enforced and denied* fields are ignored.
                              properties:
                                allowedCidrs:
                                  description: 'Deprecated: use TrafficRules instead.'
                                  items:
                                    type: string
                                  type: array
                                allowedDomains:
                                  description: 'Deprecated: use TrafficRules instead.'
                                  items:
                                    type: string
                                  type: array
                                allowedPorts:
                                  description: 'Deprecated: use TrafficRules instead.'
                                  items:
                                    description: PortSpec defines a port specification
                                    properties:
                                      endPort:
                                        description: EndPort for port ranges (optional)
                                        format: int32
                                        type: integer
                                      port:
                                        description: Port number
                                        format: int32
                                        type: integer
                                      protocol:
                                        description: Protocol (tcp or udp)
                                        type: string
                                    required:
                                    - port
                                    type: object
                                  type: array
                                credentialRules:
                                  items:
                                    description: EgressCredentialRule defines a credential
                                      injection rule matched against outbound traffic.
                                    properties:
                                      credentialRef:
                                        description: |-
                                          CredentialRef identifies the binding ref resolved by the runtime egress
                                          auth resolver when this traffic rule matches.
                                        type: string
                                      domains:
                                        description: Domains matches outbound destinations
                                          by DNS name or wildcard suffix.
                                        items:
                                          type: string
                                        type: array
                                      failurePolicy:
                                        description: |-
                                          FailurePolicy controls whether the ctld network runtime should fail-open or
                                          fail-closed when auth material cannot be enforced.
                                        type: string
                                      httpMatch:
                                        description: HTTPMatch constrains HTTP-family
                                          credential injection to request attributes.
                                        properties:
                                          headers:
                                            description: Headers matches HTTP request
                                              headers.
                                            items:
                                              description: HTTPValueMatch defines
                                                one header or query parameter matcher.
                                              properties:
                                                name:
                                                  description: Name is the header
                                                    or query parameter name.
                                                  type: string
                                                present:
                                                  description: Present controls presence-only
                                                    matching when Values is empty.
                                                  type: boolean
                                                values:
                                                  description: Values matches any
                                                    one value. Empty with Present=true
                                                    only requires presence.
                                                  items:
                                                    type: string
                                                  type: array
                                              required:
                                              - name
                                              type: object
                                            type: array
                                          methods:
                                            description: Methods matches HTTP methods.
                                              Values are normalized to uppercase.
                                            items:
                                              type: string
                                            type: array
                                          pathPrefixes:
                                            description: PathPrefixes matches URL
                                              path prefixes.
                                            items:
                                              type: string
                                            type: array
                                          paths:
                                            description: Paths matches exact URL paths.
                                            items:
                                              type: string
                                            type: array
                                          query:
                                            description: Query matches decoded query
                                              parameters.
                                            items:
                                              description: HTTPValueMatch defines
                                                one header or query parameter matcher.
                                              properties:
                                                name:
                                                  description: Name is the header
                                                    or query parameter name.
                                                  type: string
                                                present:
                                                  description: Present controls presence-only
                                                    matching when Values is empty.
                                                  type: boolean
                                                values:
                                                  description: Values matches any
                                                    one value. Empty with Present=true
                                                    only requires presence.
                                                  items:
                                                    type: string
                                                  type: array
                                              required:
                                              - name
                                              type: object
                                            type: array
                                        type: object
                                      name:
                                        description: Name is an optional stable identifier
                                          used for merge and replacement.
                                        type: string
                                      ports:
                                        description: Ports constrains the rule to
                                          specific ports/protocols.
                                        items:
                                          description: PortSpec defines a port specification
                                          properties:
                                            endPort:
                                              description: EndPort for port ranges
                                                (optional)
                                              format: int32
                                              type: integer
                                            port:
                                              description: Port number
                                              format: int32
                                              type: integer
                                            protocol:
                                              description: Protocol (tcp or udp)
                                              type: string
                                          required:
                                          - port
                                          type: object
                                        type: array
                                      protocol:
                                        description: Protocol is the intended application
                                          protocol for the rule.
                                        type: string
                                      rollout:
                                        description: Rollout controls whether this
                                          rule is active. Empty defaults to enabled.
                                        type: string
                                      tlsMode:
                                        description: |-
                                          TLSMode indicates whether the ctld network runtime should intercept TLS for
                                          matching flows.
                                        type: string
                                    required:
                                    - credentialRef
                                    type: object
                                  type: array
                                deniedCidrs:
                                  description: 'Deprecated: use TrafficRules instead.'
                                  items:
                                    type: string
                                  type: array
                                deniedDomains:
                                  description: 'Deprecated: use TrafficRules instead.'
                                  items:
                                    type: string
                                  type: array
                                deniedPorts:
                                  description: 'Deprecated: use TrafficRules instead.'
                                  items:
                                    description: PortSpec defines a port specification
                                    properties:
                                      endPort:
                                        description: EndPort for port ranges (optional)
                                        format: int32
                                        type: integer
                                      port:
                                        description: Port number
                                        format: int32
                                        type: integer
                                      protocol:
                                        description: Protocol (tcp or udp)
                                        type: string
                                    required:
                                    - port
                                    type: object
                                  type: array
                                protocolRules:
                                  items:
                                    description: ProtocolRule defines protocol-aware
                                      controls applied after traffic is allowed.
                                    properties:
                                      domains:
                                        description: Domains matches outbound destinations
                                          by DNS name or wildcard suffix.
                                        items:
                                          type: string
                                        type: array
                                      http:
                                        description: HTTP configures HTTP request
                                          policy.
                                        properties:
                                          methods:
                                            description: Methods controls HTTP methods.
                                            properties:
                                              allowed:
                                                description: Allowed permits only
                                                  listed methods when non-empty.
                                                items:
                                                  type: string
                                                type: array
                                              denied:
                                                description: Denied blocks listed
                                                  methods before evaluating Allowed.
                                                items:
                                                  type: string
                                                type: array
                                            type: object
                                          paths:
                                            description: Paths controls URL paths.
                                            properties:
                                              allowed:
                                                description: Allowed permits only
                                                  listed exact paths when any allowed
                                                  path list is non-empty.
                                                items:
                                                  type: string
                                                type: array
                                              allowedPrefixes:
                                                description: AllowedPrefixes permits
                                                  only paths with listed prefixes
                                                  when any allowed path list is non-empty.
                                                items:
                                                  type: string
                                                type: array
                                              denied:
                                                description: Denied blocks listed
                                                  exact paths before evaluating Allowed.
                                                items:
                                                  type: string
                                                type: array
                                              deniedPrefixes:
                                                description: DeniedPrefixes blocks
                                                  paths with listed prefixes before
                                                  evaluating Allowed.
                                                items:
                                                  type: string
                                                type: array
                                            type: object
                                        type: object
                                      httpMatch:
                                        description: HTTPMatch constrains HTTP-carried
                                          protocol rules to request attributes.
                                        properties:
                                          headers:
                                            description: Headers matches HTTP request
                                              headers.
                                            items:
                                              description: HTTPValueMatch defines
                                                one header or query parameter matcher.
                                              properties:
                                                name:
                                                  description: Name is the header
                                                    or query parameter name.
                                                  type: string
                                                present:
                                                  description: Present controls presence-only
                                                    matching when Values is empty.
                                                  type: boolean
                                                values:
                                                  description: Values matches any
                                                    one value. Empty with Present=true
                                                    only requires presence.
                                                  items:
                                                    type: string
                                                  type: array
                                              required:
                                              - name
                                              type: object
                                            type: array
                                          methods:
                                            description: Methods matches HTTP methods.
                                              Values are normalized to uppercase.
                                            items:
                                              type: string
                                            type: array
                                          pathPrefixes:
                                            description: PathPrefixes matches URL
                                              path prefixes.
                                            items:
                                              type: string
                                            type: array
                                          paths:
                                            description: Paths matches exact URL paths.
                                            items:
                                              type: string
                                            type: array
                                          query:
                                            description: Query matches decoded query
                                              parameters.
                                            items:
                                              description: HTTPValueMatch defines
                                                one header or query parameter matcher.
                                              properties:
                                                name:
                                                  description: Name is the header
                                                    or query parameter name.
                                                  type: string
                                                present:
                                                  description: Present controls presence-only
                                                    matching when Values is empty.
                                                  type: boolean
                                                values:
                                                  description: Values matches any
                                                    one value. Empty with Present=true
                                                    only requires presence.
                                                  items:
                                                    type: string
                                                  type: array
                                              required:
                                              - name
                                              type: object
                                            type: array
                                        type: object
                                      mcp:
                                        description: MCP configures Model Context
                                          Protocol operation policy.
                                        properties:
                                          tools:
                                            description: Tools controls MCP tools/call
                                              requests.
                                            properties:
                                              allowed:
                                                description: Allowed permits only
                                                  listed tools when non-empty.
                                                items:
                                                  type: string
                                                type: array
                                              denied:
                                                description: Denied blocks listed
                                                  tools before evaluating Allowed.
                                                items:
                                                  type: string
                                                type: array
                                            type: object
                                        type: object
                                      name:
                                        description: Name is an optional stable identifier
                                          used for merge and replacement.
                                        type: string
                                      ports:
                                        description: Ports constrains the rule to
                                          specific ports/protocols.
                                        items:
                                          description: PortSpec defines a port specification
                                          properties:
                                            endPort:
                                              description: EndPort for port ranges
                                                (optional)
                                              format: int32
                                              type: integer
                                            port:
                                              description: Port number
                                              format: int32
                                              type: integer
                                            protocol:
                                              description: Protocol (tcp or udp)
                                              type: string
                                          required:
                                          - port
                                          type: object
                                        type: array
                                      protocol:
                                        description: Protocol selects the protocol
                                          adapter for this rule.
                                        type: string
                                      tlsMode:
                                        description: |-
                                          TLSMode controls whether the ctld network runtime must terminate TLS to
                                          inspect this protocol.
                                        type: string
                                    required:
                                    - protocol
                                    type: object
                                  type: array
                                proxy:
                                  description: EgressProxyPolicy configures a transparent
                                    egress proxy for allowed TCP traffic.
                                  properties:
                                    address:
                                      description: Address is the proxy endpoint in
                                        host:port form.
                                      type: string
                                    credentialRef:
                                      description: CredentialRef optionally references
                                        a username_password credential binding.
                                      type: string
                                    type:
                                      description: Type selects the proxy protocol.
                                        The first version supports SOCKS5 only.
                                      type: string
                                  required:
                                  - address
                                  - type
                                  type: object
                                trafficRules:
                                  items:
                                    description: TrafficRule defines one ordered egress
                                      allow/deny matcher.
                                    properties:
                                      action:
                                        description: Action defines whether matching
                                          traffic is allowed or denied.
                                        type: string
                                      appProtocols:
                                        description: AppProtocols constrains the rule
                                          to classified application protocols.
                                        items:
                                          description: TrafficRuleAppProtocol defines
                                            the classified application protocol matched
                                            by one traffic rule.
                                          type: string
                                        type: array
                                      cidrs:
                                        description: CIDRs matches outbound destinations
                                          by IP range.
                                        items:
                                          type: string
                                        type: array
                                      domains:
                                        description: Domains matches outbound destinations
                                          by DNS name or wildcard suffix.
                                        items:
                                          type: string
                                        type: array
                                      name:
                                        description: Name is an optional stable identifier
                                          used for merge and replacement.
                                        type: string
                                      ports:
                                        description: Ports constrains the rule to
                                          specific ports/protocols.
                                        items:
                                          description: PortSpec defines a port specification
                                          properties:
                                            endPort:
                                              description: EndPort for port ranges
                                                (optional)
                                              format: int32
                                              type: integer
                                            port:
                                              description: Port number
                                              format: int32
                                              type: integer
                                            protocol:
                                              description: Protocol (tcp or udp)
                                              type: string
                                          required:
                                          - port
                                          type: object
                                        type: array
                                    required:
                                    - action
                                    type: object
                                  type: array
                              type: object
                            mode:
                              description: NetworkPolicyMode defines network policy
                                mode
                              type: string
                          required:
                          - mode
                          type: object
                        pod:
                          description: Pod-level configuration
                          properties:
                            affinity:
                              description: Affinity defines pod affinity rules
                              properties:
                                nodeAffinity:
                                  description: NodeAffinity defines node affinity
                                    rules
                                  properties:
                                    preferredDuringSchedulingIgnoredDuringExecution:
                                      items:
                                        description: PreferredSchedulingTerm defines
                                          preferred scheduling term
                                        properties:
                                          preference:
                                            description: NodeSelectorTerm defines
                                              node selector term
                                            properties:
                                              matchExpressions:
                                                items:
                                                  description: NodeSelectorRequirement
                                                    defines node selector requirement
                                                  properties:
                                                    key:
                                                      type: string
                                                    operator:
                                                      type: string
                                                    values:
                                                      items:
                                                        type: string
                                                      type: array
                                                  required:
                                                  - key
                                                  - operator
                                                  type: object
                                                type: array
                                              matchFields:
                                                items:
                                                  description: NodeSelectorRequirement
                                                    defines node selector requirement
                                                  properties:
                                                    key:
                                                      type: string
                                                    operator:
                                                      type: string
                                                    values:
                                                      items:
                                                        type: string
                                                      type: array
                                                  required:
                                                  - key
                                                  - operator
                                                  type: object
                                                type: array
                                            type: object
                                          weight:
                                            format: int32
                                            type: integer
                                        required:
                                        - preference
                                        - weight
                                        type: object
                                      type: array
                                    requiredDuringSchedulingIgnoredDuringExecution:
                                      description: NodeSelector defines node selector
                                      properties:
                                        nodeSelectorTerms:
                                          items:
                                            description: NodeSelectorTerm defines
                                              node selector term
                                            properties:
                                              matchExpressions:
                                                items:
                                                  description: NodeSelectorRequirement
                                                    defines node selector requirement
                                                  properties:
                                                    key:
                                                      type: string
                                                    operator:
                                                      type: string
                                                    values:
                                                      items:
                                                        type: string
                                                      type: array
                                                  required:
                                                  - key
                                                  - operator
                                                  type: object
                                                type: array
                                              matchFields:
                                                items:
                                                  description: NodeSelectorRequirement
                                                    defines node selector requirement
                                                  properties:
                                                    key:
                                                      type: string
                                                    operator:
                                                      type: string
                                                    values:
                                                      items:
                                                        type: string
                                                      type: array
                                                  required:
                                                  - key
                                                  - operator
                                                  type: object
                                                type: array
                                            type: object
                                          type: array
                                      required:
                                      - nodeSelectorTerms
                                      type: object
                                  type: object
                                podAffinity:
                                  description: PodAffinity defines pod affinity rules
                                  properties:
                                    preferredDuringSchedulingIgnoredDuringExecution:
                                      items:
                                        description: WeightedPodAffinityTerm defines
                                          weighted pod affinity term
                                        properties:
                                          podAffinityTerm:
                                            description: PodAffinityTerm defines pod
                                              affinity term
                                            properties:
                                              labelSelector:
                                                description: LabelSelector defines
                                                  label selector
                                                properties:
                                                  matchExpressions:
                                                    items:
                                                      description: LabelSelectorRequirement
                                                        defines label selector requirement
                                                      properties:
                                                        key:
                                                          type: string
                                                        operator:
                                                          type: string
                                                        values:
                                                          items:
                                                            type: string
                                                          type: array
                                                      required:
                                                      - key
                                                      - operator
                                                      type: object
                                                    type: array
                                                  matchLabels:
                                                    additionalProperties:
                                                      type: string
                                                    type: object
                                                type: object
                                              namespaces:
                                                items:
                                                  type: string
                                                type: array
                                              topologyKey:
                                                type: string
                                            required:
                                            - topologyKey
                                            type: object
                                          weight:
                                            format: int32
                                            type: integer
                                        required:
                                        - podAffinityTerm
                                        - weight
                                        type: object
                                      type: array
                                    requiredDuringSchedulingIgnoredDuringExecution:
                                      items:
                                        description: PodAffinityTerm defines pod affinity
                                          term
                                        properties:
                                          labelSelector:
                                            description: LabelSelector defines label
                                              selector
                                            properties:
                                              matchExpressions:
                                                items:
                                                  description: LabelSelectorRequirement
                                                    defines label selector requirement
                                                  properties:
                                                    key:
                                                      type: string
                                                    operator:
                                                      type: string
                                                    values:
                                                      items:
                                                        type: string
                                                      type: array
                                                  required:
                                                  - key
                                                  - operator
                                                  type: object
                                                type: array
                                              matchLabels:
                                                additionalProperties:
                                                  type: string
                                                type: object
                                            type: object
                                          namespaces:
                                            items:
                                              type: string
                                            type: array
                                          topologyKey:
                                            type: string
                                        required:
                                        - topologyKey
                                        type: object
                                      type: array
                                  type: object
                              type: object
                            emptyDirMounts:
                              items:
                                description: EmptyDirMountSpec declares an ephemeral
                                  Kubernetes emptyDir mount for the main sandbox container.
                                properties:
                                  mountPath:
                                    type: string
                                  sizeLimit:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                required:
                                - mountPath
                                type: object
                              type: array
                            nodeSelector:
                              additionalProperties:
                                type: string
                              type: object
                            serviceAccountName:
                              type: string
                            tolerations:
                              items:
                                description: Toleration defines pod toleration
                                properties:
                                  effect:
                                    type: string
                                  key:
                                    type: string
                                  operator:
                                    type: string
                                  value:
                                    type: string
                                type: object
                              type: array
                          type: object
                        pool:
                          description: Pool strategy
                          properties:
                            maxIdle:
                              format: int32
                              type: integer
                            minIdle:
                              format: int32
                              type: integer
                          required:
                          - maxIdle
                          - minIdle
                          type: object
                        tags:
                          items:
                            type: string
                          type: array
                        volumeMounts:
                          description: |-
                            VolumeMounts declares the fixed sandbox volume portal mount points that
                            can be bound to concrete SandboxVolumes when a sandbox is claimed.
                          items:
                            properties:
                              mountPath:
                                type: string
                              name:
                                type: string
                              readOnly:
                                type: boolean
                            required:
                            - mountPath
                            - name
                            type: object
                          type: array
                      required:
                      - mainContainer
                      - pool
                      type: object
                    templateId:
                      type: string
                  required:
                  - templateId
                  type: object
                type: array
              clickHouse:
                default:
                  type: disabled
                description: |-
                  ClickHouse configures the region-level ClickHouse data component used by
                  ClickHouse-backed features such as sandbox observability and metering.
                properties:
                  builtin:
                    description: Builtin configures the operator-managed ClickHouse
                      instance.
                    properties:
                      enabled:
                        default: true
                        description: Enabled enables the built-in ClickHouse instance
                          when type is builtin.
                        type: boolean
                      httpPort:
                        default: 8123
                        description: HTTPPort is the ClickHouse HTTP port.
                        format: int32
                        type: integer
                        x-kubernetes-validations:
                        - message: httpPort is immutable after creation
                          rule: self == oldSelf
                      image:
                        default: clickhouse/clickhouse-server:24.8
                        description: Image is the ClickHouse server image.
                        type: string
                      nativePort:
                        default: 9000
                        description: NativePort is the ClickHouse native TCP port.
                        format: int32
                        type: integer
                        x-kubernetes-validations:
                        - message: nativePort is immutable after creation
                          rule: self == oldSelf
                      persistence:
                        description: Persistence configures ClickHouse storage.
                        properties:
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 20Gi
                            description: Size specifies the storage size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: StorageClass specifies the storage class
                              (empty for default)
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: persistence is immutable after creation
                          rule: self == oldSelf
                      statefulResourcePolicy:
                        default: Retain
                        description: |-
                          StatefulResourcePolicy controls what happens to the builtin PVC and
                          generated credentials secret when the builtin backend is disabled or
                          replaced by an external backend.
                        enum:
                        - Retain
                        - Delete
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: persistence presence is immutable after creation
                      rule: has(self.persistence) == has(oldSelf.persistence)
                  databases:
                    default: {}
                    description: Databases configures logical ClickHouse databases
                      used by Sandbox0 features.
                    properties:
                      metering:
                        default: sandbox0_metering
                        description: Metering is the database for the regional usage
                          ledger.
                        type: string
                      observability:
                        default: sandbox0_observability
                        description: Observability is the database for per-sandbox
                          historical observability.
                        type: string
                    type: object
                  external:
                    description: External configures a user-owned ClickHouse instance.
                    properties:
                      connectTimeout:
                        default: 10s
                        description: ConnectTimeout bounds startup connection and
                          schema checks.
                        type: string
                      dsnSecret:
                        description: DSNSecret references a secret key containing
                          the ClickHouse database/sql DSN.
                        properties:
                          key:
                            default: dsn
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - dsnSecret
                    type: object
                  schemaMigration:
                    default: {}
                    description: SchemaMigration configures whether services run ClickHouse
                      schema migrations.
                    properties:
                      enabled:
                        default: true
                        description: Enabled allows Sandbox0 services to run ClickHouse
                          CREATE/ALTER statements.
                        type: boolean
                    type: object
                  type:
                    allOf:
                    - enum:
                      - disabled
                      - builtin
                      - external
                    - enum:
                      - disabled
                      - builtin
                      - external
                    default: disabled
                    description: Type selects whether ClickHouse is disabled, operator-managed,
                      or external.
                    type: string
                type: object
              cluster:
                description: Cluster configures cluster identification and capacity
                properties:
                  capacity:
                    description: Capacity specifies cluster resource capacity
                    properties:
                      cpu:
                        description: CPU specifies CPU capacity
                        properties:
                          available:
                            description: Available is the available capacity
                            type: string
                          total:
                            description: Total is the total capacity
                            type: string
                        type: object
                      maxSandboxes:
                        description: MaxSandboxes is the maximum number of sandboxes
                        format: int32
                        type: integer
                      memory:
                        description: Memory specifies memory capacity
                        properties:
                          available:
                            description: Available is the available capacity
                            type: string
                          total:
                            description: Total is the total capacity
                            type: string
                        type: object
                    type: object
                  id:
                    description: |-
                      ID is the unique Sandbox0 data-plane cluster identifier used in routing and sandbox names.
                      It is separate from the provider cluster name and must stay short enough for sandbox name encoding.
                    maxLength: 20
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  name:
                    description: Name is the human-readable cluster name
                    type: string
                required:
                - id
                type: object
              controlPlane:
                description: ControlPlane configures external control plane connection.
                properties:
                  internalAuthPublicKeySecret:
                    description: InternalAuthPublicKeySecret references the secret
                      containing control plane's public key
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the name of the secret
                        type: string
                    type: object
                  url:
                    description: URL is the control plane regional-gateway URL
                    type: string
                required:
                - internalAuthPublicKeySecret
                - url
                type: object
              credentialVault:
                description: |-
                  CredentialVault configures a Vault-compatible backend for credential
                  sources that use hashicorp_vault storage or external references.
                properties:
                  builtin:
                    default: {}
                    description: Builtin configures the built-in OpenBao instance.
                    properties:
                      enabled:
                        default: true
                        description: Enabled enables the built-in OpenBao instance.
                        type: boolean
                      image:
                        default: openbao/openbao:2.3.1
                        description: Image specifies the OpenBao image for the builtin
                          instance.
                        type: string
                      mount:
                        default: secret
                        description: Mount configures the KV v2 mount path used by
                          Sandbox0.
                        type: string
                      persistence:
                        description: Persistence configures OpenBao storage.
                        properties:
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 20Gi
                            description: Size specifies the storage size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: StorageClass specifies the storage class
                              (empty for default)
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: persistence is immutable after creation
                          rule: self == oldSelf
                      port:
                        default: 8200
                        description: Port specifies the OpenBao API port.
                        format: int32
                        type: integer
                      statefulResourcePolicy:
                        default: Retain
                        description: |-
                          StatefulResourcePolicy controls what happens to the builtin PVC and token
                          secret when the builtin vault is disabled or replaced by an external vault.
                        enum:
                        - Retain
                        - Delete
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: persistence presence is immutable after creation
                      rule: has(self.persistence) == has(oldSelf.persistence)
                  external:
                    description: External configures an existing HashiCorp Vault-compatible
                      endpoint.
                    properties:
                      address:
                        description: Address is the base URL of the external Vault
                          endpoint.
                        type: string
                      allowedPathPrefixes:
                        description: |-
                          AllowedPathPrefixes limits credential source references resolved through this connection.
                          The literal string {{teamID}} is replaced with the caller team id.
                        items:
                          type: string
                        type: array
                      caCertSecret:
                        description: CACertSecret optionally references a PEM CA bundle
                          for the Vault endpoint.
                        properties:
                          key:
                            default: ca.crt
                            description: Key is the key containing the PEM CA bundle.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      mount:
                        default: secret
                        description: Mount configures the default KV v2 mount path.
                        type: string
                      namespace:
                        description: Namespace sets X-Vault-Namespace for Vault Enterprise
                          deployments.
                        type: string
                      skipTLSVerify:
                        default: false
                        description: SkipTLSVerify disables TLS verification for development-only
                          external Vault endpoints.
                        type: boolean
                      tokenSecret:
                        description: TokenSecret references a secret containing a
                          Vault token for manager.
                        properties:
                          key:
                            default: token
                            description: Key is the key containing the Vault token.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - address
                    - tokenSecret
                    type: object
                  type:
                    default: builtin
                    description: 'Type specifies the Vault backend type: builtin or
                      external.'
                    enum:
                    - builtin
                    - external
                    type: string
                type: object
              database:
                description: Database configures the main database for sandbox0
                properties:
                  builtin:
                    default: {}
                    description: Builtin configures the built-in single-node PostgreSQL
                    properties:
                      database:
                        default: sandbox0
                        description: Database specifies the database name
                        type: string
                        x-kubernetes-validations:
                        - message: database is immutable after creation
                          rule: self == oldSelf
                      enabled:
                        default: true
                        description: Enabled enables the built-in database
                        type: boolean
                      image:
                        default: postgres:16-alpine
                        description: Image specifies the postgres image for the builtin
                          database
                        type: string
                      persistence:
                        description: Persistence configures database storage
                        properties:
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 20Gi
                            description: Size specifies the storage size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: StorageClass specifies the storage class
                              (empty for default)
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: persistence is immutable after creation
                          rule: self == oldSelf
                      port:
                        default: 5432
                        description: Port specifies the database port
                        format: int32
                        type: integer
                        x-kubernetes-validations:
                        - message: port is immutable after creation
                          rule: self == oldSelf
                      sslMode:
                        default: disable
                        description: SSLMode specifies the SSL mode for builtin DSN
                        type: string
                      statefulResourcePolicy:
                        default: Retain
                        description: |-
                          StatefulResourcePolicy controls what happens to the builtin PVC and
                          generated credentials secret when the builtin database is disabled or
                          replaced by an external database.
                        enum:
                        - Retain
                        - Delete
                        type: string
                      username:
                        default: sandbox0
                        description: Username specifies the database username
                        type: string
                        x-kubernetes-validations:
                        - message: username is immutable after creation
                          rule: self == oldSelf
                    type: object
                    x-kubernetes-validations:
                    - message: persistence presence is immutable after creation
                      rule: has(self.persistence) == has(oldSelf.persistence)
                  external:
                    description: External configures connection to external database
                    properties:
                      database:
                        description: Database specifies the database name
                        type: string
                      host:
                        description: Host specifies the database host
                        type: string
                      passwordSecret:
                        description: PasswordSecret references the secret containing
                          the password
                        properties:
                          key:
                            default: password
                            description: Key is the key in the secret
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                        type: object
                      port:
                        default: 5432
                        description: Port specifies the database port
                        format: int32
                        type: integer
                      sslMode:
                        default: require
                        description: SSLMode specifies the SSL mode for connection
                        type: string
                      username:
                        description: Username specifies the database username
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - username
                    type: object
                  type:
                    default: builtin
                    description: 'Type specifies the postgres database type: builtin,
                      or external'
                    enum:
                    - builtin
                    - external
                    type: string
                type: object
              enterpriseLicense:
                description: |-
                  EnterpriseLicense configures the signed enterprise license used by
                  licensed services such as scheduler and regional-gateway.
                properties:
                  secretRef:
                    description: |-
                      SecretRef references the secret containing the signed enterprise license.
                      When omitted, infra-operator falls back to <infra-name>-enterprise-license
                      with key license.lic for backward compatibility.
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the name of the secret
                        type: string
                    type: object
                type: object
              initUser:
                description: InitUser configures the initial admin user
                properties:
                  email:
                    description: Email is the admin user's email
                    type: string
                  homeRegionId:
                    description: HomeRegionID is required for global-gateway init
                      users so the bootstrap team is routable.
                    type: string
                  name:
                    description: Name is the admin user's display name
                    type: string
                  passwordSecret:
                    description: PasswordSecret references the secret containing the
                      password
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the name of the secret
                        type: string
                    type: object
                type: object
              internalAuth:
                description: InternalAuth configures internal authentication keys
                properties:
                  controlPlane:
                    description: ControlPlane configures control plane key pair
                    properties:
                      generate:
                        default: true
                        description: Generate enables automatic key generation
                        type: boolean
                      secretRef:
                        description: SecretRef references an existing secret containing
                          the key pair
                        properties:
                          name:
                            description: Name is the name of the secret
                            type: string
                          privateKeyKey:
                            default: private.key
                            description: PrivateKeyKey is the key for private key
                            type: string
                          publicKeyKey:
                            default: public.key
                            description: PublicKeyKey is the key for public key
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  dataPlane:
                    description: DataPlane configures data plane key pair
                    properties:
                      generate:
                        default: true
                        description: Generate enables automatic key generation
                        type: boolean
                      secretRef:
                        description: SecretRef references an existing secret containing
                          the key pair
                        properties:
                          name:
                            description: Name is the name of the secret
                            type: string
                          privateKeyKey:
                            default: private.key
                            description: PrivateKeyKey is the key for private key
                            type: string
                          publicKeyKey:
                            default: public.key
                            description: PublicKeyKey is the key for public key
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                type: object
              metadataDatabase:
                description: MetadataDatabase configures the S0FS metadata database
                properties:
                  external:
                    description: External configures an independent database for S0FS
                    properties:
                      database:
                        description: Database specifies the database name
                        type: string
                      host:
                        description: Host specifies the database host
                        type: string
                      passwordSecret:
                        description: PasswordSecret references the secret containing
                          the password
                        properties:
                          key:
                            default: password
                            description: Key is the key in the secret
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                        type: object
                      port:
                        default: 5432
                        description: Port specifies the database port
                        format: int32
                        type: integer
                      sslMode:
                        default: require
                        description: SSLMode specifies the SSL mode for connection
                        type: string
                      username:
                        description: Username specifies the database username
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - username
                    type: object
                  shareWithMain:
                    default: true
                    description: ShareWithMain uses the main database for S0FS metadata
                    type: boolean
                type: object
              metering:
                default: {}
                description: Metering configures the region usage ledger backend.
                properties:
                  clickHouse:
                    default: {}
                    description: ClickHouse configures table names and write policy
                      for ClickHouse metering.
                    properties:
                      eventsTable:
                        default: usage_events
                        type: string
                      retention:
                        default: {}
                        description: MeteringClickHouseRetentionConfig configures
                          usage ledger retention.
                        properties:
                          eventsDays:
                            default: 400
                            type: integer
                          windowsDays:
                            default: 400
                            type: integer
                        type: object
                      sandboxStateTable:
                        default: sandbox_projection_state
                        type: string
                      storageStateTable:
                        default: storage_projection_state
                        type: string
                      watermarksTable:
                        default: producer_watermarks
                        type: string
                      windowsTable:
                        default: usage_windows
                        type: string
                      write:
                        default: {}
                        description: MeteringClickHouseWriteConfig configures ClickHouse
                          metering write behavior.
                        properties:
                          failPolicy:
                            default: buffer
                            description: FailPolicy selects behavior when durable
                              writes cannot complete.
                            enum:
                            - buffer
                            - fail-closed
                            type: string
                          mode:
                            default: durable
                            description: Mode selects durable write semantics. Only
                              durable is accepted.
                            enum:
                            - durable
                            type: string
                        type: object
                    type: object
                  enabled:
                    default: false
                    description: Enabled enables PostgreSQL-buffered, ClickHouse-backed
                      metering. Metering is disabled by default.
                    type: boolean
                type: object
              network:
                description: Network configures the node-local sandbox network runtime
                  hosted by ctld.
                properties:
                  config:
                    default: {}
                    description: Config contains network runtime configuration.
                    properties:
                      auditLogMaxBackups:
                        default: 5
                        type: integer
                      auditLogMaxBytes:
                        default: 104857600
                        format: int64
                        type: integer
                      auditLogPath:
                        type: string
                      bandwidthBurstBytes:
                        description: Token bucket burst in bytes for bandwidth limiting.
                          Zero uses one second of the configured rate.
                        format: int64
                        minimum: 0
                        type: integer
                      bpfFsPath:
                        type: string
                      bpfPinPath:
                        type: string
                      burstRatio:
                        default: "0.125"
                        type: string
                      dnsPort:
                        default: 53
                        type: integer
                      edtHorizon:
                        default: 200ms
                        type: string
                      egressAuthEnabled:
                        default: false
                        type: boolean
                      egressAuthFailurePolicy:
                        default: fail-closed
                        type: string
                      egressAuthResolverTimeout:
                        default: 2s
                        type: string
                      egressAuthResolverUrl:
                        type: string
                      egressBandwidthBytesPerSecond:
                        description: Per-sandbox egress bandwidth limit in bytes per
                          second. Zero disables throttling.
                        format: int64
                        minimum: 0
                        type: integer
                      failClosed:
                        default: true
                        type: boolean
                      healthPort:
                        default: 8081
                        type: integer
                      ingressBandwidthBytesPerSecond:
                        description: Per-sandbox ingress bandwidth limit in bytes
                          per second. Zero disables throttling.
                        format: int64
                        minimum: 0
                        type: integer
                      logLevel:
                        default: info
                        type: string
                      meteringReportInterval:
                        default: 10s
                        type: string
                      metricsPort:
                        default: 9091
                        type: integer
                      metricsReportInterval:
                        default: 10s
                        type: string
                      mitmLeafTtl:
                        default: 1h
                        type: string
                      nodeName:
                        type: string
                      platformAllowedCidrs:
                        items:
                          type: string
                        type: array
                      platformAllowedDomains:
                        items:
                          type: string
                        type: array
                      platformDeniedCidrs:
                        items:
                          type: string
                        type: array
                      platformDeniedDomains:
                        items:
                          type: string
                        type: array
                      preferNft:
                        default: true
                        type: boolean
                      proxyHeaderLimit:
                        format: int64
                        type: integer
                      proxyHttpPort:
                        default: 18080
                        type: integer
                      proxyHttpsPort:
                        default: 18443
                        type: integer
                      proxyListenAddr:
                        default: 0.0.0.0
                        type: string
                      proxyUpstreamTimeout:
                        default: 30s
                        type: string
                      resyncPeriod:
                        default: 30s
                        type: string
                      shutdownDelay:
                        default: 2s
                        type: string
                      teamBandwidthBurstBytes:
                        description: Token bucket burst in bytes for team bandwidth
                          limiting. Zero uses one second of the configured rate.
                        format: int64
                        minimum: 0
                        type: integer
                      teamEgressBandwidthBytesPerSecond:
                        description: Cluster-scoped per-team egress bandwidth limit
                          in bytes per second. Requires spec.redis. Zero disables
                          throttling.
                        format: int64
                        minimum: 0
                        type: integer
                      teamIngressBandwidthBytesPerSecond:
                        description: Cluster-scoped per-team ingress bandwidth limit
                          in bytes per second. Requires spec.redis. Zero disables
                          throttling.
                        format: int64
                        minimum: 0
                        type: integer
                      useEbpf:
                        type: boolean
                      useEdt:
                        type: boolean
                      vethPrefix:
                        type: string
                    type: object
                  mitmCaSecretName:
                    description: |-
                      MITMCASecretName overrides the operator-managed cluster-local MITM CA
                      secret for HTTPS interception. Expected keys are ca.crt and ca.key. When
                      unset, infra-operator generates and reuses a managed secret.
                    type: string
                type: object
              observability:
                description: |-
                  Observability configures standard telemetry collection and export for
                  platform services.
                properties:
                  backend:
                    default:
                      type: disabled
                    description: |-
                      Backend configures optional observability export integration. It is disabled
                      by default. Sandbox0 does not manage an observability storage backend.
                    properties:
                      external:
                        description: External configures integration with user-owned
                          observability infrastructure.
                        properties:
                          collector:
                            description: |-
                              Collector configures the operator-managed collector when mode is
                              managedCollector.
                            properties:
                              image:
                                default: otel/opentelemetry-collector-contrib:0.130.1
                                description: Image specifies the OpenTelemetry Collector
                                  contrib image.
                                type: string
                              replicas:
                                default: 1
                                description: Replicas configures gateway collector
                                  replicas.
                                format: int32
                                type: integer
                              resources:
                                description: Resources overrides collector container
                                  resources.
                                properties:
                                  claims:
                                    description: |-
                                      Claims lists the names of resources, defined in spec.resourceClaims,
                                      that are used by this container.

                                      This field depends on the
                                      DynamicResourceAllocation feature gate.

                                      This field is immutable. It can only be set for containers.
                                    items:
                                      description: ResourceClaim references one entry
                                        in PodSpec.ResourceClaims.
                                      properties:
                                        name:
                                          description: |-
                                            Name must match the name of one entry in pod.spec.resourceClaims of
                                            the Pod where this field is used. It makes that resource available
                                            inside a container.
                                          type: string
                                        request:
                                          description: |-
                                            Request is the name chosen for a request in the referenced claim.
                                            If empty, everything from the claim is made available, otherwise
                                            only the result of this request.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                    x-kubernetes-list-map-keys:
                                    - name
                                    x-kubernetes-list-type: map
                                  limits:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Limits describes the maximum amount of compute resources allowed.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                  requests:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: |-
                                      Requests describes the minimum amount of compute resources required.
                                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    type: object
                                type: object
                            type: object
                          mode:
                            default: existingCollector
                            description: |-
                              Mode selects whether sandbox0 deploys collectors or points services at an
                              existing collector endpoint. Defaults to existingCollector.
                            enum:
                            - managedCollector
                            - existingCollector
                            type: string
                          otlp:
                            description: OTLP configures the OTLP endpoint used for
                              external export integration.
                            properties:
                              endpoint:
                                description: Endpoint is the OTLP gRPC endpoint.
                                type: string
                              headers:
                                additionalProperties:
                                  type: string
                                description: Headers are sent with OTLP exports.
                                type: object
                              headersSecret:
                                description: |-
                                  HeadersSecret references a secret containing OTLP headers formatted as an
                                  OTEL_EXPORTER_OTLP_HEADERS key-value list.
                                properties:
                                  key:
                                    default: headers
                                    description: Key is the Secret key containing
                                      the header list.
                                    type: string
                                  name:
                                    description: Name is the name of the Secret.
                                    type: string
                                required:
                                - name
                                type: object
                              insecure:
                                description: Insecure disables TLS verification when
                                  exporting through OTLP.
                                type: boolean
                              timeout:
                                description: Timeout bounds OTLP export requests.
                                type: string
                            type: object
                        type: object
                      type:
                        default: disabled
                        description: Type selects the export integration mode. Defaults
                          to disabled.
                        enum:
                        - disabled
                        - external
                        type: string
                    type: object
                  collection:
                    description: |-
                      Collection configures which signals sandbox0 should collect when
                      observability export is enabled. It does not define user-facing query APIs.
                    properties:
                      logs:
                        description: Logs configures platform and sandbox pod log
                          collection.
                        properties:
                          enabled:
                            default: true
                            description: |-
                              Enabled enables this signal. Defaults to true when observability export is
                              enabled.
                            type: boolean
                        type: object
                      metrics:
                        description: Metrics configures Prometheus-compatible metric
                          scraping.
                        properties:
                          enabled:
                            default: true
                            description: |-
                              Enabled enables this signal. Defaults to true when observability export is
                              enabled.
                            type: boolean
                        type: object
                      traces:
                        description: Traces configures platform service trace collection.
                        properties:
                          enabled:
                            default: true
                            description: |-
                              Enabled enables this signal. Defaults to true when observability export is
                              enabled.
                            type: boolean
                        type: object
                    type: object
                  resourceAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      ResourceAttributes are appended to OTEL_RESOURCE_ATTRIBUTES for every
                      platform component. Built-in service, region, cluster, and Kubernetes
                      attributes take precedence on key conflicts.
                    type: object
                  traces:
                    description: |-
                      Traces configures trace export through standard OpenTelemetry env vars.
                      This legacy direct exporter config remains supported and takes precedence
                      over export-derived trace endpoint defaults.
                    properties:
                      enabled:
                        description: |-
                          Enabled enables trace exporter env injection. When false or omitted,
                          services keep their default noop tracer exporter.
                        type: boolean
                      endpoint:
                        description: Endpoint is written to OTEL_EXPORTER_OTLP_TRACES_ENDPOINT.
                        type: string
                      exporter:
                        description: |-
                          Exporter is written to OTEL_TRACES_EXPORTER. Defaults to otlp when traces
                          are enabled.
                        enum:
                        - otlp
                        - stdout
                        - noop
                        type: string
                      headers:
                        additionalProperties:
                          type: string
                        description: Headers are written to OTEL_EXPORTER_OTLP_TRACES_HEADERS.
                        type: object
                      insecure:
                        description: Insecure is written to OTEL_EXPORTER_OTLP_TRACES_INSECURE
                          when set.
                        type: boolean
                      sampleRate:
                        description: |-
                          SampleRate configures OTEL_TRACES_SAMPLER and OTEL_TRACES_SAMPLER_ARG.
                          Valid values are decimal strings between 0 and 1, for example "0.25".
                        type: string
                      timeout:
                        description: Timeout is written to OTEL_EXPORTER_OTLP_TRACES_TIMEOUT.
                        type: string
                    type: object
                type: object
              publicExposure:
                description: PublicExposure configures public URL exposure for sandboxes
                properties:
                  enabled:
                    default: true
                    description: Enabled enables public exposure routing
                    type: boolean
                  regionId:
                    default: aws-us-east-1
                    description: |-
                      RegionID is the DNS-safe region label used in public URLs.
                      It is not the canonical multi-region tenancy identifier.
                    type: string
                  rootDomain:
                    default: sandbox0.app
                    description: RootDomain is the root domain for public exposure
                      URLs
                    type: string
                type: object
              redis:
                description: |-
                  Redis configures a region-level Redis backend for shared services such as
                  distributed gateway rate limiting. When omitted, gateway rate limiting
                  uses process-local memory.
                properties:
                  builtin:
                    default: {}
                    description: Builtin configures the built-in single-node Redis
                      instance.
                    properties:
                      enabled:
                        default: true
                        description: Enabled enables the built-in Redis instance.
                        type: boolean
                      image:
                        default: redis:7-alpine
                        description: Image specifies the Redis image for the builtin
                          instance.
                        type: string
                      port:
                        default: 6379
                        description: Port specifies the Redis port.
                        format: int32
                        type: integer
                    type: object
                  external:
                    description: External configures connection to an external Redis
                      instance.
                    properties:
                      urlSecret:
                        description: URLSecret references a secret containing the
                          Redis URL.
                        properties:
                          key:
                            default: url
                            description: Key is the key containing the Redis URL.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - urlSecret
                    type: object
                  failOpen:
                    default: true
                    description: FailOpen allows traffic when Redis is temporarily
                      unavailable.
                    type: boolean
                  keyPrefix:
                    default: sandbox0
                    description: KeyPrefix prefixes keys used by sandbox0 Redis clients.
                    type: string
                  operationTimeout:
                    default: 100ms
                    description: OperationTimeout bounds each Redis operation.
                    type: string
                  type:
                    default: builtin
                    description: 'Type specifies the Redis backend type: builtin or
                      external.'
                    enum:
                    - builtin
                    - external
                    type: string
                type: object
              region:
                description: Region identifies the region for multi-cluster deployments
                type: string
              registry:
                description: Registry configures the container registry
                properties:
                  aliyun:
                    description: Aliyun configures Aliyun registry integration.
                    properties:
                      credentialsSecret:
                        description: CredentialsSecret references Aliyun credentials
                          for short-lived tokens.
                        properties:
                          accessKeyKey:
                            default: accessKeyId
                            description: AccessKeyKey is the key for access key ID.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                          secretKeyKey:
                            default: accessKeySecret
                            description: SecretKeyKey is the key for secret access
                              key.
                            type: string
                        required:
                        - name
                        type: object
                      instanceId:
                        description: InstanceID specifies the ACR instance ID.
                        type: string
                      pullSecret:
                        description: PullSecret references the dockerconfigjson secret
                          to use for image pulls.
                        properties:
                          key:
                            default: .dockerconfigjson
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      region:
                        description: Region specifies the Aliyun region.
                        type: string
                      registry:
                        description: Registry specifies the registry hostname.
                        type: string
                    required:
                    - credentialsSecret
                    - instanceId
                    - pullSecret
                    - region
                    - registry
                    type: object
                  aws:
                    description: AWS configures AWS registry integration.
                    properties:
                      assumeRoleArn:
                        description: AssumeRoleARN optionally scopes registry pushes
                          through an assumed IAM role.
                        type: string
                      credentialsSecret:
                        description: CredentialsSecret references AWS credentials
                          for short-lived tokens.
                        properties:
                          accessKeyKey:
                            default: accessKeyId
                            description: AccessKeyKey is the key for access key ID.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                          secretKeyKey:
                            default: secretAccessKey
                            description: SecretKeyKey is the key for secret access
                              key.
                            type: string
                          sessionTokenKey:
                            description: SessionTokenKey is the key for session token
                              (optional).
                            type: string
                        required:
                        - name
                        type: object
                      externalId:
                        description: ExternalID is passed to STS AssumeRole when assumeRoleArn
                          is configured.
                        type: string
                      pullSecret:
                        description: PullSecret references the dockerconfigjson secret
                          to use for image pulls.
                        properties:
                          key:
                            default: .dockerconfigjson
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      region:
                        description: Region specifies the AWS region.
                        type: string
                      registry:
                        description: Registry specifies the registry hostname.
                        type: string
                      registryId:
                        description: RegistryID specifies the AWS account ID (optional).
                        type: string
                    required:
                    - credentialsSecret
                    - pullSecret
                    - region
                    type: object
                  azure:
                    description: Azure configures Azure registry integration.
                    properties:
                      credentialsSecret:
                        description: CredentialsSecret references the client credentials
                          for ACR.
                        properties:
                          clientIdKey:
                            default: clientId
                            description: ClientIDKey is the key for client ID.
                            type: string
                          clientSecretKey:
                            default: clientSecret
                            description: ClientSecretKey is the key for client secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                          tenantIdKey:
                            default: tenantId
                            description: TenantIDKey is the key for tenant ID.
                            type: string
                        required:
                        - name
                        type: object
                      pullSecret:
                        description: PullSecret references the dockerconfigjson secret
                          to use for image pulls.
                        properties:
                          key:
                            default: .dockerconfigjson
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      registry:
                        description: Registry specifies the registry hostname.
                        type: string
                    required:
                    - credentialsSecret
                    - pullSecret
                    - registry
                    type: object
                  builtin:
                    description: Builtin configures the built-in registry.
                    properties:
                      credentialsSecret:
                        description: |-
                          CredentialsSecret references the secret containing registry credentials.
                          If omitted, the operator will generate a secret named "<infra-name>-registry-credentials".
                        properties:
                          name:
                            description: Name is the name of the secret.
                            type: string
                          passwordKey:
                            default: password
                            description: PasswordKey is the key for password.
                            type: string
                          usernameKey:
                            default: username
                            description: UsernameKey is the key for username.
                            type: string
                        required:
                        - name
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables the built-in registry.
                        type: boolean
                      image:
                        default: registry:2.8.3
                        description: Image specifies the registry image.
                        type: string
                      ingress:
                        description: Ingress configures ingress settings for external
                          registry access.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations specifies provider-specific ingress
                              annotations.
                            type: object
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          extraHosts:
                            description: ExtraHosts specifies additional ingress hosts
                              routed to the same backend.
                            items:
                              type: string
                            type: array
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tls:
                            description: |-
                              TLS specifies host groups and their backing TLS secrets.
                              When set, TLS takes precedence over TLSSecret.
                            items:
                              description: IngressTLSConfig defines one TLS certificate
                                binding for ingress hosts.
                              properties:
                                hosts:
                                  description: Hosts specifies the hosts covered by
                                    this TLS secret.
                                  items:
                                    type: string
                                  type: array
                                secretName:
                                  description: SecretName specifies the Kubernetes
                                    TLS secret name.
                                  type: string
                              type: object
                            type: array
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      persistence:
                        description: Persistence configures registry persistence.
                        properties:
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 20Gi
                            description: Size specifies the storage size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: StorageClass specifies the storage class
                              (empty for default)
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: persistence is immutable after creation
                          rule: self == oldSelf
                      port:
                        default: 5000
                        description: Port specifies the registry port.
                        format: int32
                        type: integer
                      pushEndpoint:
                        description: |-
                          PushEndpoint overrides the external registry endpoint used for image push credentials.
                          Use host[:port] format, without scheme.
                        type: string
                      service:
                        description: Service configures the registry service exposure.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: |-
                              Annotations declares Service metadata annotations.
                              Use this for cloud-provider specific load balancer configuration such as
                              ACM certificate attachment on AWS.
                            type: object
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: port must be within 30000-32767 when type is NodePort
                          rule: self.type != 'NodePort' || self.port == 0 || (self.port
                            >= 30000 && self.port <= 32767)
                      statefulResourcePolicy:
                        default: Retain
                        description: |-
                          StatefulResourcePolicy controls what happens to the builtin registry PVC
                          when the builtin registry is disabled or replaced by an external
                          registry provider.
                        enum:
                        - Retain
                        - Delete
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: persistence presence is immutable after creation
                      rule: has(self.persistence) == has(oldSelf.persistence)
                  gcp:
                    description: GCP configures GCP registry integration.
                    properties:
                      pullSecret:
                        description: |-
                          PullSecret references the dockerconfigjson secret to use for image pulls.
                          Omit this on GKE when nodes or workload identity can already pull from
                          Artifact Registry without a namespace-local imagePullSecret.
                        properties:
                          key:
                            default: .dockerconfigjson
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      registry:
                        description: Registry specifies the registry hostname.
                        type: string
                      serviceAccountSecret:
                        description: |-
                          ServiceAccountSecret references the service account JSON key.
                          Omit this to use application default credentials inside manager.
                        properties:
                          key:
                            default: serviceAccount.json
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - registry
                    type: object
                  harbor:
                    description: Harbor configures Harbor registry integration.
                    properties:
                      credentialsSecret:
                        description: CredentialsSecret references Harbor credentials
                          for push authentication.
                        properties:
                          name:
                            description: Name is the name of the secret.
                            type: string
                          passwordKey:
                            default: password
                            description: PasswordKey is the key for password.
                            type: string
                          usernameKey:
                            default: username
                            description: UsernameKey is the key for username.
                            type: string
                        required:
                        - name
                        type: object
                      pullSecret:
                        description: PullSecret references the dockerconfigjson secret
                          to use for image pulls.
                        properties:
                          key:
                            default: .dockerconfigjson
                            description: Key is the key in the secret.
                            type: string
                          name:
                            description: Name is the name of the secret.
                            type: string
                        required:
                        - name
                        type: object
                      registry:
                        description: Registry specifies the registry hostname.
                        type: string
                    required:
                    - credentialsSecret
                    - pullSecret
                    - registry
                    type: object
                  imagePullSecretName:
                    default: sandbox0-registry-pull
                    description: ImagePullSecretName is the secret name to create
                      in template namespaces.
                    type: string
                  provider:
                    allOf:
                    - enum:
                      - builtin
                      - aws
                      - gcp
                      - azure
                      - aliyun
                      - harbor
                    - enum:
                      - builtin
                      - aws
                      - gcp
                      - azure
                      - aliyun
                      - harbor
                    default: builtin
                    description: 'Provider specifies the registry provider: builtin,
                      aws, gcp, azure, aliyun, or harbor.'
                    type: string
                type: object
              sandboxNodePlacement:
                description: |-
                  SandboxNodePlacement configures the shared node placement used by
                  sandbox workloads and node-local sandbox services.
                properties:
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: |-
                      NodeSelector constrains sandbox workloads and node-local sandbox services
                      onto a specific node set.
                    type: object
                  tolerations:
                    description: |-
                      Tolerations allow sandbox workloads and node-local sandbox services to run
                      on tainted sandbox nodes.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              sandboxObservability:
                default:
                  type: disabled
                description: |-
                  SandboxObservability configures the region-local historical per-sandbox
                  observability backend and collection pipelines.
                properties:
                  audit:
                    description: |-
                      Audit configures the licensed centralized per-sandbox audit signal. Audit
                      collection and query remain disabled unless explicitly enabled.
                    properties:
                      deliveryMode:
                        default: durable_async
                        description: |-
                          DeliveryMode controls admission for non-mutating API requests, public
                          exposure requests, and flows observed by the ctld network runtime.
                          ClickHouse remains the only canonical audit store in both modes. Mutating
                          APIs always use canonical_sync.
                        enum:
                        - durable_async
                        - canonical_sync
                        type: string
                      deliveryPersistence:
                        description: |-
                          DeliveryPersistence configures the durable, non-canonical audit event
                          delivery buffer used while ClickHouse is unavailable. The current
                          implementation requires one cluster-gateway replica and a storage class
                          whose volume can be reattached after cross-node rescheduling.
                        properties:
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 1Gi
                            description: Size specifies the delivery buffer capacity.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: |-
                              StorageClass selects a durable Kubernetes storage class. Empty uses the
                              cluster default.
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: audit delivery persistence is immutable after creation
                          rule: self == oldSelf
                      enabled:
                        default: false
                        description: Enabled enables the enterprise sandbox_audit
                          feature.
                        type: boolean
                    type: object
                    x-kubernetes-validations:
                    - message: deliveryPersistence presence is immutable after creation
                      rule: has(self.deliveryPersistence) == has(oldSelf.deliveryPersistence)
                  backend:
                    allOf:
                    - enum:
                      - disabled
                      - clickhouse
                    - enum:
                      - disabled
                      - clickhouse
                    default: disabled
                    description: |-
                      Backend selects the historical query backend. New configurations should
                      use clickhouse with spec.clickHouse. The legacy type field is still
                      accepted for compatibility.
                    type: string
                  builtin:
                    description: Builtin configures the operator-managed ClickHouse
                      backend.
                    properties:
                      clickHouse:
                        default: {}
                        description: ClickHouse configures the operator-managed ClickHouse
                          instance.
                        properties:
                          database:
                            default: sandbox0_observability
                            type: string
                          eventsTable:
                            default: sandbox_audit_events
                            type: string
                          httpPort:
                            default: 8123
                            description: HTTPPort is the ClickHouse HTTP port.
                            format: int32
                            type: integer
                            x-kubernetes-validations:
                            - message: httpPort is immutable after creation
                              rule: self == oldSelf
                          image:
                            default: clickhouse/clickhouse-server:24.8
                            description: Image is the ClickHouse server image.
                            type: string
                          logsTable:
                            default: sandbox_logs
                            type: string
                          nativePort:
                            default: 9000
                            description: NativePort is the ClickHouse native TCP port.
                            format: int32
                            type: integer
                            x-kubernetes-validations:
                            - message: nativePort is immutable after creation
                              rule: self == oldSelf
                          persistence:
                            description: Persistence configures ClickHouse storage.
                            properties:
                              size:
                                anyOf:
                                - type: integer
                                - type: string
                                default: 20Gi
                                description: Size specifies the storage size
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              storageClass:
                                description: StorageClass specifies the storage class
                                  (empty for default)
                                type: string
                            type: object
                            x-kubernetes-validations:
                            - message: persistence is immutable after creation
                              rule: self == oldSelf
                          runtimeSamplesTable:
                            default: sandbox_runtime_samples
                            type: string
                          statefulResourcePolicy:
                            default: Retain
                            description: |-
                              StatefulResourcePolicy controls what happens to the builtin PVC and
                              generated credentials secret when the builtin backend is disabled or
                              replaced by an external backend.
                            enum:
                            - Retain
                            - Delete
                            type: string
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables the built-in backend when type
                          is builtin.
                        type: boolean
                    type: object
                    x-kubernetes-validations:
                    - message: clickHouse persistence presence is immutable after
                        creation
                      rule: has(self.clickHouse.persistence) == has(oldSelf.clickHouse.persistence)
                  enabled:
                    description: |-
                      Enabled enables the per-sandbox historical observability feature. When
                      omitted, the legacy type field controls enablement.
                    type: boolean
                  external:
                    description: External configures a user-owned ClickHouse backend.
                    properties:
                      clickHouse:
                        description: ClickHouse configures the external query backend.
                        properties:
                          connectTimeout:
                            default: 10s
                            type: string
                          database:
                            default: sandbox0_observability
                            type: string
                          dsnSecret:
                            description: DSNSecret references a secret key containing
                              the ClickHouse database/sql DSN.
                            properties:
                              key:
                                default: dsn
                                description: Key is the key in the secret.
                                type: string
                              name:
                                description: Name is the name of the secret.
                                type: string
                            type: object
                          eventsTable:
                            default: sandbox_audit_events
                            type: string
                          logsTable:
                            default: sandbox_logs
                            type: string
                          runtimeSamplesTable:
                            default: sandbox_runtime_samples
                            type: string
                          skipSchemaMigration:
                            description: SkipSchemaMigration disables cluster-gateway
                              ClickHouse CREATE/ALTER TABLE statements.
                            type: boolean
                        required:
                        - dsnSecret
                        type: object
                    required:
                    - clickHouse
                    type: object
                  ingest:
                    default: {}
                    description: Ingest configures bounded async producer-to-gateway
                      ingestion.
                    properties:
                      batchSize:
                        default: 100
                        description: BatchSize controls producer POST batch size.
                        type: integer
                      flushInterval:
                        default: 1s
                        description: FlushInterval controls producer batch flush cadence.
                        type: string
                      maxRetries:
                        default: 3
                        description: MaxRetries controls producer retry attempts before
                          dropping a batch.
                        type: integer
                      queueSize:
                        default: 1024
                        description: QueueSize bounds each producer queue.
                        type: integer
                      requestTimeout:
                        default: 2s
                        description: RequestTimeout bounds producer POST requests.
                        type: string
                      retryBackoff:
                        default: 100ms
                        description: RetryBackoff controls producer retry delay.
                        type: string
                    type: object
                  retention:
                    default: {}
                    description: Retention configures TTLs for the per-sandbox read
                      model.
                    properties:
                      auditDays:
                        default: 90
                        description: AuditDays controls retention for audit and lifecycle
                          events.
                        type: integer
                      logDays:
                        default: 7
                        description: LogDays controls retention for sandbox process
                          logs.
                        type: integer
                      runtimeSampleDays:
                        default: 30
                        description: RuntimeSampleDays controls retention for sandbox
                          runtime samples.
                        type: integer
                    type: object
                  type:
                    allOf:
                    - enum:
                      - disabled
                      - builtin
                      - external
                    - enum:
                      - disabled
                      - builtin
                      - external
                    default: disabled
                    description: |-
                      Type selects the historical query backend mode. Defaults to disabled so
                      ClickHouse is installed only when explicitly requested.
                    type: string
                type: object
              services:
                default: {}
                description: Services configures individual services
                properties:
                  clusterGateway:
                    default: {}
                    description: ClusterGateway configures the cluster-gateway service
                      (data plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains cluster-gateway specific configuration
                        properties:
                          allowedCallers:
                            default:
                            - regional-gateway
                            - scheduler
                            - cluster-gateway
                            items:
                              type: string
                            type: array
                          authMode:
                            default: internal
                            enum:
                            - internal
                            - public
                            - both
                            type: string
                          baseUrl:
                            default: http://localhost:8080
                            description: BaseURL sets the external base URL used by
                              browser-facing auth flows.
                            type: string
                          builtInAuth:
                            default: {}
                            description: BuiltInAuth configures local email/password
                              authentication.
                            properties:
                              adminOnly:
                                description: AdminOnly restricts built-in auth to
                                  admin accounts only.
                                type: boolean
                              allowRegistration:
                                description: AllowRegistration allows new users to
                                  register.
                                type: boolean
                              emailVerificationRequired:
                                description: EmailVerificationRequired requires email
                                  verification.
                                type: boolean
                              enabled:
                                default: true
                                description: Enabled enables built-in email/password
                                  authentication.
                                type: boolean
                            type: object
                          databaseMaxConns:
                            default: 30
                            type: integer
                          databaseMinConns:
                            default: 8
                            type: integer
                          defaultTeamName:
                            default: Personal Team
                            description: Identity and Teams
                            type: string
                          healthCheckPeriod:
                            default: 10s
                            type: string
                          httpPort:
                            default: 8443
                            type: integer
                          jwtAccessTokenTTL:
                            default: 15m
                            type: string
                          jwtIssuer:
                            description: JWTIssuer sets the JWT issuer for gateway-issued
                              tokens.
                            type: string
                          jwtPrivateKeyFile:
                            description: |-
                              JWTPrivateKeyFile points at a PEM-encoded Ed25519 private key file used to
                              sign user-facing JWTs.
                            type: string
                          jwtPrivateKeyPEM:
                            description: |-
                              JWTPrivateKeyPEM sets the PEM-encoded Ed25519 private key used to sign
                              user-facing JWTs.
                            type: string
                          jwtPublicKeyFile:
                            description: |-
                              JWTPublicKeyFile points at a PEM-encoded Ed25519 public key file used to
                              verify user-facing JWTs.
                            type: string
                          jwtPublicKeyPEM:
                            description: |-
                              JWTPublicKeyPEM sets the PEM-encoded Ed25519 public key used to verify
                              user-facing JWTs.
                            type: string
                          jwtRefreshTokenTTL:
                            default: 168h
                            type: string
                          logLevel:
                            default: info
                            type: string
                          oidcProviders:
                            description: OIDCProviders configures external identity
                              providers.
                            items:
                              description: OIDCProviderConfig configures an OIDC identity
                                provider.
                              properties:
                                autoProvision:
                                  type: boolean
                                clientId:
                                  type: string
                                clientSecret:
                                  type: string
                                discoveryUrl:
                                  type: string
                                enabled:
                                  type: boolean
                                externalAuthPortalUrl:
                                  type: string
                                id:
                                  type: string
                                name:
                                  type: string
                                scopes:
                                  default:
                                  - openid
                                  - email
                                  - profile
                                  items:
                                    type: string
                                  type: array
                                teamMapping:
                                  description: TeamMappingConfig configures automatic
                                    team mapping for OIDC users.
                                  properties:
                                    defaultRole:
                                      type: string
                                    defaultTeamId:
                                      type: string
                                    domain:
                                      type: string
                                  type: object
                                tokenEndpointAuthMethod:
                                  type: string
                              type: object
                            type: array
                          oidcStateCleanupInterval:
                            default: 5m
                            type: string
                          oidcStateTtl:
                            default: 10m
                            type: string
                          proxyTimeout:
                            default: 10s
                            type: string
                          rateLimitBurst:
                            default: 200
                            type: integer
                          rateLimitCleanupInterval:
                            default: 10m
                            type: string
                          rateLimitRps:
                            default: 100
                            description: Rate limiting
                            type: integer
                          schedulerPermissions:
                            default:
                            - '*:*'
                            items:
                              type: string
                            type: array
                          shutdownTimeout:
                            default: 30s
                            type: string
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: |-
                              Annotations declares Service metadata annotations.
                              Use this for cloud-provider specific load balancer configuration such as
                              ACM certificate attachment on AWS.
                            type: object
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: port must be within 30000-32767 when type is NodePort
                          rule: self.type != 'NodePort' || self.port == 0 || (self.port
                            >= 30000 && self.port <= 32767)
                    type: object
                  ctld:
                    default: {}
                    description: Ctld configures the ctld node-local service (data
                      plane)
                    properties:
                      containerdHostDataRoot:
                        default: /var/lib/containerd
                        description: |-
                          ContainerdHostDataRoot is the containerd data root path on the host.
                          Set this when nodes use a non-default containerd root such as a dedicated
                          sandbox worker data disk.
                        pattern: ^/.*
                        type: string
                      rootfsObjectCacheMaxAge:
                        description: |-
                          RootFSObjectCacheMaxAge evicts cache entries older than this age. Zero
                          disables age-based eviction.
                        type: string
                      rootfsObjectCacheMaxBytes:
                        default: 20Gi
                        description: |-
                          RootFSObjectCacheMaxBytes caps the ctld node-local rootfs object cache.
                          Set 0 to disable the cache.
                        type: string
                      rootfsObjectCacheMinFreeBytes:
                        description: |-
                          RootFSObjectCacheMinFreeBytes asks ctld GC to keep at least this much free
                          space on the cache filesystem.
                        type: string
                      rootfsObjectCacheSweepInterval:
                        description: |-
                          RootFSObjectCacheSweepInterval controls how often ctld sweeps the
                          node-local rootfs object cache.
                        type: string
                    type: object
                  globalGateway:
                    default: {}
                    description: GlobalGateway configures the global-gateway service
                      (global service layer)
                    properties:
                      config:
                        default: {}
                        description: Config contains global-gateway specific configuration
                        properties:
                          baseUrl:
                            default: http://localhost:8080
                            description: BaseURL sets the external base URL used by
                              browser-facing auth flows.
                            type: string
                          builtInAuth:
                            default: {}
                            description: BuiltInAuth configures local email/password
                              authentication.
                            properties:
                              adminOnly:
                                description: AdminOnly restricts built-in auth to
                                  admin accounts only.
                                type: boolean
                              allowRegistration:
                                description: AllowRegistration allows new users to
                                  register.
                                type: boolean
                              emailVerificationRequired:
                                description: EmailVerificationRequired requires email
                                  verification.
                                type: boolean
                              enabled:
                                default: true
                                description: Enabled enables built-in email/password
                                  authentication.
                                type: boolean
                            type: object
                          databaseMaxConns:
                            default: 30
                            type: integer
                          databaseMinConns:
                            default: 8
                            type: integer
                          databaseSchema:
                            default: global_gateway
                            type: string
                          defaultTeamName:
                            default: Personal Team
                            description: Identity and Teams
                            type: string
                          httpPort:
                            default: 8080
                            type: integer
                          jwtAccessTokenTTL:
                            default: 15m
                            type: string
                          jwtIssuer:
                            description: JWTIssuer sets the JWT issuer for gateway-issued
                              tokens.
                            type: string
                          jwtPrivateKeyFile:
                            description: |-
                              JWTPrivateKeyFile points at a PEM-encoded Ed25519 private key file used to
                              sign user-facing JWTs.
                            type: string
                          jwtPrivateKeyPEM:
                            description: |-
                              JWTPrivateKeyPEM sets the PEM-encoded Ed25519 private key used to sign
                              user-facing JWTs.
                            type: string
                          jwtPublicKeyFile:
                            description: |-
                              JWTPublicKeyFile points at a PEM-encoded Ed25519 public key file used to
                              verify user-facing JWTs.
                            type: string
                          jwtPublicKeyPEM:
                            description: |-
                              JWTPublicKeyPEM sets the PEM-encoded Ed25519 public key used to verify
                              user-facing JWTs.
                            type: string
                          jwtRefreshTokenTTL:
                            default: 168h
                            type: string
                          logLevel:
                            default: info
                            type: string
                          oidcProviders:
                            description: OIDCProviders configures external identity
                              providers.
                            items:
                              description: OIDCProviderConfig configures an OIDC identity
                                provider.
                              properties:
                                autoProvision:
                                  type: boolean
                                clientId:
                                  type: string
                                clientSecret:
                                  type: string
                                discoveryUrl:
                                  type: string
                                enabled:
                                  type: boolean
                                externalAuthPortalUrl:
                                  type: string
                                id:
                                  type: string
                                name:
                                  type: string
                                scopes:
                                  default:
                                  - openid
                                  - email
                                  - profile
                                  items:
                                    type: string
                                  type: array
                                teamMapping:
                                  description: TeamMappingConfig configures automatic
                                    team mapping for OIDC users.
                                  properties:
                                    defaultRole:
                                      type: string
                                    defaultTeamId:
                                      type: string
                                    domain:
                                      type: string
                                  type: object
                                tokenEndpointAuthMethod:
                                  type: string
                              type: object
                            type: array
                          oidcStateCleanupInterval:
                            default: 5m
                            type: string
                          oidcStateTtl:
                            default: 10m
                            type: string
                          rateLimitBurst:
                            default: 200
                            type: integer
                          rateLimitCleanupInterval:
                            default: 10m
                            type: string
                          rateLimitRps:
                            default: 100
                            description: Rate limiting
                            type: integer
                          serverIdleTimeout:
                            default: 120s
                            type: string
                          serverReadTimeout:
                            default: 30s
                            type: string
                          serverWriteTimeout:
                            default: 60s
                            type: string
                          shutdownTimeout:
                            default: 30s
                            type: string
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations specifies provider-specific ingress
                              annotations.
                            type: object
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          extraHosts:
                            description: ExtraHosts specifies additional ingress hosts
                              routed to the same backend.
                            items:
                              type: string
                            type: array
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tls:
                            description: |-
                              TLS specifies host groups and their backing TLS secrets.
                              When set, TLS takes precedence over TLSSecret.
                            items:
                              description: IngressTLSConfig defines one TLS certificate
                                binding for ingress hosts.
                              properties:
                                hosts:
                                  description: Hosts specifies the hosts covered by
                                    this TLS secret.
                                  items:
                                    type: string
                                  type: array
                                secretName:
                                  description: SecretName specifies the Kubernetes
                                    TLS secret name.
                                  type: string
                              type: object
                            type: array
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: |-
                              Annotations declares Service metadata annotations.
                              Use this for cloud-provider specific load balancer configuration such as
                              ACM certificate attachment on AWS.
                            type: object
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: port must be within 30000-32767 when type is NodePort
                          rule: self.type != 'NodePort' || self.port == 0 || (self.port
                            >= 30000 && self.port <= 32767)
                    type: object
                  manager:
                    default: {}
                    description: Manager configures the manager service (data plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains manager specific configuration
                        properties:
                          allowColdStartWithoutReadyDataPlane:
                            description: |-
                              AllowColdStartWithoutReadyDataPlane lets cold claims create Pending pods
                              when no sandbox data-plane-ready nodes exist yet. This is required for
                              node autoscaler scale-from-zero deployments.
                            type: boolean
                          autoscaler:
                            default: {}
                            description: AutoscalerConfig defines manager autoscaler
                              settings.
                            properties:
                              maxScaleStep:
                                default: 10
                                format: int32
                                type: integer
                              minIdleBuffer:
                                default: 2
                                format: int32
                                type: integer
                              minScaleInterval:
                                default: 100ms
                                type: string
                              noTrafficScaleDownAfter:
                                default: 10m
                                type: string
                              scaleDownPercent:
                                default: "0.1"
                                type: string
                              scaleUpFactor:
                                default: "1.5"
                                type: string
                              targetIdleRatio:
                                default: "0.2"
                                type: string
                            type: object
                          cleanupInterval:
                            default: 60s
                            type: string
                          databaseMaxConns:
                            default: 10
                            format: int32
                            type: integer
                          databaseMinConns:
                            default: 2
                            format: int32
                            type: integer
                          defaultSandboxTtl:
                            default: 0s
                            type: string
                          defaultTeamQuotas:
                            description: |-
                              DefaultTeamQuotas configures region-wide fallback team quota limits.
                              Team-specific quota rows in the database override these defaults.
                            items:
                              description: |-
                                TeamQuotaLimitConfig configures a fallback quota limit for teams without a
                                database override for the same dimension.
                              properties:
                                dimension:
                                  enum:
                                  - active_sandboxes
                                  - cpu_millicpu
                                  - memory_mib
                                  - volume_storage_gb
                                  - snapshot_storage_gb
                                  - egress
                                  - ingress
                                  type: string
                                limitValue:
                                  format: int64
                                  minimum: 0
                                  type: integer
                              required:
                              - dimension
                              - limitValue
                              type: object
                            maxItems: 7
                            type: array
                            x-kubernetes-list-map-keys:
                            - dimension
                            x-kubernetes-list-type: map
                          egressAuthDefaultResolveTtl:
                            default: 5m
                            description: |-
                              EgressAuthDefaultResolveTTL controls the default lifetime of resolved
                              egress auth material cached by the ctld network runtime when a binding does
                              not set cachePolicy.ttl.
                            type: string
                          httpPort:
                            default: 8080
                            type: integer
                          k8sClientBurst:
                            default: 100
                            type: integer
                          k8sClientQps:
                            default: 50
                            type: integer
                          kubeConfig:
                            type: string
                          leaderElection:
                            default: true
                            type: boolean
                          logLevel:
                            default: info
                            type: string
                          metricsPort:
                            default: 9090
                            type: integer
                          netdPolicyApplyPollInterval:
                            default: 500ms
                            type: string
                          netdPolicyApplyTimeout:
                            default: 30s
                            type: string
                          pauseMemoryBufferRatio:
                            default: "1.1"
                            type: string
                          pauseMinCpu:
                            default: 10m
                            type: string
                          pauseMinMemoryLimit:
                            default: 32Mi
                            type: string
                          pauseMinMemoryRequest:
                            default: 10Mi
                            type: string
                          procdBinImageRef:
                            description: ProcdBinImageRef overrides the OCI image
                              used for the procd binary image volume.
                            type: string
                          procdClientTimeout:
                            default: 30s
                            type: string
                          procdConfig:
                            default: {}
                            description: ProcdConfig defines user-facing procd settings
                              managed by manager.
                            properties:
                              contextCleanupInterval:
                                default: 30s
                                type: string
                              contextFinishedTtl:
                                default: 0s
                                type: string
                              contextIdleTimeout:
                                default: 0s
                                type: string
                              contextMaxLifetime:
                                default: 0s
                                type: string
                              httpPort:
                                default: 49983
                                type: integer
                              logLevel:
                                default: info
                                type: string
                              rootPath:
                                default: /workspace
                                type: string
                              webhookBaseBackoff:
                                default: 500ms
                                type: string
                              webhookMaxRetries:
                                default: 3
                                type: integer
                              webhookOutboxDir:
                                type: string
                              webhookQueueSize:
                                default: 256
                                type: integer
                              webhookRequestTimeout:
                                default: 5s
                                type: string
                            type: object
                          procdInitTimeout:
                            default: 6s
                            type: string
                          resyncPeriod:
                            default: 30s
                            type: string
                          sandboxMaxMemory:
                            default: 32Gi
                            description: SandboxMaxMemory is the maximum memory limit
                              accepted for a single sandbox.
                            type: string
                          sandboxRuntimeClassName:
                            type: string
                          shutdownTimeout:
                            default: 30s
                            type: string
                          teamTemplateMemoryPerCpu:
                            default: 4Gi
                            type: string
                          webhookCertPath:
                            default: /tmp/k8s-webhook-server/serving-certs/tls.crt
                            type: string
                          webhookKeyPath:
                            default: /tmp/k8s-webhook-server/serving-certs/tls.key
                            type: string
                          webhookPort:
                            default: 9443
                            type: integer
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: |-
                              Annotations declares Service metadata annotations.
                              Use this for cloud-provider specific load balancer configuration such as
                              ACM certificate attachment on AWS.
                            type: object
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: port must be within 30000-32767 when type is NodePort
                          rule: self.type != 'NodePort' || self.port == 0 || (self.port
                            >= 30000 && self.port <= 32767)
                    type: object
                  regionalGateway:
                    default: {}
                    description: RegionalGateway configures the regional-gateway service
                      (control plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains regional-gateway specific configuration
                        properties:
                          authMode:
                            default: self_hosted
                            enum:
                            - self_hosted
                            - federated_global
                            type: string
                          baseUrl:
                            default: http://localhost:8080
                            description: BaseURL sets the external base URL used by
                              browser-facing auth flows.
                            type: string
                          builtInAuth:
                            default: {}
                            description: BuiltInAuth configures local email/password
                              authentication.
                            properties:
                              adminOnly:
                                description: AdminOnly restricts built-in auth to
                                  admin accounts only.
                                type: boolean
                              allowRegistration:
                                description: AllowRegistration allows new users to
                                  register.
                                type: boolean
                              emailVerificationRequired:
                                description: EmailVerificationRequired requires email
                                  verification.
                                type: boolean
                              enabled:
                                default: true
                                description: Enabled enables built-in email/password
                                  authentication.
                                type: boolean
                            type: object
                          clusterCacheTtl:
                            default: 30s
                            type: string
                          databaseMaxConns:
                            default: 30
                            type: integer
                          databaseMinConns:
                            default: 8
                            type: integer
                          defaultTeamName:
                            default: Personal Team
                            description: Identity and Teams
                            type: string
                          edition:
                            default: self-hosted
                            type: string
                          httpPort:
                            default: 8080
                            type: integer
                          internalAuthCaller:
                            default: regional-gateway
                            type: string
                          internalAuthTtl:
                            default: 30s
                            type: string
                          jwtAccessTokenTTL:
                            default: 15m
                            type: string
                          jwtIssuer:
                            description: JWTIssuer sets the JWT issuer for gateway-issued
                              tokens.
                            type: string
                          jwtPrivateKeyFile:
                            description: |-
                              JWTPrivateKeyFile points at a PEM-encoded Ed25519 private key file used to
                              sign user-facing JWTs.
                            type: string
                          jwtPrivateKeyPEM:
                            description: |-
                              JWTPrivateKeyPEM sets the PEM-encoded Ed25519 private key used to sign
                              user-facing JWTs.
                            type: string
                          jwtPublicKeyFile:
                            description: |-
                              JWTPublicKeyFile points at a PEM-encoded Ed25519 public key file used to
                              verify user-facing JWTs.
                            type: string
                          jwtPublicKeyPEM:
                            description: |-
                              JWTPublicKeyPEM sets the PEM-encoded Ed25519 public key used to verify
                              user-facing JWTs.
                            type: string
                          jwtRefreshTokenTTL:
                            default: 168h
                            type: string
                          logLevel:
                            default: info
                            type: string
                          oidcProviders:
                            description: OIDCProviders configures external identity
                              providers.
                            items:
                              description: OIDCProviderConfig configures an OIDC identity
                                provider.
                              properties:
                                autoProvision:
                                  type: boolean
                                clientId:
                                  type: string
                                clientSecret:
                                  type: string
                                discoveryUrl:
                                  type: string
                                enabled:
                                  type: boolean
                                externalAuthPortalUrl:
                                  type: string
                                id:
                                  type: string
                                name:
                                  type: string
                                scopes:
                                  default:
                                  - openid
                                  - email
                                  - profile
                                  items:
                                    type: string
                                  type: array
                                teamMapping:
                                  description: TeamMappingConfig configures automatic
                                    team mapping for OIDC users.
                                  properties:
                                    defaultRole:
                                      type: string
                                    defaultTeamId:
                                      type: string
                                    domain:
                                      type: string
                                  type: object
                                tokenEndpointAuthMethod:
                                  type: string
                              type: object
                            type: array
                          oidcStateCleanupInterval:
                            default: 5m
                            type: string
                          oidcStateTtl:
                            default: 10m
                            type: string
                          proxyTimeout:
                            default: 10s
                            type: string
                          rateLimitBurst:
                            default: 200
                            type: integer
                          rateLimitCleanupInterval:
                            default: 10m
                            type: string
                          rateLimitRps:
                            default: 100
                            description: Rate limiting
                            type: integer
                          schedulerEnabled:
                            type: boolean
                          schedulerUrl:
                            type: string
                          serverIdleTimeout:
                            default: 120s
                            type: string
                          serverReadTimeout:
                            default: 30s
                            type: string
                          serverWriteTimeout:
                            default: 60s
                            type: string
                          shutdownTimeout:
                            default: 30s
                            type: string
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      ingress:
                        description: Ingress configures ingress settings
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations specifies provider-specific ingress
                              annotations.
                            type: object
                          className:
                            description: ClassName specifies the ingress class name
                            type: string
                          enabled:
                            default: false
                            description: Enabled enables ingress
                            type: boolean
                          extraHosts:
                            description: ExtraHosts specifies additional ingress hosts
                              routed to the same backend.
                            items:
                              type: string
                            type: array
                          host:
                            description: Host specifies the ingress host
                            type: string
                          tls:
                            description: |-
                              TLS specifies host groups and their backing TLS secrets.
                              When set, TLS takes precedence over TLSSecret.
                            items:
                              description: IngressTLSConfig defines one TLS certificate
                                binding for ingress hosts.
                              properties:
                                hosts:
                                  description: Hosts specifies the hosts covered by
                                    this TLS secret.
                                  items:
                                    type: string
                                  type: array
                                secretName:
                                  description: SecretName specifies the Kubernetes
                                    TLS secret name.
                                  type: string
                              type: object
                            type: array
                          tlsSecret:
                            description: TLSSecret specifies the TLS secret name
                            type: string
                        type: object
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: |-
                              Annotations declares Service metadata annotations.
                              Use this for cloud-provider specific load balancer configuration such as
                              ACM certificate attachment on AWS.
                            type: object
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: port must be within 30000-32767 when type is NodePort
                          rule: self.type != 'NodePort' || self.port == 0 || (self.port
                            >= 30000 && self.port <= 32767)
                    type: object
                  scheduler:
                    description: Scheduler configures the scheduler service (control
                      plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains scheduler specific configuration
                        properties:
                          databasePool:
                            description: DatabasePoolConfig defines scheduler database
                              pool settings.
                            properties:
                              maxConnIdleTime:
                                default: 5m
                                type: string
                              maxConnLifetime:
                                default: 30m
                                type: string
                              maxConns:
                                default: 10
                                format: int32
                                type: integer
                              minConns:
                                default: 2
                                format: int32
                                type: integer
                            type: object
                          httpPort:
                            default: 8080
                            type: integer
                          idleTimeout:
                            default: 120s
                            type: string
                          logLevel:
                            default: info
                            type: string
                          podsPerNode:
                            default: 50
                            type: integer
                          proxyTimeout:
                            default: 10s
                            type: string
                          readTimeout:
                            default: 30s
                            type: string
                          reconcileInterval:
                            default: 30s
                            type: string
                          shutdownTimeout:
                            default: 30s
                            type: string
                          writeTimeout:
                            default: 60s
                            type: string
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: |-
                              Annotations declares Service metadata annotations.
                              Use this for cloud-provider specific load balancer configuration such as
                              ACM certificate attachment on AWS.
                            type: object
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: port must be within 30000-32767 when type is NodePort
                          rule: self.type != 'NodePort' || self.port == 0 || (self.port
                            >= 30000 && self.port <= 32767)
                    type: object
                  sshGateway:
                    default: {}
                    description: SSHGateway configures the ssh-gateway service (control
                      plane)
                    properties:
                      config:
                        default: {}
                        description: Config contains ssh-gateway specific configuration.
                        properties:
                          databaseMaxConns:
                            default: 30
                            type: integer
                          databaseMinConns:
                            default: 8
                            type: integer
                          internalAuthCaller:
                            default: ssh-gateway
                            type: string
                          internalAuthTtl:
                            default: 30s
                            type: string
                          logLevel:
                            default: info
                            type: string
                          resumePollInterval:
                            default: 500ms
                            type: string
                          resumeTimeout:
                            default: 30s
                            type: string
                          shutdownTimeout:
                            default: 30s
                            type: string
                          sshPort:
                            default: 2222
                            type: integer
                        type: object
                      enabled:
                        default: false
                        description: Enabled enables or disables the service
                        type: boolean
                      endpointPort:
                        description: |-
                          EndpointPort optionally overrides the SSH port advertised by regional and
                          cluster gateways. Use this when an external load balancer listens on a
                          different port than the Kubernetes Service or NodePort.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      replicas:
                        default: 1
                        description: Replicas specifies the number of replicas
                        format: int32
                        type: integer
                      resources:
                        description: Resources specifies resource requirements
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: Service configures the Kubernetes service
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: |-
                              Annotations declares Service metadata annotations.
                              Use this for cloud-provider specific load balancer configuration such as
                              ACM certificate attachment on AWS.
                            type: object
                          port:
                            default: 80
                            description: Port specifies the service port
                            format: int32
                            type: integer
                          type:
                            default: ClusterIP
                            description: Type specifies the service type
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: port must be within 30000-32767 when type is NodePort
                          rule: self.type != 'NodePort' || self.port == 0 || (self.port
                            >= 30000 && self.port <= 32767)
                    type: object
                type: object
              storage:
                description: Storage configures the storage backend (S0FS S3 backend)
                properties:
                  builtin:
                    default: {}
                    description: Builtin configures the built-in RustFS storage
                    properties:
                      bucket:
                        default: sandbox0
                        description: Bucket specifies the default bucket name for
                          builtin storage
                        type: string
                      consoleEnabled:
                        default: true
                        description: ConsoleEnabled enables the RustFS console
                        type: boolean
                      consolePort:
                        default: 9001
                        description: ConsolePort specifies the RustFS console port
                        format: int32
                        type: integer
                      credentials:
                        description: Credentials configures access credentials (auto-generated
                          if not specified)
                        properties:
                          accessKey:
                            description: AccessKey is the access key
                            type: string
                          secretKey:
                            description: SecretKey is the secret key
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: credentials are immutable after creation
                          rule: self == oldSelf
                      enabled:
                        default: true
                        description: Enabled enables the built-in storage
                        type: boolean
                      image:
                        default: rustfs/rustfs:1.0.0-alpha.79
                        description: Image specifies the RustFS image for builtin
                          storage
                        type: string
                      obsEnvironment:
                        default: develop
                        description: ObsEnvironment specifies the RustFS environment
                          label
                        type: string
                      obsLogDirectory:
                        default: /data/logs
                        description: ObsLogDirectory specifies the RustFS log directory
                        type: string
                      obsLoggerLevel:
                        default: debug
                        description: ObsLoggerLevel specifies the RustFS log level
                        type: string
                      persistence:
                        description: Persistence configures storage persistence
                        properties:
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            default: 20Gi
                            description: Size specifies the storage size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: StorageClass specifies the storage class
                              (empty for default)
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: persistence is immutable after creation
                          rule: self == oldSelf
                      port:
                        default: 9000
                        description: Port specifies the RustFS API port
                        format: int32
                        type: integer
                      region:
                        default: us-east-1
                        description: Region specifies the default region for builtin
                          storage
                        type: string
                      statefulResourcePolicy:
                        default: Retain
                        description: |-
                          StatefulResourcePolicy controls what happens to the builtin PVC and
                          generated credentials secret when the builtin storage is disabled or
                          replaced by an external storage backend.
                        enum:
                        - Retain
                        - Delete
                        type: string
                      volumes:
                        default: /data
                        description: Volumes specifies the RustFS data path
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: persistence presence is immutable after creation
                      rule: has(self.persistence) == has(oldSelf.persistence)
                    - message: credentials presence is immutable after creation
                      rule: has(self.credentials) == has(oldSelf.credentials)
                  gcs:
                    description: GCS configures Google Cloud Storage using native
                      GCS credentials.
                    properties:
                      bucket:
                        description: Bucket specifies the GCS bucket name.
                        type: string
                      workloadIdentityServiceAccountEmail:
                        description: |-
                          WorkloadIdentityServiceAccountEmail is the Google service account email
                          used by storage clients through GKE Workload Identity.
                        type: string
                    required:
                    - bucket
                    type: object
                  oss:
                    description: OSS configures Aliyun OSS storage
                    properties:
                      bucket:
                        description: Bucket specifies the OSS bucket name
                        type: string
                      credentialsSecret:
                        description: CredentialsSecret references the secret containing
                          Aliyun credentials
                        properties:
                          accessKeyKey:
                            default: accessKeyId
                            description: AccessKeyKey is the key for access key ID
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                          secretKeyKey:
                            default: accessKeySecret
                            description: SecretKeyKey is the key for access key secret
                            type: string
                        required:
                        - name
                        type: object
                      endpoint:
                        description: Endpoint specifies the OSS endpoint
                        type: string
                      region:
                        description: Region specifies the Aliyun region
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    - endpoint
                    - region
                    type: object
                  runtime:
                    description: Runtime configures the sandbox volume runtime hosted
                      by manager and ctld.
                    properties:
                      auditFile:
                        default: /var/log/storage-proxy/audit.log
                        type: string
                      auditLog:
                        default: true
                        type: boolean
                      cacheDir:
                        default: /var/lib/storage-proxy/cache
                        type: string
                      cacheSizeLimit:
                        default: 20Gi
                        type: string
                      cleanupInterval:
                        default: 60s
                        type: string
                      databaseMaxConns:
                        default: 30
                        type: integer
                      databaseMinConns:
                        default: 5
                        type: integer
                      databaseSchema:
                        default: storage_proxy
                        type: string
                      directVolumeFileIdleTTL:
                        default: 30s
                        type: string
                      filesystemAttrTimeout:
                        default: 1s
                        type: string
                      filesystemBlockSize:
                        default: 4096
                        type: integer
                      filesystemCompression:
                        default: lz4
                        type: string
                      filesystemDirEntryTimeout:
                        default: 1s
                        type: string
                      filesystemEntryTimeout:
                        default: 1s
                        type: string
                      filesystemMaxUpload:
                        default: 20
                        type: integer
                      filesystemMetaRetries:
                        default: 10
                        type: integer
                      filesystemName:
                        default: sandbox0
                        type: string
                      filesystemTrashDays:
                        default: 1
                        type: integer
                      flushTimeout:
                        default: 30s
                        type: string
                      heartbeatInterval:
                        default: 5s
                        type: string
                      heartbeatTimeout:
                        default: 15
                        type: integer
                      httpAddr:
                        default: 0.0.0.0
                        type: string
                      httpIdleTimeout:
                        default: 60s
                        type: string
                      httpPort:
                        default: 8081
                        type: integer
                      httpReadTimeout:
                        default: 15s
                        type: string
                      httpWriteTimeout:
                        default: 15s
                        type: string
                      kubeconfigPath:
                        type: string
                      logLevel:
                        default: info
                        type: string
                      logSizeLimit:
                        default: 1Gi
                        type: string
                      maxBytesPerSecond:
                        default: 1073741824
                        format: int64
                        type: integer
                      maxOpsPerSecond:
                        default: 10000
                        type: integer
                      metricsEnabled:
                        default: true
                        type: boolean
                      metricsPort:
                        default: 9090
                        type: integer
                      objectEncryptionAlgo:
                        default: aes256gcm-rsa
                        type: string
                      objectEncryptionEnabled:
                        default: true
                        type: boolean
                      objectEncryptionPassphrase:
                        type: string
                      restoreRemountTimeout:
                        default: 30s
                        type: string
                      s0fsCompactionInterval:
                        default: 1m
                        type: string
                      s0fsCompactionMinDeadRatio:
                        default: "0.5"
                        type: string
                      s0fsCompactionMinReclaimSize:
                        default: 1Mi
                        type: string
                      s0fsSegmentTargetSize:
                        default: 4Mi
                        type: string
                      volumePortalCacheSizeLimit:
                        default: 20Gi
                        type: string
                      volumePortalRootMinFree:
                        default: 5Gi
                        type: string
                      watchEventQueueSize:
                        default: 256
                        type: integer
                      watchEventsEnabled:
                        default: true
                        type: boolean
                    type: object
                  s3:
                    description: S3 configures S3 or S3-compatible storage
                    properties:
                      bucket:
                        description: Bucket specifies the S3 bucket name
                        type: string
                      credentialsSecret:
                        description: CredentialsSecret references the secret containing
                          AWS credentials
                        properties:
                          accessKeyKey:
                            default: accessKeyId
                            description: AccessKeyKey is the key for access key ID
                            type: string
                          name:
                            description: Name is the name of the secret
                            type: string
                          secretKeyKey:
                            default: secretAccessKey
                            description: SecretKeyKey is the key for secret access
                              key
                            type: string
                        required:
                        - name
                        type: object
                      endpoint:
                        description: Endpoint specifies the S3 endpoint (optional
                          for AWS)
                        type: string
                      region:
                        description: Region specifies the AWS region
                        type: string
                      sessionTokenKey:
                        description: SessionTokenKey is the key for session token
                          in the secret (optional)
                        type: string
                    required:
                    - bucket
                    - credentialsSecret
                    - region
                    type: object
                  type:
                    default: builtin
                    description: 'Type specifies the storage type: builtin, s3, oss,
                      or gcs.'
                    enum:
                    - builtin
                    - s3
                    - oss
                    - gcs
                    type: string
                type: object
            type: object
          status:
            description: Sandbox0InfraStatus defines the observed state of Sandbox0Infra
            properties:
              cluster:
                description: Cluster contains cluster registration status
                properties:
                  id:
                    description: ID is the cluster identifier
                    type: string
                  registered:
                    description: Registered indicates if the cluster is registered
                    type: boolean
                  registeredAt:
                    description: RegisteredAt is the registration timestamp
                    format: date-time
                    type: string
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the infrastructure's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endpoints:
                description: Endpoints contains the service endpoints
                properties:
                  clusterGateway:
                    description: ClusterGateway is the cluster-gateway URL
                    type: string
                  globalGateway:
                    description: GlobalGateway is the global-gateway URL
                    type: string
                  regionalGateway:
                    description: RegionalGateway is the external regional-gateway
                      URL
                    type: string
                  regionalGatewayInternal:
                    description: RegionalGatewayInternal is the internal regional-gateway
                      URL
                    type: string
                type: object
              internalAuth:
                description: InternalAuth contains internal authentication status
                properties:
                  controlPlanePublicKey:
                    description: ControlPlanePublicKey references the control plane
                      public key
                    properties:
                      secretKey:
                        description: SecretKey is the key in the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - secretKey
                    - secretName
                    type: object
                  dataPlanePublicKey:
                    description: DataPlanePublicKey references the data plane public
                      key
                    properties:
                      secretKey:
                        description: SecretKey is the key in the secret
                        type: string
                      secretName:
                        description: SecretName is the name of the secret
                        type: string
                    required:
                    - secretKey
                    - secretName
                    type: object
                type: object
              lastMessage:
                description: LastMessage is the most recent status message (success
                  or error)
                type: string
              lastOperation:
                description: LastOperation contains the last operation information
                properties:
                  completedAt:
                    description: CompletedAt is when the operation completed
                    format: date-time
                    type: string
                  error:
                    description: Error contains error message if failed
                    type: string
                  startedAt:
                    description: StartedAt is when the operation started
                    format: date-time
                    type: string
                  status:
                    description: Status is the operation status
                    type: string
                  type:
                    description: Type is the operation type
                    type: string
                type: object
              observedVersion:
                description: ObservedVersion is the last observed version
                type: string
              phase:
                description: Phase represents the current phase of the infrastructure
                enum:
                - Installing
                - Ready
                - Degraded
                - Failed
                - Upgrading
                type: string
              progress:
                description: Progress shows readiness progress in "ready/total" format
                type: string
              retainedResources:
                description: |-
                  RetainedResources lists builtin stateful resources intentionally retained
                  after builtin-to-external or enabled-to-disabled transitions.
                items:
                  description: |-
                    RetainedResourceStatus describes a builtin stateful resource retained by
                    policy after a lifecycle transition.
                  properties:
                    component:
                      description: Component is the logical component name, for example
                        database or storage.
                      type: string
                    kind:
                      description: Kind is the Kubernetes resource kind.
                      type: string
                    name:
                      description: Name is the Kubernetes resource name.
                      type: string
                    policy:
                      description: Policy is the stateful resource policy that caused
                        the resource to be retained.
                      enum:
                      - Retain
                      - Delete
                      type: string
                    reason:
                      description: Reason explains why the resource is retained.
                      type: string
                  required:
                  - component
                  - kind
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
