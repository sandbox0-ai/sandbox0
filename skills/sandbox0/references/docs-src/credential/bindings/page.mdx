# Credential Bindings

Bindings connect a reusable credential source to a stable local reference inside `SandboxNetworkPolicy`.

There is no standalone bindings API. You define bindings inside `network.credentialBindings` on a template, at claim time, or when updating sandbox network policy at runtime.

## Binding Fields

| Field | Required | Description |
|-------|----------|-------------|
| `ref` | Yes | Stable local name matched by `credentialRules[*].credentialRef` |
| `sourceRef` | Yes | Region-scoped credential source reference to resolve |
| `projection` | Yes | How the source is shaped for runtime injection |
| `cachePolicy.ttl` | No | Optional cache lifetime duration string such as `5m` or `1h` for resolved material |

## Projection Example

This binding projects a stored token into an `Authorization` header:

```yaml
credentialBindings:
    - ref: gh-token
      sourceRef: github-source
      projection:
          type: http_headers
          httpHeaders:
              headers:
                  - name: Authorization
                    valueTemplate: "Bearer {{token}}"
```

In this example, `{{token}}` refers to the `token` key inside a `static_headers` source's `values` map. If the referenced key is missing, header rendering fails.

## Configure Bindings On A Sandbox

The same `SandboxNetworkPolicy` shape is used for:

- template `spec.network`
- sandbox claim `config.network`
- sandbox runtime update `PUT /api/v1/sandboxes/{id}/network`

The SDKs can send this shape directly at claim time. `s0` now supports the same claim-time shape through `sandbox create --config-file`.

<Tabs
  tabs={[
    {
      label: "Go",
      language: "go",
      code: `sandbox, err := client.ClaimSandbox(ctx, "default",
    sandbox0.WithSandboxNetworkPolicy(apispec.SandboxNetworkPolicy{
        Mode: apispec.SandboxNetworkPolicyModeBlockAll,
        Egress: apispec.NewOptNetworkEgressPolicy(apispec.NetworkEgressPolicy{
            TrafficRules: []apispec.TrafficRule{
                {
                    Name: apispec.NewOptString("allow-github-api"),
                    Action: apispec.TrafficRuleActionAllow,
                    Domains: []string{"api.github.com"},
                    Ports: []apispec.PortSpec{
                        {
                            Port: 443,
                            Protocol: apispec.NewOptString("tcp"),
                        },
                    },
                },
            },
        }),
        CredentialBindings: []apispec.CredentialBinding{
            {
                Ref: "gh-token",
                SourceRef: "github-source",
                Projection: apispec.ProjectionSpec{
                    Type: apispec.CredentialProjectionTypeHTTPHeaders,
                    HttpHeaders: apispec.NewOptHTTPHeadersProjection(apispec.HTTPHeadersProjection{
                        Headers: []apispec.ProjectedHeader{
                            {
                                Name: "Authorization",
                                ValueTemplate: "Bearer {{token}}",
                            },
                        },
                    }),
                },
            },
        },
    }),
)`
    },
    {
      label: "CLI",
      language: "bash",
      code: `cat <<'EOF' > sandbox-config.yaml
network:
    mode: block-all
    egress:
        trafficRules:
            - name: allow-github-api
              action: allow
              domains: [api.github.com]
              ports:
                  - port: 443
                    protocol: tcp
    credentialBindings:
        - ref: gh-token
          sourceRef: github-source
          projection:
              type: http_headers
              httpHeaders:
                  headers:
                      - name: Authorization
                        valueTemplate: "Bearer {{token}}"
EOF

s0 sandbox create -t default --config-file sandbox-config.yaml`
    },
    {
      label: "Python",
      language: "python",
      code: `from sandbox0.apispec.models.sandbox_config import SandboxConfig

sandbox = client.claim_sandbox(
    template="default",
    config=SandboxConfig.from_dict({
        "network": {
            "mode": "block-all",
            "egress": {
                "trafficRules": [
                    {
                        "name": "allow-github-api",
                        "action": "allow",
                        "domains": ["api.github.com"],
                        "ports": [{"port": 443, "protocol": "tcp"}],
                    },
                ],
            },
            "credentialBindings": [
                {
                    "ref": "gh-token",
                    "sourceRef": "github-source",
                    "projection": {
                        "type": "http_headers",
                        "httpHeaders": {
                            "headers": [
                                {
                                    "name": "Authorization",
                                    "valueTemplate": "Bearer {{token}}",
                                },
                            ],
                        },
                    },
                },
            ],
        },
    }),
)`
    },
    {
      label: "TypeScript",
      language: "typescript",
      code: `const sandbox = await client.sandboxes.claim('default', {
    network: {
        mode: 'block-all',
        egress: {
            trafficRules: [
                {
                    name: 'allow-github-api',
                    action: 'allow',
                    domains: ['api.github.com'],
                    ports: [{ port: 443, protocol: 'tcp' }],
                },
            ],
        },
        credentialBindings: [
            {
                ref: 'gh-token',
                sourceRef: 'github-source',
                projection: {
                    type: 'http_headers',
                    httpHeaders: {
                        headers: [
                            {
                                name: 'Authorization',
                                valueTemplate: 'Bearer {{token}}',
                            },
                        ],
                    },
                },
            },
        ],
    },
});`
    }
  ]}
/>

<Callout variant="warning">
Legacy sibling fields such as `credential_bindings` and top-level template `credentialBindings` are deprecated. New code should put bindings under `network.credentialBindings`.
</Callout>

<Callout variant="info">
`s0 sandbox create` supports claim-time bindings through `--config-file`. It still does not support inline `--credential-binding` or `--traffic-rule` flags on the create command.
</Callout>

## Next Step

After a binding exists, add destination matching with [`credentialRules`](/docs/credential/egress-auth).
