# Task Routing

Use this framework to route agent work:

- implementation questions: inspect source code first
- API behavior questions: inspect `pkg/apispec/openapi.yaml`
- CLI behavior questions: inspect `s0` source and command help
- broader product guidance: consult bundled docs after code and spec

This file intentionally provides only the routing framework, not concrete task
content.
