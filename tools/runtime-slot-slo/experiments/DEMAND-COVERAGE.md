# D-DEMAND-COVERAGE: ordinary roots do not enter fragment scheduling

Date: 2026-09-13. Local source/geometry audit, not another runtime comparison.
Evidence root: `/tmp/sandbox0-demand-coverage.shQ9Tb`.

## Result and decision correction

The current ordinary Node and Coding immutable roots contain **zero partial
data views**. The candidate requires an uncached partial view before scheduling
fragments. It cannot directly parallelize full views in these immutable roots.

| Exact ordinary artifact | Leaf pages | Full data views | Partial data views |
| --- | ---: | ---: | ---: |
| Node `2b5208c9...` | 5 | 4341 | 0 |
| Coding `21a2f3ba...` | 79 | 80781 | 0 |

D-DEMAND-LIVE's Coding combined median reduction of about314ms remains an
observation; do not erase it. But it is **not evidence that Coding's own RootFS
used fragment parallelism**. The same mixed trial ran history00/history16 with
shared cache and source admission. Changed work in those other Readers could
affect ordinary Readers indirectly. Cross-boot/provider variation and binary
layout/overhead effects also remain unisolated. This proves neither a specific
indirect cause nor that the candidate has no effect in the mixed workload.

Retain the history-claim signal and prior encrypted-component qualification.
Do not promote history-only scheduling as a direct ordinary-Coding fix, or
repeat an unchanged mixed ABBA to strengthen that attribution.

## Verification and limits

- Reverify173 sealed D-DEMAND-LIVE artifacts, including runtime builds/overlay.
  Bind both full profiles by SHA256 and exact descriptor/artifact equality to
  that campaign's expected artifacts and D-MAPPING-TRACE's authority. The
  profile collector walks every authenticated leaf and rejects parent mismatch.
  Its old `inline` helper name does not change these ordinary format2 roots.
- Reverify selected historical inputs and all four raw traces. All14373 reads
  bind to16 original identities, exact lease branches, literal `node -v` and32
  claim/first-command groups. None intersects a partial immutable base view.
  Historical instrumentation/times are not current-pin SLO evidence.
- Actual candidate Go tests check all85122 views with nil cache/source; each
  declines before I/O and preserves output. An AST check binds the sole Reader
  dispatch site to `!fullDataRange(entry)`. Positive controlled-history and
  outside-demand tests still pass. Three race repetitions:30pass events,0skips.
- No current live invocation counter is fabricated for the latest64 samples.
  This is not a scheduler replay, payload verification or full live writer
  inspection. Current dirty branches and history templates are outside the two
  ordinary-profile inventories. Existing positive history evidence is retained.

## Next measurement boundary

Mixed follow-up must attribute batch admission, source loads/bytes and exact
generation identity per Reader, separately for ordinary and history roots.
Aggregate ctld/NBD counters cannot identify which root took the new path. Charge
shared-flight work once, keep cold/cached-new identities and literal first node-v.

Ordinary full-view payload dependency cost remains a separate target. Reuse
existing full-demand/provider/host evidence; do not restart closed parameter or
layout sweeps, introduce command-specific profiles, or prewarm tenant RootFS.

All1362 product files and their inventory are unchanged. Only diagnostic files
and requested experiment records change. No cloud/remote call, new claim,
guest command, production change, merge/tag or local e2e. The last remote state
observation remains D-DEMAND-LIVE's09:59:57UTC2CPU/8GiBStopped/StopCharging
receipt, not a fresh query. Populated roots, actual occupied width and original
regional1s/2s gates remain open. Two document patches failed context matching
before any writes; neither caused a test or remote replay.
