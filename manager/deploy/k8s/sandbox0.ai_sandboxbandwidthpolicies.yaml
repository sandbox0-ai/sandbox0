---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: sandboxbandwidthpolicies.sandbox0.ai
spec:
  group: sandbox0.ai
  names:
    kind: SandboxBandwidthPolicy
    listKind: SandboxBandwidthPolicyList
    plural: sandboxbandwidthpolicies
    singular: sandboxbandwidthpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SandboxBandwidthPolicy defines bandwidth policy for a sandbox
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SandboxBandwidthPolicySpec defines the desired state of SandboxBandwidthPolicy
            properties:
              accounting:
                description: Accounting defines traffic accounting configuration
                properties:
                  enabled:
                    default: true
                    description: Enabled enables traffic accounting
                    type: boolean
                  reportIntervalSeconds:
                    default: 10
                    description: |-
                      ReportIntervalSeconds is the interval for reporting traffic statistics
                      Fixed at 10 seconds per platform policy
                    format: int32
                    type: integer
                required:
                - enabled
                type: object
              egressRateLimit:
                description: EgressRateLimit defines egress rate limiting
                properties:
                  burstBytes:
                    description: BurstBytes is the burst size in bytes
                    format: int64
                    type: integer
                  ceilBps:
                    description: CeilBps is the ceiling rate in bits per second (for
                      HTB)
                    format: int64
                    type: integer
                  rateBps:
                    description: RateBps is the rate limit in bits per second
                    format: int64
                    type: integer
                required:
                - rateBps
                type: object
              ingressRateLimit:
                description: IngressRateLimit defines ingress rate limiting
                properties:
                  burstBytes:
                    description: BurstBytes is the burst size in bytes
                    format: int64
                    type: integer
                  ceilBps:
                    description: CeilBps is the ceiling rate in bits per second (for
                      HTB)
                    format: int64
                    type: integer
                  rateBps:
                    description: RateBps is the rate limit in bits per second
                    format: int64
                    type: integer
                required:
                - rateBps
                type: object
              sandboxId:
                description: SandboxID is the unique identifier of the sandbox this
                  policy applies to
                type: string
              teamId:
                description: TeamID is the team that owns this sandbox
                type: string
            required:
            - sandboxId
            - teamId
            type: object
          status:
            description: SandboxBandwidthPolicyStatus defines the observed state of
              SandboxBandwidthPolicy
            properties:
              currentStats:
                description: CurrentStats contains current bandwidth statistics
                properties:
                  connectionCount:
                    description: ConnectionCount is the total number of connections
                    format: int64
                    type: integer
                  egressBytes:
                    description: EgressBytes is the total egress bytes
                    format: int64
                    type: integer
                  egressPackets:
                    description: EgressPackets is the total egress packets
                    format: int64
                    type: integer
                  ingressBytes:
                    description: IngressBytes is the total ingress bytes
                    format: int64
                    type: integer
                  ingressPackets:
                    description: IngressPackets is the total ingress packets
                    format: int64
                    type: integer
                  lastUpdated:
                    description: LastUpdated is the last time stats were updated
                    format: date-time
                    type: string
                type: object
              lastAppliedTime:
                description: LastAppliedTime is the last time the policy was applied
                format: date-time
                type: string
              message:
                description: Message provides additional information about the status
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation observed by netd
                format: int64
                type: integer
              phase:
                description: Phase is the current phase of the policy
                enum:
                - Pending
                - Applied
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
