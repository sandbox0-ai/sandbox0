# Repository Map

This file is a framework placeholder for repository navigation.

- `sandbox0/` contains the core control plane, data plane, operator, and UI
- `pkg/apispec/openapi.yaml` is the canonical HTTP contract
- `s0/` contains the Sandbox0 CLI implementation
- bundled docs are generated from `sandbox0-ui/apps/website`

Release tooling expands this framework with release-matched bundled docs and
artifact metadata.
