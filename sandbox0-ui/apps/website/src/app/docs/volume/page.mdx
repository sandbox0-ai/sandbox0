# Volume

Volume provides persistent storage for Sandbox0. It is a storage unit independent of the Sandbox lifecycle, allowing data sharing and reuse across multiple Sandboxes.

## Why Volumes?

The default Sandbox filesystem is ephemeral—when a Sandbox is deleted, all its data is lost. Volumes solve this problem:

- **Data Persistence**: Store data that needs long-term retention, such as databases, model files, and user uploads
- **Cross-Sandbox Sharing**: Mount the same Volume to multiple Sandboxes for data sharing
- **Fast Snapshots**: Create point-in-time snapshots in seconds for backup and versioning
- **Fast Forking**: Create independent child Volumes with Copy-on-Write isolation
- **Quick Recovery**: Restore from snapshots quickly, ideal for rollbacks and environment cloning

## Volume and Sandbox Relationship

```
┌─────────────────────────────────────────────────────┐
│                      Volume                         │
│                 (Persistent Storage)                │
│                                                     │
│    /data/                                           │
│    ├── models/                                      │
│    ├── cache/                                       │
│    └── output/                                      │
└──────────┬──────────────┬──────────────┬───────────┘
           │              │              │
           ▼              ▼              ▼
     ┌──────────┐   ┌──────────┐   ┌──────────┐
     │ Sandbox A│   │ Sandbox B│   │ Sandbox C│
     │ (RWX)    │   │ (ROX)    │   │ (RWX)    │
     └──────────┘   └──────────┘   └──────────┘
```

## Access Modes

Volumes support three access modes:

| Mode | Full Name | Description | Typical Use Cases |
|------|-----------|-------------|-------------------|
| `RWO` | Read-Write Once | Single-node read-write | Database storage, exclusive workspaces |
| `ROX` | Read-Only Cross | Multi-node read-only | Shared model files, static resource distribution |
| `RWX` | Read-Write Cross | Multi-node read-write | Collaborative workspaces, shared caches |

<Callout variant="info">
The default access mode is `RWO`. Specify `RWX` mode when creating a Volume if you need cross-Sandbox sharing.
</Callout>

## Performance Configuration

Volumes support the following performance tuning parameters:

| Parameter | Type | Description | Example Values |
|-----------|------|-------------|----------------|
| `cache_size` | string | Cache size | `512M`, `1G` |
| `buffer_size` | string | Buffer size | `128M`, `256M` |
| `prefetch` | integer | Number of blocks to prefetch | `1`, `2`, `4` |
| `writeback` | boolean | Enable write-back cache | `true`, `false` |

<Callout variant="warning">
Enabling `writeback` improves write performance but may lose unwritten data in exceptional cases. Disable it for scenarios requiring strong data consistency.
</Callout>

## Create a Volume

<Tabs
  tabs={[
    { label: "Go", language: "go", code: "package main\n\nimport (\n    \"context\"\n    \"fmt\"\n    \n    \"github.com/sandbox0-ai/sdk-go\"\n    \"github.com/sandbox0-ai/sdk-go/pkg/apispec\"\n)\n\nfunc main() {\n    client := sandbox0.NewClient()\n    ctx := context.Background()\n\n    // Create Volume with default settings\n    volume, err := client.CreateVolume(ctx, apispec.CreateSandboxVolumeRequest{})\n    if err != nil {\n        panic(err)\n    }\n    fmt.Printf(\"Volume created: %s\\n\", volume.ID)\n    \n    // Create Volume with custom access mode and performance settings\n    volume, err = client.CreateVolume(ctx, apispec.CreateSandboxVolumeRequest{\n        AccessMode: apispec.NewOptVolumeAccessMode(apispec.VolumeAccessModeRWX),\n        CacheSize:  apispec.NewOptString(\"512M\"),\n        BufferSize: apispec.NewOptString(\"128M\"),\n        Writeback:  apispec.NewOptBool(true),\n    })\n    if err != nil {\n        panic(err)\n    }\n    fmt.Printf(\"Volume created with RWX mode: %s\\n\", volume.ID)\n}" },
    { label: "Python", language: "python", code: "from sandbox0 import Client\nfrom sandbox0.models import CreateSandboxVolumeRequest, VolumeAccessMode\n\nclient = Client()\n\n# Create Volume with default settings\nvolume = client.volumes.create(CreateSandboxVolumeRequest())\nprint(f\"Volume created: {volume.id}\")\n\n# Create Volume with custom access mode and performance settings\nvolume = client.volumes.create(CreateSandboxVolumeRequest(\n    access_mode=VolumeAccessMode.RWX,\n    cache_size=\"512M\",\n    buffer_size=\"128M\",\n    writeback=True,\n))\nprint(f\"Volume created with RWX mode: {volume.id}\")\n\n# Using context manager for auto-cleanup\nwith client.volumes.create(CreateSandboxVolumeRequest(\n    access_mode=VolumeAccessMode.RWX,\n)) as volume:\n    print(f\"Volume created: {volume.id}\")\n    # Use volume here...\n# Volume is automatically deleted when exiting context" },
    { label: "TypeScript", language: "typescript", code: "import { Sandbox0 } from 'sandbox0';\nimport { VolumeAccessMode } from 'sandbox0/models';\n\nconst client = new Sandbox0();\n\n// Create Volume with default settings\nconst volume = await client.volumes.create({});\nconsole.log(`Volume created: ${volume.id}`);\n\n// Create Volume with custom access mode and performance settings\nconst volumeWithConfig = await client.volumes.create({\n    accessMode: VolumeAccessMode.Rwx,\n    cacheSize: '512M',\n    bufferSize: '128M',\n    writeback: true,\n});\nconsole.log(`Volume created with RWX mode: ${volumeWithConfig.id}`);" },
    { label: "CLI", language: "bash", code: "# Create Volume with default settings\ns0 volume create\n\n# Output:\n# Volume created successfully\n# ID: vol_abc123xyz\n# Access Mode: RWO" }
  ]}
/>

## List Volumes

<Tabs
  tabs={[
    { label: "Go", language: "go", code: "volumes, err := client.ListVolumes(ctx)\nif err != nil {\n    panic(err)\n}\n\nfor _, v := range volumes {\n    fmt.Printf(\"Volume: %s, Mode: %s\\n\", v.ID, v.AccessMode)\n}" },
    { label: "Python", language: "python", code: "volumes = client.volumes.list()\n\nfor v in volumes:\n    print(f\"Volume: {v.id}, Mode: {v.access_mode}\")" },
    { label: "TypeScript", language: "typescript", code: "const volumes = await client.volumes.list();\n\nfor (const v of volumes) {\n    console.log(`Volume: ${v.id}, Mode: ${v.accessMode}`);\n}" },
    { label: "CLI", language: "bash", code: "# List all Volumes\ns0 volume list\n\n# Output:\n# ID              ACCESS MODE   CREATED AT\n# vol_abc123xyz   RWX           2024-01-15 10:30:00\n# vol_def456uvw   RWO           2024-01-14 15:20:00" }
  ]}
/>

## Get Volume Details

<Tabs
  tabs={[
    { label: "Go", language: "go", code: "volume, err := client.GetVolume(ctx, \"vol_abc123xyz\")\nif err != nil {\n    panic(err)\n}\n\nfmt.Printf(\"Volume ID: %s\\n\", volume.ID)\nfmt.Printf(\"Access Mode: %s\\n\", volume.AccessMode)\nfmt.Printf(\"Cache Size: %s\\n\", volume.CacheSize.Value)\nfmt.Printf(\"Created At: %s\\n\", volume.CreatedAt)" },
    { label: "Python", language: "python", code: "volume = client.volumes.get(\"vol_abc123xyz\")\n\nprint(f\"Volume ID: {volume.id}\")\nprint(f\"Access Mode: {volume.access_mode}\")\nprint(f\"Cache Size: {volume.cache_size}\")\nprint(f\"Created At: {volume.created_at}\")" },
    { label: "TypeScript", language: "typescript", code: "const volume = await client.volumes.get('vol_abc123xyz');\n\nconsole.log(`Volume ID: ${volume.id}`);\nconsole.log(`Access Mode: ${volume.accessMode}`);\nconsole.log(`Cache Size: ${volume.cacheSize}`);\nconsole.log(`Created At: ${volume.createdAt}`);" },
    { label: "CLI", language: "bash", code: "# Get Volume details\ns0 volume get vol_abc123xyz\n\n# Output:\n# ID: vol_abc123xyz\n# Access Mode: RWX\n# Cache Size: 512M\n# Buffer Size: 128M\n# Writeback: true\n# Created At: 2024-01-15T10:30:00Z" }
  ]}
/>

## Delete a Volume

<Tabs
  tabs={[
    { label: "Go", language: "go", code: "err := client.DeleteVolume(ctx, \"vol_abc123xyz\")\nif err != nil {\n    panic(err)\n}\nfmt.Println(\"Volume deleted\")" },
    { label: "Python", language: "python", code: "client.volumes.delete(\"vol_abc123xyz\")\nprint(\"Volume deleted\")" },
    { label: "TypeScript", language: "typescript", code: "await client.volumes.delete('vol_abc123xyz');\nconsole.log('Volume deleted');" },
    { label: "CLI", language: "bash", code: "# Delete Volume\ns0 volume delete vol_abc123xyz\n\n# Output:\n# Volume vol_abc123xyz deleted successfully" }
  ]}
/>

<Callout variant="warning">
Ensure all mounts are unmounted before deleting a Volume. If active mounts exist, the delete operation returns a 409 Conflict error.
</Callout>

---

## Next Steps

<CardGrid>
  <LinkCard
    title="Volume Mounts"
    href="/docs/volume/mounts"
    cta="Learn More"
  >
    Mount volumes to sandboxes for persistent storage
  </LinkCard>

  <LinkCard
    title="Snapshots"
    href="/docs/volume/snapshots"
    cta="Learn More"
  >
    Create, restore, and manage volume snapshots
  </LinkCard>

  <LinkCard
    title="Volume Fork"
    href="/docs/volume/fork"
    cta="Learn More"
  >
    Clone a volume with Copy-on-Write isolation
  </LinkCard>

  <LinkCard
    title="Sandbox"
    href="/docs/sandbox"
    cta="Learn More"
  >
    Sandbox lifecycle and execution management
  </LinkCard>
</CardGrid>
