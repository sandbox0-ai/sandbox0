# Get Started

Sandbox0 provides isolated execution environments for AI Agents with persistent storage, session state maintenance, sub-200ms cold starts, and easy self-hosted deployment.

## What is Sandbox0?

Sandbox0 is an enterprise-grade AI Agent Sandbox platform that enables you to:

- **Run code securely** in isolated environments with full process control
- **Persist data** across sandbox lifecycles with volume storage and snapshots
- **Control network access** with fine-grained policies
- **Scale efficiently** with template-based sandbox pools and fast cold starts

### Control Plane vs Data Plane

Sandbox0 follows a control plane / data plane architecture:

| Layer | Components | Purpose |
|-------|------------|---------|
| **Control Plane** | edge-gateway, scheduler | Multi-cluster routing, tenant management, request forwarding |
| **Data Plane** | internal-gateway, manager, storage-proxy, netd | Sandbox lifecycle, process execution, file operations, storage |

Each provider/region (e.g., `aws/us-east-1`) has an independent data plane and control plane deployment with dedicated PostgreSQL and S3 storage.

---

## Quickstart

Get your first sandbox running in minutes.

### Prerequisites

- A self-hosted deployment
- `s0` CLI installed

### Install s0 CLI (Required)

<S0Install />

### Install SDK

<Tabs
  tabs={[
    {
      label: "Go",
      language: "go",
      code: `go get github.com/sandbox0-ai/sdk-go`
    },
    {
      label: "Python",
      language: "python",
      code: `pip install sandbox0`
    },
    {
      label: "TypeScript",
      language: "bash",
      code: `npm install sandbox0`
    }
  ]}
/>

### Authenticate

Set up your credentials. Configure the base URL for your [self-hosted deployment](/docs/self-hosted):

First, create an API key to use as `SANDBOX0_TOKEN`:

```bash
# Login with your account (stores session in ~/.s0/config.yaml)
s0 auth login

# Create a 30-day admin API key and export it
export SANDBOX0_TOKEN="$(s0 apikey create --name test-apikey --role admin --expires-in 30d --raw)"
export SANDBOX0_BASE_URL="http://localhost:30080"
```

<Tabs
  tabs={[
    {
      label: "Go",
      language: "go",
      code: `import sandbox0 "github.com/sandbox0-ai/sdk-go"

client, err := sandbox0.NewClient(
    sandbox0.WithToken(os.Getenv("SANDBOX0_TOKEN")),
    sandbox0.WithBaseURL(os.Getenv("SANDBOX0_BASE_URL")),
)`
    },
    {
      label: "Python",
      language: "python",
      code: `from sandbox0 import Client
import os

client = Client(
    token=os.environ["SANDBOX0_TOKEN"],
    base_url=os.environ.get("SANDBOX0_BASE_URL", "http://localhost:30080"),
)`
    },
    {
      label: "TypeScript",
      language: "typescript",
      code: `import { Client } from 'sandbox0';

const client = new Client({
    token: process.env.SANDBOX0_TOKEN!,
    baseUrl: process.env.SANDBOX0_BASE_URL || 'http://localhost:30080',
});`
    }
  ]}
/>

### Claim Your First Sandbox

Create a sandbox from the default template:

<Tabs
  tabs={[
    {
      label: "Go",
      language: "go",
      code: `package main

import (
    "context"
    "fmt"
    "log"
    "os"

    sandbox0 "github.com/sandbox0-ai/sdk-go"
)

func main() {
    client, err := sandbox0.NewClient(
        sandbox0.WithToken(os.Getenv("SANDBOX0_TOKEN")),
        sandbox0.WithBaseURL(os.Getenv("SANDBOX0_BASE_URL")),
    )
    if err != nil {
        log.Fatal(err)
    }

    ctx := context.Background()

    // Claim a sandbox from the "default" template
    sandbox, err := client.ClaimSandbox(ctx, "default",
        sandbox0.WithSandboxTTL(300),      // Auto-pause after 5 minutes (default: 5m)
        sandbox0.WithSandboxHardTTL(3600), // Auto-delete after 1 hour (optional, 0 disables)
    )
    if err != nil {
        log.Fatal(err)
    }
    fmt.Printf("Sandbox ID: %s\\n", sandbox.ID)
    fmt.Printf("Status: %s\\n", sandbox.Status)

    // Clean up when done
    defer client.DeleteSandbox(ctx, sandbox.ID)
}`
    },
    {
      label: "Python",
      language: "python",
      code: `from sandbox0 import Client
from sandbox0.apispec.models.sandbox_config import SandboxConfig
import os

client = Client(
    token=os.environ["SANDBOX0_TOKEN"],
    base_url=os.environ.get("SANDBOX0_BASE_URL", "http://localhost:30080"),
)

# Claim a sandbox from the "default" template
with client.sandboxes.open("default", config=SandboxConfig(
    ttl=300,       # Auto-pause after 5 minutes (default: 5m)
    hard_ttl=3600, # Auto-delete after 1 hour (optional, 0 disables)
)) as sandbox:
    print(f"Sandbox ID: {sandbox.id}")
    print(f"Status: {sandbox.status}")`
    },
    {
      label: "TypeScript",
      language: "typescript",
      code: `// Run with: bun run main.ts
// Or: npx tsx main.ts (requires @types/node)
import { Client } from 'sandbox0';

const client = new Client({
    token: process.env.SANDBOX0_TOKEN!,
    baseUrl: process.env.SANDBOX0_BASE_URL || 'http://localhost:30080',
});

// Claim a sandbox from the "default" template
const sandbox = await client.sandboxes.claim('default', {
    ttl: 300,       // Auto-pause after 5 minutes (default: 5m)
    hardTtl: 3600,  // Auto-delete after 1 hour (optional, 0 disables)
});
console.log('Sandbox ID:', sandbox.id);
console.log('Status:', sandbox.status);

// Clean up when done
await client.sandboxes.delete(sandbox.id);`
    },
    {
      label: "CLI",
      language: "bash",
      code: `# Claim a sandbox
s0 sandbox create --template default --ttl 300 --hard-ttl 3600

# Output:
# Sandbox ID: sb_abc123
# Status: running

# Clean up when done
s0 sandbox delete sb_abc123`
    }
  ]}
/>

### Run Your First Command

Execute code in your sandbox using `Run` (REPL-style, stateful) or `Cmd` (one-shot command):

<Tabs
  tabs={[
    {
      label: "Go",
      language: "go",
      code: `// Run a REPL-style snippet (stateful; env/vars preserved between calls)
runResult, err := sandbox.Run(ctx, "python", "x = 2")
if err != nil {
    log.Fatal(err)
}
fmt.Print(runResult.OutputRaw)

runResult, err = sandbox.Run(ctx, "python", "print(x)")
if err != nil {
    log.Fatal(err)
}
fmt.Print(runResult.OutputRaw)

// Run a one-shot command (stateless)
cmdResult, err := sandbox.Cmd(ctx, "/bin/sh -c 'echo Hello, Sandbox0!'")
if err != nil {
    log.Fatal(err)
}
fmt.Print(cmdResult.OutputRaw)`
    },
    {
      label: "Python",
      language: "python",
      code: `# Run a REPL-style snippet (stateful; env/vars preserved between calls)
run_result = sandbox.run("python", "x = 2")
print(run_result.output_raw, end="")

run_result = sandbox.run("python", "print(x)")
print(run_result.output_raw, end="")

# Run a one-shot command (stateless)
cmd_result = sandbox.cmd("/bin/sh -c 'echo Hello, Sandbox0!'")
print(cmd_result.output_raw, end="")`
    },
    {
      label: "TypeScript",
      language: "typescript",
      code: `// Run a REPL-style snippet (stateful; env/vars preserved between calls)
const runResult = await sandbox.run('python', 'x = 2');
process.stdout.write(runResult.outputRaw);

const runResult2 = await sandbox.run('python', 'print(x)');
process.stdout.write(runResult2.outputRaw);

// Run a one-shot command (stateless)
const cmdResult = await sandbox.cmd("/bin/sh -c 'echo Hello, Sandbox0!'");
process.stdout.write(cmdResult.outputRaw);`
    },
    {
      label: "CLI",
      language: "bash",
      code: `# Execute a command in the sandbox
s0 sandbox exec sb_abc123 -- echo "Hello, Sandbox0!"

# Output:
# Hello, Sandbox0!`
    }
  ]}
/>

### Basic File Operations

Read and write files in your sandbox:

<Tabs
  tabs={[
    {
      label: "Go",
      language: "go",
      code: `// Write a file
_, err = sandbox.WriteFile(ctx, "/tmp/hello.txt", []byte("Hello from Sandbox0!"))
if err != nil {
    log.Fatal(err)
}

// Read the file back
content, err := sandbox.ReadFile(ctx, "/tmp/hello.txt")
if err != nil {
    log.Fatal(err)
}
fmt.Printf("File content: %s\\n", string(content))`
    },
    {
      label: "Python",
      language: "python",
      code: `# Write a file
sandbox.write_file("/tmp/hello.txt", b"Hello from Sandbox0!")

# Read the file back
content = sandbox.read_file("/tmp/hello.txt")
print(f"File content: {content.decode()}")`
    },
    {
      label: "TypeScript",
      language: "typescript",
      code: `// Write a file
await sandbox.writeFile('/tmp/hello.txt', 'Hello from Sandbox0!');

// Read the file back
const content = await sandbox.readFile('/tmp/hello.txt');
console.log('File content:', Buffer.from(content).toString());`
    },
    {
      label: "CLI",
      language: "bash",
      code: `# Write a file
s0 sandbox files write /tmp/hello.txt --data "Hello from Sandbox0!" -s sb_abc123

# Read a file
s0 sandbox files cat /tmp/hello.txt -s sb_abc123

# Output:
# Hello from Sandbox0!`
    }
  ]}
/>

---

## Next Steps

<CardGrid>
  <LinkCard
    title="Core Concepts"
    href="/docs/get-started/concepts"
    cta="Learn More"
  >
    Learn about sandbox lifecycle, contexts, templates, and volumes
  </LinkCard>

  <LinkCard
    title="Sandbox API"
    href="/docs/sandbox"
    cta="View Docs"
  >
    Complete sandbox lifecycle and management documentation
  </LinkCard>
</CardGrid>
